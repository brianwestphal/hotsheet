{
  "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
  "version": "2.1.0",
  "runs": [
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mrwy26cqfkfo4ix4",
          "message": {
            "text": "Fix is wire-deep, not CSS-only: `summary` was only ever the FIRST LINE of `message.text` (see `prNotesReader.ts::toReviewProofNote`), so wrapping the head alone could never show the note. The new `body` field carries the full text; it renders in the DETAIL (markdown via the `markdownSetup` escape-html marked config — bodies are markdown per Glassbox docs/20) and the head's ellipsis preview hides while open (`.is-open .review-proof-summary { display: none }`) so line 1 isn't duplicated. `body` is optional on the wire with a summary fallback, so an older server during an upgrade window degrades to exactly the pre-fix behavior rather than a validation failure.\n",
            "markdown": "Fix is wire-deep, not CSS-only: `summary` was only ever the FIRST LINE of `message.text` (see `prNotesReader.ts::toReviewProofNote`), so wrapping the head alone could never show the note. The new `body` field carries the full text; it renders in the DETAIL (markdown via the `markdownSetup` escape-html marked config — bodies are markdown per Glassbox docs/20) and the head's ellipsis preview hides while open (`.is-open .review-proof-summary { display: none }`) so line 1 isn't duplicated. `body` is optional on the wire with a summary fallback, so an older server during an upgrade window degrades to exactly the pre-fix behavior rather than a validation failure.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/client/reviewProofSection.tsx"
                },
                "region": {
                  "startLine": 117,
                  "endLine": 129,
                  "snippet": {
                    "text": "  // HS-9387 — the expanded row leads with the FULL note text, markdown-rendered\n  // and wrapping (the head's one-line ellipsis summary hides while open, via\n  // `.is-open` CSS). `body` is optional on the wire (older server) — fall back\n  // to the one-line summary rather than showing nothing.\n  const bodyText = (note.body ?? note.summary).trim();\n  if (bodyText !== '') {\n    detail.appendChild(toElement(\n      <div className=\"review-proof-body\">\n        {/* eslint-disable-next-line kerfjs/no-raw-with-dynamic-arg -- sanitized markdown HTML from `marked.parse(...)` (markdownSetup escapes raw HTML). */}\n        {raw(marked.parse(bodyText, { async: false }))}\n      </div>,\n    ));\n  }"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "HS-9387"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "5cb4cab9f739387842edf3aa903ec3b9"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/hotsheet.git",
          "revisionId": "5ced74d1a0c364f24d30dd268f6c57df1bfe7da2",
          "branch": "main"
        }
      ]
    },
    {
      "tool": {
        "driver": {
          "name": "Hot Sheet",
          "rules": [
            {
              "id": "review-note",
              "name": "ReviewNote",
              "shortDescription": {
                "text": "AI-authored, line-anchored review note."
              }
            }
          ]
        }
      },
      "results": [
        {
          "ruleId": "review-note",
          "ruleIndex": 0,
          "kind": "informational",
          "level": "none",
          "guid": "mrwzfoujflw42rel",
          "message": {
            "text": "`aggregateReviewAction` is a pure exported function precisely so the button-decision matrix (single→range, single-commit→commit, interleaved→chooser+span, started-dirty→uncommitted, fallback→files, else none) is unit-tested deterministically — the section's async render races the detail-panel rebuild (the reason this section has no other e2e), so the e2e only asserts the coarse chooser-renders + range-POST path with routes installed BEFORE navigation. A single-commit group deliberately uses `mode:'commit'` not `range` from `<sha>^`: the `^` base breaks on a repo-root commit and `--commit` is the sharper single-change view.\n",
            "markdown": "`aggregateReviewAction` is a pure exported function precisely so the button-decision matrix (single→range, single-commit→commit, interleaved→chooser+span, started-dirty→uncommitted, fallback→files, else none) is unit-tested deterministically — the section's async render races the detail-panel rebuild (the reason this section has no other e2e), so the e2e only asserts the coarse chooser-renders + range-POST path with routes installed BEFORE navigation. A single-commit group deliberately uses `mode:'commit'` not `range` from `<sha>^`: the `^` base breaks on a repo-root commit and `--commit` is the sharper single-change view.\n"
          },
          "locations": [
            {
              "physicalLocation": {
                "artifactLocation": {
                  "uri": "src/client/reviewProofSection.tsx"
                },
                "region": {
                  "startLine": 191,
                  "endLine": 215,
                  "snippet": {
                    "text": "}\n\n/**\n * Pure — decide the header button's behavior from the discovery result + the\n * notes' anchored files (docs/122 §122.3): one group → direct range/commit review;\n * several → chooser (+ the earliest→latest span option with its unrelated-count\n * caveat when the span exists — HEAD-interleaved case); no commits → the\n * started-and-dirty uncommitted fallback, else a files-mode aggregate over the\n * note-anchored files, else nothing.\n */\nexport function aggregateReviewAction(commits: TicketCommitsResponse | null, noteFiles: string[]): AggregateReviewAction {\n  const groups = commits?.groups ?? [];\n  if (groups.length === 1) {\n    return { kind: 'direct', label: 'Open in Glassbox', req: groupReq(groups[0]) };\n  }\n  if (groups.length > 1) {\n    const options = groups.map(g => ({ label: groupLabel(g), req: groupReq(g) }));\n    if (commits?.span != null) {\n      options.push({\n        label: `Review all, earliest→latest (includes ${String(commits.span.unrelatedCount)} unrelated commit${commits.span.unrelatedCount === 1 ? '' : 's'})`,\n        req: { mode: 'range', from: commits.span.from, to: commits.span.to },\n      });\n    }\n    return { kind: 'chooser', label: 'Open in Glassbox', options };\n  }"
                  }
                }
              }
            }
          ],
          "properties": {
            "tags": [
              "rationale"
            ]
          },
          "workItemUris": [
            "HS-9393"
          ],
          "partialFingerprints": {
            "prNoteAnchor/v1": "e3604b8cd1a87dc60928a424756004fc"
          }
        }
      ],
      "versionControlProvenance": [
        {
          "repositoryUri": "git@github.com:brianwestphal/hotsheet.git",
          "revisionId": "9ac2dbacf7b42c252ac06c15f5bddb226e416c31",
          "branch": "main"
        }
      ]
    }
  ]
}
