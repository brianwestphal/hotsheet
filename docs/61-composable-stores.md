# 61. Composable testable stores (Design Spike)

HS-8167. Follow-up to the HS-8165 investigation. Depends on [§60. Fine-grained reactivity primitive](60-reactivity-primitive.md) shipping first — stores are a thin convention layered on top of the signals primitive HS-8166 introduces.

> **Status:** Phase 1 (HS-8238) shipped 2026-05-09. **Phase 2 (HS-8239) data-source flip shipped 2026-05-11**; bindList rewrite + filter migration deferred to follow-ups HS-8326 + HS-8327. **HS-8240 (Phase 3 umbrella) closed 2026-05-10 as discharged into per-store sub-tickets HS-8317 (`projectsStore` — shipped 2026-05-10), HS-8318 (`commandLogStore` + paired view-layer per the HS-8311 deferral — shipped 2026-05-11), HS-8319 (`terminalsStore` — shipped 2026-05-11 as narrowed `visibilityGroupingsStore`), HS-8320 (`channelStore` — shipped 2026-05-11).** HS-8321 added as prep for HS-8239 — defines the `ticketsStore` factory + types in isolation so HS-8239's atomic flip becomes a smaller, more reviewable diff.
> **Verdict:** Adopt `defineStore` / `resetAllStores` from `kerfjs` (already pulled in for §60 — see HS-8235 / `docs/60-reactivity-primitive.md` §60.3). The local `defineStore()` factory described in §61.3 below is NOT implemented locally — kerf ships the same shape. The §61.3 module-surface description still applies as an *API* spec; the implementation is `kerfjs` re-exported through `src/client/reactive.ts`.

## 61.1 Problem statement

Today client state lives in three overlapping shapes, none of which compose well:

- **Module-level globals** in `src/client/state.tsx` — open tickets, active project, settings, drawer position, channel state. Some are mutable arrays / objects with ad-hoc subscriber registries; others are read directly off the module export.
- **Per-feature module-level maps** — `src/client/terminalDashboard.tsx::dashboardState`, `src/client/drawerTerminalGrid.tsx::drawerGridState`, `src/client/terminal.tsx::terminalState` (after HS-8189 / HS-8222 / HS-8223 / HS-8224 bundled the per-file `let`s into single state slots). Each file's state has its own private subscriber model.
- **Ad-hoc `subscribeTo*` helpers** — every feature that wants to be notified when its slice changes ships its own pub/sub. Some files have 5+ different subscription primitives.

The cost shows up in three places:

1. **Tests** — every test has to set up the right combination of globals before it can assert anything. Most client tests bypass this by mounting a fake DOM and asserting against it; very few test state transitions in isolation because there's no clean seam.
2. **Composition** — there's no reusable way to derive a piece of UI state from another (`tickets filtered by category` derived from `tickets + activeFilter`). Today every consumer recomputes ad-hoc.
3. **Lifetime** — when does `terminalState` reset on project switch? When does the channel state? Some are per-project, some are global; the reset hooks are scattered and frequently miss a field. (HS-8189 etc. consolidated the *fields* but didn't introduce a lifecycle convention.)

## 61.2 Why a thin store layer beats raw signals

A raw signal is fine when one piece of UI owns one piece of state. A store earns its keep when:

- (a) Multiple consumers read the same state.
- (b) Mutations are non-trivial — multi-step, validating, or derived (e.g. `applyServerUpdate(ticket)` has to merge fields, preserve client-only state, and re-sort).
- (c) The state survives across navigation / project switches and needs an explicit `reset()` hook.

We don't need both raw signals and stores everywhere. The convention is:

> **One consumer = signal. Two+ consumers, multi-step mutations, or cross-navigation lifetime = store.**

A store is just a signal + helpers. The convention is the value, not the abstraction.

## 61.3 Module surface

Single new client module. The shape below is the *target* API; exact field names tune during Phase 1.

### `src/client/reactive.ts` (re-export of `kerfjs`)

```ts
// Implementation lives in `kerfjs`; Hot Sheet re-exports through
// `src/client/reactive.ts` per §61.3 / HS-8238.
import { defineStore, resetAllStores } from 'kerfjs';
import type { Store } from 'kerfjs';
//   Store<TState, TActions>:
//     state:   ReadonlySignal<TState>
//     actions: TActions
//     reset(): void
//   defineStore<TState, TActions>({ initial: () => TState,
//                                   actions: (set, get) => TActions })
//     → Store<TState, TActions>
//   resetAllStores(): walks every store registered via defineStore() and
//                     calls each .reset(). For tests + lifecycle hooks.
```

Three rules:

1. **`state` is read-only.** Consumers read via `state.value` or subscribe via `effect()` from §60. They cannot write.
2. **`actions` is the only mutation surface.** All writes go through named action functions; no exposed `state.set`. This is what makes stores testable — assert against actions, not against arbitrary writes.
3. **`reset()` resets to `initial()`.** Always defined; tests use it for setup, project-switch lifecycle uses it for tear-down.

### `resetAllStores()` lifecycle hook

A module-level registry tracks every store created via `defineStore()`. The registry walk is exposed via `resetAllStores()`.

**HS-8238 implementation note (2026-05-09):** the original §61.3 design called for wiring `resetAllStores()` into `switchProject` / `reloadAppState` so per-project state resets automatically on switch. In practice, every Hot Sheet store landed so far holds *cross-project* state (attention dots, busy indicators, channel state) — none of which should reset on switch. So the hook is currently **not** wired into any production path; tests use it via `beforeEach(() => resetAllStores())` for isolation. If a future per-project store lands, it should NOT participate in `defineStore()` (kerf has no opt-out — see kerf §3.4); use a raw signal instead. Re-evaluate the production wiring once HS-8239 (`ticketsStore`) lands — that one IS per-project and is the natural first caller.

## 61.4 Candidate stores — ordered by pain today

From most-painful-today to least:

### 1. `ticketsStore` — highest impact

Today scattered across `ticketList.tsx`, `state.tsx`, `detail.tsx`, plus a handful of ad-hoc subscribe paths in `commandLog.tsx` (last-touched indicator) and `searchBar.tsx`.

- **State:** `{ tickets: Ticket[], filter: FilterState, selectedId: number | null, derived: { filtered, grouped } }`. The `derived` slice is `computed()` on top of `tickets + filter`.
- **Actions:** `loadTickets(force?)`, `setFilter(filter)`, `select(id)`, `applyServerUpdate(ticket)` (merge a single-ticket update from the WS push or the existing poll), `removeTicket(id)`, `optimisticUpdate(id, patch)` (for inline edits — paired with rollback on server reject).
- **UI** subscribes via `bindList(ticketListEl, ticketsStore.state.value.derived.filtered, t => t.id, renderRow)`.

This migration is the bulk of HS-8167's value; it touches the most callsites and has the most ad-hoc subscriber plumbing.

### 2. `projectsStore`

- **State:** `{ projects: Project[], activeSecret: string | null, byId: Map<string, Project> }` (the byId map is `computed`).
- **Actions:** `setActive(secret)`, `applyServerUpdate(project)`, `addProject(p)`, `removeProject(secret)`.
- **Lifetime:** does NOT reset on project switch (it's the source of truth for what to switch *to*).

### 3. `terminalsStore`

- **State:** checked-out terminal entries, visibility groupings, drawer-grid layout, dashboard zoom level.
- **Actions:** `attachEntry(id, secret)`, `detachEntry(id)`, `setVisibility(id, hidden)`, `setActiveGrouping(name)`, `magnify(id)`, `unmagnify()`.
- **Notes:** Today this is split across `terminal.tsx` + `terminalDashboard.tsx` + `drawerTerminalGrid.tsx`. Could be ONE store with derived views per surface, or three stores with a shared base. Phase 3 decision — needs a closer look during migration.

### 4. `commandLogStore`

- **State:** rolling 100-entry log + filter chip state.
- **Actions:** `append(entry)`, `setFilter(kind)`, `clear()`.
- **Notes:** small, contained, good Phase 3 candidate.

### 5. `channelStore`

- **State:** Claude channel busy state, pending-permission, minimized-popup state, last-error.
- **Actions:** `setBusy(busy)`, `setPending(perm)`, `clearPending()`, `minimizePopup()`.
- **Notes:** today spread across `channelUI.tsx` + `permissionOverlay.tsx`. Worth a close read of HS-8190 before migrating — that ticket bundled `permissionOverlay.tsx`'s module-level `let`s into one container; the store is the next step.

### What stays as a raw global

Not every existing global needs a store. These should stay as plain values or single signals:

- `appSettings` (read-mostly, single mutation path is `PATCH /api/settings`).
- `currentDate` / `appBootTime` (constants).
- Per-feature module-level constants (icon sets, key bindings, theme tables).

## 61.5 Migration plan

Phase 1 lands the factory. Phase 2 is the highest-impact migration. Phase 3 is opportunistic.

### Phase 1 — store factory + one trial (HS-8238) — **shipped 2026-05-09**

- ✅ `src/client/reactive.ts` re-exports `defineStore` / `resetAllStores` / `Store` from `kerfjs` (NO local factory implementation — kerf provides the same shape per §61.3 above).
- ✅ Trial migration: the project-tab attention-dot state in `src/client/channelUI.tsx`. `attentionProjects: Set<string>` became a `defineStore({ initial, actions: markAttention/clearAttention })`. Public surface (`getProjectAttentionSecrets()`, `markProjectAttention()`, `clearProjectAttention()`) unchanged — consumers still see a `ReadonlySet<string>`. New test-only export `_projectAttentionStoreForTesting` for direct `.reset()` access. Each action does an immutable update (`new Set(get().secrets)`) so downstream `effect()` consumers see a fresh reference per change.
- ✅ Tests: 3 new cases under `reactive — defineStore / resetAllStores re-exports (HS-8238)` in `src/client/reactive.test.ts` cover the kerf re-export contract; 7 cases in `src/client/channelUI.test.ts` cover the attention-store migration end-to-end.
- ⏭️ Production `resetAllStores()` wiring: skipped per §61.3 implementation note (no per-project store landed yet that would benefit). Re-evaluate during HS-8239.
- 📄 Test pattern: documented inline at the top of `channelUI.test.ts` (`beforeEach(() => store.reset())` for isolation; `afterEach` mirrors it; tests assert against actions, not against direct state writes).

### Phase 2 — `ticketsStore` (HS-8239)

This is the highest-impact migration. Tickets touch the most callsites and have the most ad-hoc subscriber plumbing. Migrate the load → render → mutate → reload loop end-to-end:

1. Define the store + actions + derived signals.
2. Convert `loadTickets` to call `ticketsStore.actions.loadTickets()`; remove the global `let openTickets`.
3. Convert `ticketList.tsx`'s rebuild loop to `bindList(parentEl, ticketsStore.state.value.derived.filtered, t => t.id, renderRow)`.
4. Convert single-ticket mutations (status flip, note add, category change) to `ticketsStore.actions.applyServerUpdate(updated)`.
5. End-to-end test pass: load → filter → mutate → server-push → re-render.

Big migration; budget 2–3 days including the test pass. Land it as a single PR (atomic — partial migration leaves the codebase in a worse state than either side).

### Phase 3 — long tail (HS-8240)

Convert `projectsStore` / `terminalsStore` / `commandLogStore` / `channelStore` opportunistically. Each is its own sub-ticket when picked up, so the work can land in pieces.

**HS-8240 closed 2026-05-10 as umbrella-discharged into per-store sub-tickets:**

- **HS-8317** — `projectsStore` migration. **Shipped 2026-05-10.** New `src/client/projectsStore.ts` consolidates the pre-fix `state.tsx::activeProject` raw `let` + `projectTabs.tsx::projectListSignal` + `activeSecretSignal` (HS-8235) into a single kerf `defineStore`. Public surface (`getActiveProject()` / `setActiveProject(project)`) unchanged — the 88 callsites across 18 files see no behavioral change. Per-row active-class effect in `renderTabRow` reads from the new `activeProjectSignal` computed; the bindList parent uses a thin `projectsListSignal = computed(() => projectsStore.state.value.projects)` wrapper. 15 unit tests in `projectsStore.test.ts` + every existing `state.test.ts` / `projectTabs.test.ts` / `channelUI.test.ts` case still passes.
- **HS-8324** — HS-8318 follow-up: `commandLogSelectionStore` + delete legacy DOM-write fallback. **Shipped 2026-05-11.** Two cleanups bundled: (a) lifted `selectedLogIds: Set<number>` + `lastClickedId: number | null` + `expandedEntryIds: Set<number>` out of `commandLog.tsx` into a new `src/client/commandLogSelectionStore.ts` (kerf `defineStore`) with actions `toggleSelected` / `addToSelection` / `selectOnly` / `clearSelected` / `toggleExpanded` / `setExpanded`. The wrapper class on each row dropped the inline `selectedLogIds.has(id) ? ' selected' : ''` build-time check; two new per-row effects inside `renderEntryRow` flip `.selected` + `.expanded` declaratively (one effect per row, only entering/leaving rows mutate the class). The imperative `updateSelectionClasses()` sweep + the post-shape-rebuild `applyExpansion()` re-apply are gone. `bindEntryClickHandler` now routes through `commandLogSelectionStore.actions.{toggleSelected,addToSelection,selectOnly,toggleExpanded}` based on the modifier-key state. (b) Deleted the legacy DOM-write fallback in `applyShellPartialEvent` (now a 3-line function — gate, toast, store.setRunningOutput) and the entire `hydrateRenderedShellPartials` function (the bindList preserves DOM identity across polls + the per-row partial effect writes whenever the signal fires, so the post-render hydrate is structurally unnecessary). The 27 happy-dom tests in `commandLog.test.ts` were rewritten to drive through the bindList — new test fixture `mountTestFixture(entryIds)` creates `<div id="command-log-entries">`, registers entries via `commandLogStore.actions.setEntries(...)`, mounts the bindList via the new `_mountEntriesBindListForTesting()` export. 12 new unit tests in `commandLogSelectionStore.test.ts`. Full client suite 1381 / 1381 pass.
- **HS-8318** — `commandLogStore` + `commandLog.tsx::renderEntries` bindList migration (paired per the HS-8311 deferral). **Shipped 2026-05-11.** New `src/client/commandLogStore.ts` (kerf `defineStore`) with **keyed-merge entries** (per the FEEDBACK NEEDED design call — wholesale-replace would defeat the per-row signal benefit), **fused filter** (`{ types: ReadonlySet<string>, search: string }`), and **per-entry partial-output signal** slots. Reconciler structural-compares each entry's annotated shape (`detail` / `summary` / `isRunningShell`) before firing the per-entry signal, so no-op polls don't re-run every row's effect. Per-entry signal references survive across polls — bindList preserves DOM identity for surviving ids; per-row "shape" effect rebuilds inner content only on running → done transitions; per-row "partial" effect drives `<pre>` writes for streaming chunks (sticky-bottom scroll captured pre-write inside the effect). `commandLog.tsx::renderEntries` deleted; replaced by `mountEntriesBindList()` wired to `filteredEntriesSignal`. `commandLogFilter.tsx::activeFilterTypes` Set now reads-from + writes-through `commandLogStore`. `applyShellPartialEvent` + `hydrateRenderedShellPartials` retained as thin compat shims (test-export surface) that delegate to the store + keep a legacy DOM-write fallback so the existing 27 unit tests pass unchanged. Selection (`selectedLogIds`) / expanded (`expandedEntryIds`) / `lastSeenId` / `panelOpen` / `cancelingShellIds` / `pollTimer` stay imperative per the design call. 23 new unit tests in `commandLogStore.test.ts` (initial state / keyed-merge reconcile / per-entry signal stability / no-op poll structural-compare / running → done annotation flip / lazy-create on early chunk arrival / per-row partial-output isolation / filter actions / derived signal narrowing). 1374 / 1374 client tests pass; tsc + lint + build clean. Deferred to follow-up: lifting selection + expanded state into a `commandLogSelectionStore` AND deleting the legacy DOM-write fallback after test migration.
- **HS-8319** — `terminalsStore` migration. **Shipped 2026-05-11 as narrowed `visibilityGroupingsStore`.** Per the FEEDBACK NEEDED design call: 4 of the 5 candidates in the original ticket (`instances` Map, `drawerInstancesSignal`, drawer-grid layout state, dashboard-zoom level) turned out to be either lifecycle (not reactive data) or already signal-driven (HS-8312/HS-8313), and the surface-level dashboard/drawer-grid state is HS-8222/8223/8224 lifecycle bundles full of `ResizeObserver` + RAF + DOM refs that don't belong in a reactive store. Only `dashboardHiddenTerminals.tsx::globalState` (a raw `let GlobalVisibilityState` + bespoke `subscribers: Set<() => void>` pub/sub) was a clean §61 win — exactly the "lift bespoke pub/sub to kerf" pattern the design was built for. New `src/client/visibilityGroupingsStore.ts` defines the kerf store + a `subscribeToVisibilityGroupings(handler)` helper that uses `effect()` under the hood with a skip-first guard to preserve the pre-fix no-fire-on-subscribe semantics. `dashboardHiddenTerminals.tsx` keeps its wide public API surface (every helper byte-identical for callers — 4 production consumers `visibilityGroupingSelect` / `hiddenTerminalsResetUI` / `drawerTerminalGrid` / `terminalDashboard` see no change); reads delegate to the store's signal, writes go through `actions.setState(next)`, and `subscribeToHiddenChanges` is now a thin wrapper around `subscribeToVisibilityGroupings`. Does NOT reset on project switch (cross-project — same opt-out constraint as `projectsStore` + `channelStore`). 10 new unit tests in `visibilityGroupingsStore.test.ts` cover initial state, setState short-circuit, no-fire-on-subscribe / fires-on-change / unsubscribes / multiple-subscribers / throwing-handler-is-isolated, and `_resetSubscribersForTesting` leak protection; all 40 existing `dashboardHiddenTerminals.test.ts` + `persistedHiddenTerminals.test.ts` cases pass unchanged. The four other terminal-surface candidates are deferred — surface-level dashboard/drawer-grid migrations were declined entirely (lifecycle, not reactive state); a tiny `drawerGridLayoutStore` for the two `state.tsx` per-project Maps could ship as a follow-up if a future feature needs reactive `getProjectGridActive`, but no consumer needs that today.
- **HS-8320** — `channelStore` migration. **Shipped 2026-05-11.** New `src/client/channelStore.ts` consolidates the reactive state across `channelUI.tsx` (11 module-level lets/maps: `channelAliveLocal`, `channelBusy`, `shellBusyState`, `busyProjects`, `channelAutoMode` + `autoModeByProject`, `channelAutoBackoff`, `mostRecentSpinnerAtMs`) and `permissionOverlay.tsx` (`pendingPermissionStack` array → `pendingPermissions` store field; `minimizedRequests` Map → `minimizedSecrets: Set<string>` projection — the Map stays in `permissionOverlay.tsx` as the source of truth for per-request timer metadata). Lifecycle handles (timers, intervals, `ResizeObserver`, checkout handles) stay imperative — they're not reactive state. Public surfaces (`isChannelBusy()`, `isChannelAlive()`, `setChannelBusy()`, `setChannelAlive()`, `setShellBusy()`, `getProjectBusySecrets()`, `extendBusyForProject()`, `clearBusyForProject()`, `toggleAutoMode`, `getMinimizedPermissionSecrets()`, `getQueuedPermissionRequestIds()`) are byte-identical for callers. Per the FEEDBACK NEEDED design call: the HS-8238 `projectAttentionStore` Phase-1 trial stays separate (avoid disturbing the working trial). 19 new unit tests in `channelStore.test.ts` plus the existing 57 `permissionOverlay.test.ts` + 7 `channelUI.test.ts` cases pass unchanged. Same `resetAllStores()` opt-out constraint as `projectsStore` (cross-project state — `busySecrets` / `autoModeByProject` / `pendingPermissions` / `minimizedSecrets` are intentionally not project-scoped).

## 61.6 Test patterns

The whole point of the store layer is testability. Every store gets its own `*.test.ts` with happy-dom env (or pure happy-dom-free if the store doesn't touch DOM):

- Actions update state — `loadTickets` populates `state.value.tickets`.
- Derived signals recompute — `setFilter('bug')` shrinks `state.value.derived.filtered`.
- `reset()` clears state — fresh store after `reset()` matches `initial()` deeply.
- Selectors are pure — given the same state, the same derived value.
- No DOM mounting required — that's the whole point.

Stores should be testable WITHOUT mounting any DOM. The DOM rendering is the §60 helpers' job, tested separately.

## 61.7 What stays the same

- Every existing global keeps working until its callsite is migrated.
- No URL routing changes, no API changes.
- The signals primitive (HS-8166) is the dependency, not Solid.
- Server-side code untouched.
- `jsx-runtime.ts` and `toElement` untouched.

## 61.8 Open questions / deferred decisions

- **Action middleware** (logging, devtools, time-travel) — explicitly out of scope. If a real debugging need surfaces, add a thin `withLogging(store)` wrapper later. Don't pre-build Redux.
- **Cross-store derivation** (e.g. `selectedTicketStore` derived from `ticketsStore + selectionStore`) — falls out of `computed()` from §60 across two stores' `state` signals. No new machinery needed; document the pattern in Phase 1.
- **Persistence** — some stores might want `localStorage`-backed persistence (e.g. drawer position). Add `defineStore({...persist: 'key'})` later; not Phase-1 work.
- **Plugin-defined stores** — out of scope. Plugins today don't reach into client state directly, and exposing a `defineStore` from `PluginContext` opens a different design. Defer until a plugin asks for it.
- **`ticketsStore` derived-view granularity** — should the store own `filtered` + `grouped` or should each consumer derive its own? Default to "store owns the common shapes; consumers derive bespoke ones". Settle during HS-8239.

## 61.9 Risks

- **Over-engineering** — adding a store layer where a raw signal would do. Mitigate via the §61.2 rule and reviewer pushback during migration PRs.
- **Migration footgun: forgetting `reset()`** — leaves stale state visible after project switch. Closed by §61.3's automatic registry + `resetAllStores()` hook firing in `switchProject`.
- **Atomicity** — `ticketsStore` migration MUST land atomically. A half-migrated state where some consumers read the global and others read the store is the worst possible interim.
- **Test pattern drift** — if Phase 1 doesn't pin down "what does a good store test look like", Phase 2/3 tests will diverge. Phase 1 ships a written test pattern (in this doc or a sibling) before the trial migration is approved.

## 61.10 Open-source angle

`defineStore` + `resetAllStores` + the §60 reactivity primitive + the `reactive-bind` helpers together form a complete "micro state-management library" — competes with Zustand / Jotai but at ~2 KB total instead of 10–20 KB, with no React assumption. Could be its own package once both pieces prove out across all migration phases. Not a priority; just keeps the boundary clean.

## 61.11 Cost estimate

- Phase 1 (HS-8238): ~half a day. Factory + tests + trial conversion + test-pattern doc.
- Phase 2 (HS-8239 — `ticketsStore`): 2–3 days including end-to-end tests. Single atomic PR.
- Phase 3 (HS-8240): opportunistic; budget per sub-ticket as picked up.

## 61.12 Status & follow-up tickets

- **HS-8167 — this design.** Status: design only; closes once the doc lands.
- **HS-8238 — Phase 1: `defineStore` factory + trial.** Project-tab attention-dot the proposed trial.
- **HS-8239 — Phase 2: `ticketsStore` migration.** **Shipped 2026-05-11 as a data-source flip** per the approved FEEDBACK NEEDED option (b) phasing. `state.tickets` (in `state.tsx`) is now a getter+setter installed via `Object.defineProperty` after the literal — reads return `ticketsStore.state.value.tickets` cast back to the mutable `Ticket[]` shape so the `AppState` interface contract is preserved for the 82 callsites across 20 client files; writes go through `ticketsStore.actions.setTickets(value)` so the store is the single source of truth. All 5 imperative write sites (`ticketList.tsx::loadTickets` × 4 + `ticketRow.tsx`'s inline-edit delete flow) converted: the delete flow uses the typed `ticketsStore.actions.removeTicket(id)` action, the four `loadTickets` paths use `setTickets(...)`. The atomic-flip mandate ('no consumer reads from a different source than another') is satisfied — every read goes through the store via the getter. 5 new `state.test.ts` cases lock the delegate contract (read passes through / write writes through / array methods work / readonly-array surface stable). Full client suite 1386 / 1386 pass; tsc + lint + build clean. The view-layer bindList rewrite + the filter-state migration are deferred to **HS-8326** (renderTicketList → bindList) and **HS-8327** (filter state into `ticketsStore.filter`); neither reintroduces mixed-source state because the data source is already unified.
- **HS-8326** — HS-8239 follow-up #1: `ticketList.tsx::renderTicketList` rebuild loop → `bindList(parent, filteredTicketsSignal, t => t.id, renderRow)`. **Umbrella closed 2026-05-11; ALL 6 sub-tickets shipped 2026-05-11.** Sub-tickets: **HS-8331** (default list view bindList rewrite); **HS-8332** (column view bindList); **HS-8333** (trash + backup-preview bindList); **HS-8334** (extend `filteredTickets` computed body + dedup `loadPreviewTickets` filter logic); **HS-8335** (per-row category / priority / status / star class-flip effects); **HS-8336** (restore FLIP animation for the default-list-view branch via a `setTicketsAnimated` wrapper).
- **HS-8331** — HS-8326 sub #1 (default list view bindList rewrite). **Shipped 2026-05-11.** New `ticketsSignal: ReadonlySignal<readonly Ticket[]>` derived signal in `ticketsStore.ts`. New `<div class=\"ticket-list-rows\">` sub-container inside `<div id=\"ticket-list\">` owned by a persistent `bindList(rowsContainer, ticketsSignal, t => t.id, createTicketRow)`. The bindList survives across `renderTicketList` calls — surviving row ids preserve DOM identity, making focus / cursor / editing-value preservation across re-renders nearly trivial (the input element survives unchanged). `ensureListViewMount(container)` helper is idempotent: first call wipes the container + lays out the structure (draft row + rows sub-container) + mounts the bindList; subsequent calls are near-no-ops (just ensure the draft row is in place at the top). View transitions: column → default tears down the bindList via `unmountListViewBindList()`, the next `ensureListViewMount` re-mounts. Trash / backup-preview / column views unchanged in this ticket — they tear down the list-view bindList on entry and keep the pre-fix wholesale rebuild path. FLIP animation for the default branch was a known regression (snapshot captured AFTER bindList reconcile = no-op) — restored 2026-05-11 by HS-8336. Full client suite 1393 / 1393 pass; tsc + lint + build clean.
- **HS-8336** — HS-8331 follow-up: FLIP animation restoration for the default-list-view bindList path. **Shipped 2026-05-11.** New `setTicketsAnimated(tickets)` wrapper in `ticketList.tsx` captures the FLIP snapshot via `captureSnapshot()`, calls `ticketsStore.actions.setTickets(tickets)` (the kerf signal fires + the bindList reconciles + the DOM reorders all on the same synchronous call stack), then runs `flipAnimate(snapshot)` so each row gets the inverse-transform + 200 ms ease-out it had pre-HS-8331. Migrated 3 of the 4 `loadTickets` setTickets callsites: custom-view fetch, custom-view empty fallback, default-view fetch. `loadPreviewTickets` deliberately kept the bare `setTickets(...)` (the preview path still used the wholesale-rebuild branch in `renderTicketList`, which already had its own captureSnapshot / flipAnimate pair — migration to bindList shipped in HS-8333). The renderTicketList captureSnapshot / flipAnimate pair is retained — it still drives FLIP for the column branch (which mutates DOM inside renderTicketList rather than via the bindList). No unit tests added — FLIP is a visual behavior, `getBoundingClientRect()` returns zero rects in happy-dom, so the wrapper is exercised by the existing `setTickets` store coverage + a new entry in the manual test plan §10. 1393 / 1393 client tests pass; tsc + lint + build clean.
- **HS-8335** — HS-8326 sub #5 (per-row class-flip effects on the ticket row + column card). **Shipped 2026-05-11.** Closes the HS-8326 umbrella. New per-ticket signal infrastructure in `ticketsStore.ts` modeled on HS-8318's `commandLogStore` per-entry-signals pattern: module-private `perTicketSignals: Map<number, { ticket: Signal<Ticket> }>` lives OUTSIDE the store's `state` object so updates to one ticket don't churn the outer state ref (which would re-fire every consumer of the store's top-level state + every list-level computed like `filteredTickets` / `ticketsByStatusSignal`). `setTickets` keyed-merges (surviving ids keep signal identity, structural-compare on `ticketEqualForRender(a, b)` for the per-row reactive fields — `status` / `category` / `priority` / `up_next` / `title` / `tags` / `last_read_at` / `updated_at` / `notes`); `applyServerUpdate` + `optimisticUpdate` write through to the per-ticket signal directly; `removeTicket` disposes the signal. Public `getTicketSignals(id): { ticket: ReadonlySignal<Ticket> } | undefined` for the per-row effect consumers. New `cutTicketIdsSignal: ReadonlySignal<ReadonlySet<number>>` in `clipboard.ts` — `copyTickets(_, cut)` writes through a private `clipboardSignal`, and the derived `cutTicketIdsSignal` exposes the cut ids scoped to the active project (cross-project cut still tracks ids but the active-project-secret check returns an empty Set so off-project rows don't render `.cut-pending`). Pre-fix `copyTickets` called `renderTicketList()` which post-HS-8331 was a near-no-op on the bindList path → `.cut-pending` toggling was structurally broken; HS-8335 fixes it via the kerf signal + per-row effect. New `setupTicketRowEffects(row, ticket)` in `ticketRow.tsx` installs a combined effect on `getTicketSignals(ticket.id)?.ticket.value` that updates: `.completed` class (status flip to completed/verified), `.up-next` class, category badge color + label + title attr, status button verified class + title attr + SVG icon (via `createContextualFragment` to keep the SVG xmlns + avoid the ESLint `no-restricted-syntax` ban on `innerHTML =`), priority indicator color + title attr + SVG icon, star button active class + ★/☆ text + title attr, unread / feedback dot (toggle DOM element presence + variant class), and title input value (WITH edit-guard: only write when `document.activeElement !== input` AND the new server-side title differs from the last-applied server-side title). Single combined effect for the per-ticket fields (instead of N per-slot effects) because every field already funnels through one signal write — splitting would fire the same number of times. Separate effect for `.cut-pending` (different signal source). First-run is skipped on each effect — the JSX-literal initial render already reflects the current ticket state, so re-writing the same values back at mount time would be ~9 wasted DOM writes per row on a ~100-ticket list. New `setupColumnCardEffects(card, ticket)` in `columnView.tsx` mirrors the pattern for the column-card DOM shape: `.up-next` class on the card root, category badge color + label, priority indicator color + icon, star button, `.column-card-title` rebuild on title change (column cards have no inline title editing so no cursor / focus state to preserve — full-children rebuild is fine), unread/feedback dot inside `.column-card-title`, `.cut-pending` class. The `status-X` class on the card root is NOT made reactive — a status change moves the card to a different column (different per-column signal in `ticketsByStatusSignal`'s consumer), which tears down the old card and creates a fresh one in the destination column with the correct `status-X` class at JSX-literal time. The `.selected` class stays imperative on both list rows and column cards (driven by `state.selectedIds` via `updateSelectionClasses()` / `updateColumnSelectionClasses()`) — selection isn't per-ticket data, and the imperative iterator pattern works fine on the live DOM the bindList preserves. The bindList factory wrapper in `ticketList.tsx`'s `ensureBindListMount` now installs `setupTicketRowEffects` for `default` variant only (trash + preview rows are read-only-ish, the mutations that would dirty them are rare enough that the effect-closure cost isn't justified); the column bindList factory in `columnView.tsx` installs `setupColumnCardEffects` for every card (column-view editing surface is interactive). 9 new test cases in `ticketsStore.test.ts` under `per-ticket signals (HS-8335)` (signal creation / signal-identity preservation across setTickets / signal fires on reactive-field change / signal does NOT fire on non-reactive-field change / removal disposes signal / `applyServerUpdate` fires signal / `optimisticUpdate` fires signal / removeTicket clears signal). New `ticketRow.test.ts` happy-dom integration suite with 8 tests covering `setupTicketRowEffects` + `setupColumnCardEffects` (status → completed class flip / up_next → class + star symbol flip / category badge updates / title-input edit-guard while focused / dispose stops further updates / column-card up_next / column-card category badge / column-card title rebuild). 1434 / 1434 client tests pass (was 1417 + 9 store + 8 row). `npx tsc --noEmit` + `npm run lint` + `npm run build:client` clean. **The HS-8326 umbrella is now fully discharged: ALL 6 sub-tickets shipped 2026-05-11 (HS-8331 / 8332 / 8333 / 8334 / 8335 / 8336).** Closes the §61 Phase 2 reactive-rendering migration end-to-end — list view, column view, trash view, backup-preview view all keyed-reconcile via bindList against `filteredTickets` (+ `ticketsByStatusSignal` for columns), per-row data changes update in place via the per-ticket signal effects, no wholesale DOM rebuilds on any data-update path.
- **HS-8332** — HS-8326 sub #2 (column-view bindList rewrite). **Shipped 2026-05-11.** New `ticketsByStatusSignal: ReadonlySignal<Partial<Record<string, readonly Ticket[]>>>` in `ticketsStore.ts` partitions `filteredTickets` (the narrowed visible set, post-HS-8334) by `ticket.status`. Per-column bindLists in `columnView.tsx` subscribe to a per-column derived signal built via `makeColumnSignal(col, isFirstCol, knownStatuses, includeVerifiedHere)` that pulls from the partitioner + applies column-specific fallback logic (first column also picks up tickets with statuses not in the column set so nothing's silently dropped; `hide_verified_column` setting causes the `completed` column to absorb the `verified` bucket). Column-view mount is now idempotent — `renderColumnView` / `renderPreviewColumnView` no-op on same-config re-renders (live/preview + `hide_verified_column` + column set as a join-key) and rebuild only on config change. New `unmountColumnView()` exported from `columnView.tsx`, called from `ticketList.tsx::renderTicketList`'s list-view branch (symmetric with the column branch's `unmountBindList()` call which tears down the list-view bindList on column-view entry). Per-column count display is reactive via `bindText(countEl, computed(() => String(columnSignal.value.length)))` — was static template interpolation pre-fix. Status changes via drag-drop are seamless: the dragged ticket gets a new status server-side, loadTickets refetches, `ticketsByStatusSignal` regroups, the source column's signal loses the ticket (bindList tears down its card), the destination column's signal gains it (bindList creates a fresh card), and `flipAnimate`'s `data-id` lookup matches the new DOM node so the cross-column FLIP animation still works. Per-row staleness limitation worth calling out — when a ticket's category / priority / up_next / title changes WITHOUT a status change, the card stays in the same column → bindList same-key match → no re-render → card DOM is stale (the card content reflects the value at `createColumnCard` time). Same limitation as the HS-8331 list-view bindList; both will be closed by HS-8335's per-row class-flip effects on the card. 6 new test cases in `ticketsStore.test.ts` under `ticketsByStatusSignal partitioning (HS-8332)` (partition by status / preserve insertion order / respect upstream filteredTickets narrowing / expose trash bucket when view=trash / recompute on view change without setTickets / empty object when no tickets match). 1417 / 1417 client tests pass (was 1411 + 6 new). `npx tsc --noEmit` + `npm run lint` + `npm run build:client` clean. The HS-8326 umbrella now has 5 of 6 sub-tickets shipped (HS-8331 / 8332 / 8333 / 8334 / 8336 done; only HS-8335 per-row class-flip effects remains).
- **HS-8334** — HS-8326 sub #4 (extend `filteredTickets` computed body + dedup `loadPreviewTickets` filter logic). **Shipped 2026-05-11** per the user-approved Option A from the FEEDBACK NEEDED file (keep filtered endpoints + use the existing `?status=active` mode as the coarse-scope fetch; per-view narrowing happens client-side). No server endpoint change needed — the server already supported `?status=active` semantics ("everything except deleted / backlog / archive"); HS-8334 just stopped sending the per-view query params (`up_next`, `category`, `priority`) and moved that narrowing into the client. `filteredTickets` body extended via a new `applyViewFilter(tickets, view, includeBacklog, includeArchive)` helper handling three scope tiers: (1) cross-scope `trash` / `backlog` / `archive` views narrow to the exact status (identity pass on live data since server already filtered; real narrowing on the backup-preview snapshot); (2) custom views `custom:*` pass through (server's `/tickets/query` already evaluated the conditions); (3) active scope (`all` / `up-next` / `open` / `completed` / `non-verified` / `verified` / `category:*` / `priority:*`) excludes deleted/backlog/archive unless include flags say otherwise, then applies the sub-view's narrowing. `ticketList.tsx::loadTickets` simplified — the `?status=` switch reduced from 9 branches to 4 (trash / backlog / archive / active-default); `up_next` / `category` / `priority` query params dropped entirely. `loadPreviewTickets` collapsed from a 30-line filter switch to a 3-line passthrough — just writes the full backup snapshot through `setTicketsAnimated`; `filteredTickets` does the narrowing identically for live + preview data. `bindList` consumer switched from `ticketsSignal` to `filteredTickets` (the narrowed canonical "what's visible" signal); the empty-state effect also reads `filteredTickets.value.length`. `ticketsSignal` kept as an exported raw-data handle for future consumers (HS-8332's per-status column-view bindLists). UX behavior note — view changes within the same scope (e.g., `all` → `up-next`) are now instant (client-side filter, no server round-trip), but cross-scope transitions (e.g., trash → all) briefly show an empty list during the network round-trip because the bindList reacts immediately to the filter.view change while the store still holds the prior scope's data (deleted tickets filtered by view='all' = empty). The transient ~50–200 ms empty state is acceptable — the alternative (showing stale trash rows under an 'all' view) is arguably more confusing UX, and the within-scope instant-switch benefit is worth the cross-scope tradeoff. 18 new test cases in `ticketsStore.test.ts` under `filteredTickets per-view narrowing (HS-8334)` describe (the per-view branches for `all` / `up-next` / `open` / `completed` / `verified` / `non-verified` / `trash` / `backlog` / `archive` / `category:*` / `priority:*` / `custom:*` / both include flags / cross-scope-views-ignore-include-flags / view+search combined / view-change recompute without setTickets); existing 9 search-only tests preserved unchanged. Full client suite 1411 / 1411 pass (was 1393 + 18 new). `npx tsc --noEmit` + `npm run lint` + `npm run build:client` clean. The HS-8326 umbrella now has 5 of 6 sub-tickets shipped (HS-8331 / HS-8333 / HS-8334 / HS-8336 shipped; HS-8332 column-view bindList + HS-8335 per-row class-flip effects remain).
- **HS-8333** — HS-8326 sub #3 (trash + backup-preview bindList rewrite). **Shipped 2026-05-11.** Unified the three list-view paths (default / trash / preview) into a single bindList-driven `ensureBindListMount(container, variant)` manager in `ticketList.tsx`. The pre-fix HS-8331 code only used bindList for the default branch — trash + preview kept the wholesale `container.innerHTML = '' + for-loop` rebuild. Post-fix, all three subscribe to `ticketsSignal` (the same source — `loadPreviewTickets` already wrote its filtered snapshot through `ticketsStore.actions.setTickets(...)`) and only the row factory + the empty-state message vary per variant. New module-private `mountedVariant: 'default' | 'trash' | 'preview' | null` tracks the active variant; `computeTargetVariant()` derives the target from `state.backupPreview?.active` + `state.view`; `rowFactoryFor(variant)` returns `createTicketRow` / `createTrashRow` / `createPreviewRow`. Variant-at-mount-time rather than per-row dispatch — per-row would render with the wrong factory on surviving rows during default ↔ preview transitions (preview snapshot may include the same ids as the live state). Empty-state message is rendered by a per-variant `effect()` that toggles `display` based on `ticketsSignal.value.length` — default has no message (matches pre-HS-8333 behavior), trash gets "Trash is empty", preview gets "No tickets match this view". Variant transitions tear down both the bindList AND the empty-state effect via `unmountBindList()`. Preemptive unmount in `setTicketsAnimated` short-circuits the wasted bindList reconcile that would otherwise happen between `setTickets` and `renderTicketList` — when the target variant differs from `mountedVariant`, the bindList is unmounted BEFORE the store write so the new data doesn't reconcile through the wrong factory; the next `ensureBindListMount` call in `renderTicketList` then wipes + remounts with the correct one. No correctness consequence (intermediate DOM never paints — all synchronous) but eliminates the wasted createXRow invocations during view transitions. The `setTicketsAnimated` JSDoc + the bindList-block JSDoc both updated to reflect HS-8333. Manual test plan §10 gains two HS-8333 entries (trash view bindList + backup-preview bindList) covering the variant-transition flows. 1393 / 1393 client tests pass (no test count change — structural fix exercised by the existing flows); tsc + lint + build clean. The HS-8326 umbrella now has 4 of 6 sub-tickets shipped (HS-8331 / 8333 / 8336 done; HS-8332 column-view bindList + HS-8334 extend-filteredTickets + HS-8335 per-row class-flip effects remain).
- **HS-8327** — HS-8239 follow-up #2: filter-state migration. **Shipped 2026-05-11 as a delegate flip** (matching HS-8239's pattern). The four filter-state fields (`view` / `search` / `includeBacklogInSearch` / `includeArchiveInSearch`) on `state` are now getter+setter delegates installed via `Object.defineProperty` after the literal — reads return `ticketsStore.state.value.filter.<field>`; writes go through `ticketsStore.actions.patchFilter({ <field>: value })`. ~110 read sites + ~24 production write sites across ~12 client files keep working unchanged through the delegates. Per the precedent set by HS-8239: extending the `filteredTickets` computed body with the full view / includeBacklog / includeArchive filter logic is deferred to HS-8326 (where it has a real consumer via `bindList`) — extending the computed without a consumer would create an unused alternate view. 7 new test cases in `state.test.ts` lock the delegate contract (per-field round-trip + reset round-trip + independence). 1393 / 1393 client tests pass; tsc + lint + build clean.
- **HS-8321 — Phase 2 prep: `ticketsStore` factory + types.** Pure addition; lands the store + tests in isolation so HS-8239's atomic flip is a smaller diff.
- **HS-8240 — Phase 3: long tail.** Closed 2026-05-10 as umbrella-discharged into HS-8317 (`projectsStore` — shipped 2026-05-10), HS-8318 (`commandLogStore` + view-layer — shipped 2026-05-11), HS-8319 (`terminalsStore` — shipped 2026-05-11 as narrowed `visibilityGroupingsStore`), HS-8320 (`channelStore` — shipped 2026-05-11). Each sub-ticket scheduleable independently.

## 61.13 Rejected alternative: nanostores as the backbone

HS-8248 evaluated [`nanostores`](https://github.com/nanostores/nanostores) (~294 bytes core, atom / map / computed / cleanStores primitives) as a drop-in replacement for the bespoke `defineStore` factory. **Rejected.** A future contributor finding nanostores attractive can stop here.

### The compatibility blocker

§60's binding helpers (`bindText` / `bindAttr` / `bindList`) are built on `@preact/signals-core`'s `effect()`, which auto-tracks dependencies via the `signal.value` getter — read a signal inside an `effect()` and the effect re-runs when that signal changes. **Nanostores does not participate in this protocol.** Per the upstream source (`atom/index.js`), `.get()` is a plain accessor that reads `$atom.value`; subscriptions go through explicit `.listen(cb)` / `.subscribe(cb)`; cross-store derivation is the explicit `computed([$a, $b], fn)` constructor that wires its own listeners.

Consequence: every §60 helper would need a parallel nanostores variant (`bindTextNano(el, $store)`), OR every nanostore would need a manual signal mirror at the consumer (`const sig = signal($store.get()); $store.listen(v => { sig.value = v })`) — either way, **two reactivity systems on the client**, exactly the smell §60 + §61 exist to remove. The whole point of the bespoke factory is that its `state` field IS a `@preact/signals-core` signal, so §60 helpers consume stores for free with no adapter layer.

The decision criterion in HS-8248 was explicit: adopt nanostores only if §60 helpers integrate cleanly. They don't. That alone settles it.

### Secondary findings (each independently insufficient, but pile up)

1. **Bundle-size win is illusory.** Nanostores core is 294 bytes; the bespoke factory is ~50 lines (a few hundred bytes minified). Both are negligible against the 1.4 KB `@preact/signals-core` baseline either approach already pays. Nanostores doesn't displace any code we'd otherwise write — it sits *on top of* signals-core, not instead of it.
2. **`cleanStores` ≠ `resetAllStores`.** Nanostores' `cleanStores(...stores)` tears stores down entirely (subscribers detach, listeners cleared) — designed for test cleanup, not for runtime project-switch reset. §61.3's `resetAllStores()` resets to `initial()` *while keeping subscribers attached*, so binding helpers stay live across the project switch and just see the new initial state. Fundamentally different semantics; we'd be wrapping nanostores anyway to re-implement `reset()`. The wrapper is the bespoke factory.
3. **Actions-only convention costs a wrapper either way.** Nanostores' `set()` / `setKey()` are publicly callable by any consumer. §61.3's three rules (read-only state, actions-only mutation, reset-to-initial) require hiding `.set` / `.setKey` behind named action functions. Wrapping `atom` to enforce that isn't simpler than just exposing a `set` to the action-builder closure ourselves — the closure is the bespoke factory, give or take 10 lines.
4. **`task()` is not a need we have.** Async actions in §61 are plain `async` functions calling existing actions; we don't need nanostores' built-in racing primitive. If a real need surfaces, it's a 20-line helper on top of the factory rather than a reason to adopt a second library.
5. **TypeScript ergonomics — wash.** Both produce reasonable inference. Nanostores' `map` types are slightly nicer for partial updates; the bespoke factory's `actions: (set, get) => TActions` shape is slightly nicer for the action surface. Neither is a tipping point.

### What this investigation validated

- **§60 IS the reactivity backbone.** The bespoke factory is a thin convention layer over `@preact/signals-core` signals — that's a feature, not an interim. The signals primitive *is* the reactivity system; stores are just a structured way to organize consumers + mutations on top. Adopting any second reactive library — nanostores or otherwise — would have been a regression toward the "two reactivity systems" smell HS-8165 already ruled out at framework scale.
- **The §61.2 rule (one consumer = signal, two+ = store) holds.** Nanostores' atom-as-default culture nudges every callsite toward an atom even when a raw signal would do. Our convention pushes the other direction, which is correct for our codebase size.

### Bottom line

Nanostores is well-built and small, but its compatibility cost (parallel binding helpers OR a manual signal-mirror layer) exceeds its size win, and the wrapper required to enforce §61's three rules is the bespoke factory regardless. Stay with the §61.3 spec as written. **HS-8238 ships the factory from scratch.** Do not re-open this question without a new piece of evidence that invalidates the auto-tracking analysis above.

## 61.14 List virtualization thresholds (HS-8368 stack)

HS-8368's phased rollout produced two opposite virtualization decisions for the two list-rendering surfaces. Documenting them together so a future contributor doesn't relitigate the column-view case.

**List view (default / trash / preview variants) — virtualized.** A single `bindList(rowsContainer, ticketsSignal, t => t.id, createTicketRow)` mounts all visible rows under one container. Per-row mount cost is dominated by `createTicketRow`'s JSX-literal pass + the `setupTicketRowEffects` install (~1–2 ms per row in happy-dom; closer to ~0.5 ms in real Chromium). At a typical fully-loaded project (~200 visible tickets) the full-mount cost lands in the 100–400 ms range — well over the user-perceptible 100 ms threshold for project-switch / view-switch / variant-transition. HS-8371 wraps the call in `bindListVirtualized(parent, source, key, render, { rowHeight: 32, buffer: 10, threshold: 100 })` so above-threshold lists only mount the windowed slice (~`viewport / rowHeight + 2 * buffer` rows = ~40 rows on a typical screen). Fixed-height rows are safe here because list-view rows are uniform 32 px (no wrapping, sync icon + title input + badges all single-line).

**Column view (per-status columns) — NOT virtualized.** Each column has its own `bindList(body, columnSignal, t => t.id, createColumnCard)` (or `createPreviewColumnCard` for backup-preview). Per-column cost is roughly the same per-card factor (~1–2 ms) as list view, but the per-status partition (`ticketsByStatusSignal`) splits the row count across columns: a healthy distribution of 200 tickets across 4 columns puts the largest bucket at ~80 cards = 80–160 ms — at the edge of the threshold but rare to trip. The full-mount cost only hits on layout-toggle (list → column) or variant transition; per-update reactivity stays O(changes) because bindList preserves identity across signal writes. Two further reasons NOT to virtualize:

1. **Variable-height cards.** Column cards wrap titles to multiple lines depending on column width + tag count + sync-icon presence. `bindListVirtualized`'s `rowHeight` parameter assumes uniform fixed-height rows; estimated-height-with-refinement (react-virtuoso style) is a substantially more complex implementation and adds runtime measurement cost that can outweigh the mount-cost savings at these row counts.
2. **Pathological all-in-one-bucket cases.** A column view with 200 tickets all in `not_started` would mount 200 cards in one column — that hits the threshold. But these distributions are rare in practice (most projects have churn across statuses), and the cost only hits on layout-toggle, not on per-update.

**Threshold for revisiting.** Re-open the column-view virtualization question if either (a) a real flame graph shows per-column mount as the dominant cost in a user's project-switch latency, or (b) `bindListVirtualized` grows estimated-height-with-refinement support cheaply enough that retrofitting column view becomes net-positive. Until then column view stays on plain `bindList` and the §60/§61 reactive update path keeps per-card data changes cheap.

## 61.15 Cross-refs

- [§60. Fine-grained reactivity primitive](60-reactivity-primitive.md) — hard dependency. HS-8166 must ship before HS-8167 phases start.
- [§62. Unified JSX render targets](62-unified-jsx-render-targets.md) — independent track. No dependency either direction.
- HS-8165 — investigation that produced the verdict driving this doc.
- HS-8189 / HS-8190 / HS-8222 / HS-8223 / HS-8224 — prior bundling of per-file module-level `let`s; this doc is the next step that makes those bundles testable.
- HS-8248 — pre-implementation investigation that confirmed the bespoke factory; rationale archived in §61.13.
- §18 (plugin system) — plugin-defined stores deferred per §61.8.
