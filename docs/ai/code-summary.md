# Hot Sheet — AI Code Summary

**Purpose.** This document is the fast-onboarding map of the Hot Sheet codebase for a fresh AI session (or human reader). Read it first; then open only the specific files you need. It is a *map*, not a reference — for full behavior see `docs/*` (requirements) and the code itself.

**Maintenance.** This file must be kept in sync with the code. See §17 (Maintenance rules) — if any trigger listed there fires in a change, update this file in the same pass.

---

## 1. Stack at a glance

- **Runtime:** Node.js 20+, ESM (`"type": "module"`, `.js` extensions on imports).
- **Language:** TypeScript strict mode.
- **Server:** Hono on `@hono/node-server`, default port 4174 (fallback 4174–4193).
- **DB:** PGLite (embedded Postgres WASM). Per-project data in `.hotsheet/db/`.
- **Rendering:** Custom JSX runtime → HTML strings (`SafeHtml`). Shared server + client. No React.
- **Build:** tsup (server CLI + channel + client IIFE), sass for styles, esbuild for plugins.
- **Desktop:** Tauri v2 wraps the Node server as a sidecar.
- **AI integration:** Markdown worklist exports + MCP channel server (`src/channel.ts`, `CHANNEL_VERSION=4`).

---

## 2. Request lifecycles

### Server startup (`src/cli.ts` → `src/server.ts`)

1. Parse args (`--port`, `--data-dir`, `--no-open`, `--demo:N`, `--close`, `--list`, `--strict-port`, `--check-for-updates`).
2. Check for update (`src/update-check.ts`), resolve/register data dir, acquire lock (`src/lock.ts`).
3. Init PGLite + run schema migrations (`src/db/connection.ts`).
4. Auto-cleanup (stale trash, attachments) — `src/cleanup.ts`.
5. Start Hono server + mount routes (`src/routes/api.ts`).
6. Debounced markdown export (`src/sync/markdown.ts`) → `.hotsheet/worklist.md` (500ms), `.hotsheet/open-tickets.md` (5s).
7. Generate AI skill files (Claude `.claude/skills/hotsheet`, Cursor, Copilot, Windsurf) — version-gated.
8. Schedule 3-tier backups (`src/backup.ts`).
9. Write `~/.hotsheet/instance.json`, open browser unless `--no-open`.

### Client boot (`src/client/app.tsx`)

Served via `/` → `src/routes/pages.tsx` renders HTML shell (`src/components/layout.tsx`), which loads `/static/app.js` (IIFE) + `/static/styles.css`. `app.tsx` initializes state, binds handlers, starts long-poll (`poll.tsx`), and mounts sidebar/list/detail modules.

### Mutations

UI → `src/client/api.tsx` → `/api/...` → route handler → `src/db/*` → change-version bumps → long-poll notifies other tabs → debounced markdown resync.

---

## 3. Directory tree (annotated)

### `src/` top-level

| File | Role |
|---|---|
| `cli.ts` | CLI entry + multi-project boot. Exports pure `createSignalHandler(hooks)` factory (HS-7934) so the SIGINT/SIGTERM escalation logic is unit-testable without spawning a child process. Entry-point guard ensures `main()` only runs when the file is `process.argv[1]` (else importing `cli.js` from a unit test would trigger a full Hot Sheet startup) |
| `cli/args.ts` | HS-8089 — pure CLI argument parser extracted from `cli.ts` (`parseArgs`, `ParsedArgs`, `printUsage`). |
| `cli/close.ts` | HS-8089 — `--close` / `--list` / join-running-instance flows extracted from `cli.ts` (`handleClose`, `handleList`, `joinRunningInstance`, `shutdownRunningInstance`). |
| `diagnostics/freezeLogger.ts` | HS-8054 v3 — file-based `<dataDir>/freeze.log` JSONL writer + Node event-loop heartbeat + `instrumentSync` / `instrumentAsync` wrappers. Heartbeat starts from `cli.ts::startAndConfigure` after `startServer`, stops in `lifecycle.ts::runShutdownPipeline` after `closeDatabases`. |
| `server.ts` | Hono app, middleware, static assets, secret validation |
| `channel.ts` | MCP server (stdio + HTTP). `CHANNEL_VERSION=4`. Pending-permission state lives in `channelPermissions.ts` (HS-8047). HS-8205 added `POST /permission/inject` (debug-only injector used by `scripts/simulate-claude-prompts.mjs`'s permission-* types — same shape as the MCP `permission_request` notification handler; calls `enqueuePermission` + `notifyMainServer`). HS-8192: `POST /permission/respond` body now validates against `PermissionRespondBodySchema = z.object({ request_id: z.string().min(1), behavior: z.enum(['allow', 'deny']) })` so malformed JSON or wrong-shape payloads return a clean 400 instead of the prior raw `as` cast silently propagating. Ditto `src/terminals/websocket.ts`'s `ControlMessageSchema` (`z.discriminatedUnion('type', [resize { cols, rows positive }, kill, ping])`) — bad shapes are dropped before reaching `handleControl`. |
| `channel-config.ts` | `.mcp.json` registration + `EXPECTED_CHANNEL_VERSION` check |
| `channelPermissions.ts` | HS-8047 — FIFO queue for pending channel-permission requests + auto-expiry (`PERMISSION_TTL_MS = 120_000`). Exports `enqueuePermission` / `peekPending(now?)` / `completePermission(id)` / `clearAllPermissions`. Wire shape on `/permission` stays single-slot (`{ pending: head | null }`) so client + main-server are oblivious to the queue beneath; once the user responds to the head, the next 100 ms client poll surfaces the next queued request. Lives in its own module so the queue logic is unit-testable without spawning the channel-server process (`channel.ts` is a process entry-point with module-top-level `await mcp.connect(...)`). |
| `claude-hooks.ts` | Install/remove Claude hook entries in `~/.claude/settings.json` |
| `jsx-runtime.ts` | `SafeHtml`, `raw()`, server/client shared JSX → HTML strings. **Footgun:** passing a DOM element as a JSX child throws — the runtime renders to strings, so DOM nodes can't be composed. Build trees in one JSX expression and `querySelector` after `toElement()` (HS-6341/HS-6342 root cause). |
| `types.ts` | `Ticket`, `TicketCategory`, `TicketPriority`, `AppEnv` |
| `projects.ts` / `project-list.ts` | Multi-project registry (secret ↔ dataDir) |
| `instance.ts` | `~/.hotsheet/instance.json` PID + port; stale cleanup |
| `lifecycle.ts` | HS-7931 + HS-7934 graceful-shutdown pipeline: `gracefulShutdown(reason)` closes HTTP → **kills running shell-command children (HS-8040)** → destroys PTYs → disposes git watchers → `closeAllDatabases()` → `releaseAllLocks()` → removes instance file. Idempotent; shared by `/api/shutdown`, SIGINT, SIGTERM. `registerHttpServerForShutdown(server)` wires the live server in. The lock-release step was folded in (HS-7934) after `lock.ts`'s own SIGINT handler proved to race the async pipeline. The shell-kill step (HS-8040) was added after a long-running button-launched command (`target: 'shell'` custom-command, e.g. `npm run dev` from a button) was observed surviving Hot Sheet exit indefinitely — pipeline now lazy-imports `routes/shell.ts::killAllRunningShellCommands()` (SIGTERM-then-1s-grace-then-SIGKILL fallback) so children never outlive the parent. |
| `lock.ts` | `.hotsheet/hotsheet.lock` — single instance per dataDir |
| `file-settings.ts` | Read/write `.hotsheet/settings.json` (reserved vs user keys) |
| `global-config.ts` | `~/.hotsheet/config.json` (channel enabled, share stats) |
| `backup.ts` | 3-tier automated backups (5min/hourly/daily); CHECKPOINT before dump, startup catch-up for overdue tiers, JSON co-save sibling. **HS-8178 (2026-05-04)** — every `backupDir`-touching write (tarball, JSON co-save, attachment manifest, per-attachment blob copy) is now async via `fs.promises` so the fsync runs on libuv's threadpool instead of blocking the main event loop on slow Google-Drive-style `backupDir` stalls. Each call site is wrapped with `instrumentAsync` (was `instrumentSync`); same `freeze.log` labels survive (`backup.writeTarball:<tier>`, `backup.writeJsonCoSave:<tier>`, etc.). |
| `dbJsonExport.ts` | HS-7893 versioned JSON snapshot of every table; atomic gzip write co-saved alongside each tarball. HS-8178 — `writeJsonExportAtomically` is now `async` (`fs.promises.open` + `handle.write` + `handle.sync` + `handle.close` + `fs.promises.rename`); pre-fix was sync `openSync` / `writeSync` / `fsyncSync` / `closeSync` / `renameSync`. |
| `attachmentBackup.ts` | HS-7929 attachment-blob backups: streaming `hashFile`, hash-addressed `<backupRoot>/attachments/<sha256>` store with link-then-copy fallback, atomic `<base>.attachments.json` manifest writes, daily orphan GC (aborts on any parse failure), `restoreAttachmentsFromManifest` with `-restored-<TS>` collision suffix. HS-8178 — `writeManifestAtomically` and `ensureBlobInStore` are now `async` (every disk-touching call routes through `fs.promises`); restore path's `copyFileSync` stays sync (user-initiated, blocking is acceptable per the HS-8178 ticket out-of-scope). |
| `permissionAllowRules.ts` | HS-7952 (§47.4) channel-permission allow-list helpers — `loadPermissionAllowRules(dataDir)` reads `permission_allow_rules: PermissionAllowRule[]` from `<dataDir>/settings.json`, `findMatchingAllowRule(toolName, input, rules)` is the server-side gate consumed by `routes/channel.ts` to auto-approve permission requests + write `channel_permission_auto_allow` audit log entries before the overlay surfaces |
| `cleanup.ts` | Prune old trash/completed/verified + orphaned attachments |
| `demo.ts` | `--demo:N` seed data |
| `skills.ts` | Generates `.claude/skills/hotsheet` + per-category `hs-{cat}` + Cursor/Copilot/Windsurf files |
| `feature-flags.ts` | `PLUGINS_ENABLED` (build-time, env fallback, browser-safe) |
| `keychain.ts` | macOS `security` / Linux `secret-tool` abstraction (no native deps) |
| `mime-types.ts` | Attachment MIME mapping |
| `open-in-file-manager.ts` | Platform-specific "Reveal in Finder/Explorer" |
| `gitignore.ts` | Ensure `.hotsheet/` is in `.gitignore` |
| `migrate-settings.ts` | Upgrade older settings.json shapes |
| `update-check.ts` | Version freshness check against npm |
| `test-helpers.ts` | `setupTestDb` / `cleanupTestDb` for unit tests |

### `src/routes/`

| File | Endpoints |
|---|---|
| `api.ts` | Composes all sub-routers + `/api/poll`, `/api/shutdown`, `/api/ensure-skills`, etc. |
| `tickets.ts` | `GET/POST/PATCH/DELETE /api/tickets[...]`, `/batch`, `/query`, `/duplicate`, `/restore`, `/up-next`, notes bulk/single |
| `attachments.ts` | `POST /api/tickets/:id/attachments`, `DELETE /api/attachments/:id`, `POST /:id/reveal`, `GET /api/attachments/file/*` |
| `settings.ts` | `/api/categories`, `/api/category-presets`, `/api/tags`, DB `/api/settings`, file `/api/file-settings`, `/api/gitignore` |
| `channel.ts` | `/api/channel/{status,trigger,heartbeat,claude-check,permission,permission/respond,permission/dismiss,permission/notify,done}` |
| `commandLog.ts` | `GET /api/command-log`, `DELETE /api/command-log`, `GET /api/command-log/count`, `POST /api/terminal-prompt/audit` (HS-7987) |
| `dashboard.ts` | `/api/poll`, `/api/stats`, `/api/dashboard`, `/api/worklist-info`, `/api/browse`, `/api/global-config` |
| `plugins.ts` | `GET /api/plugins`, `/ui`, `:id/{action,validate,config,test}`, `/sync/manual`, conflicts, schedules, `global-config`, `image-proxy`, bundled install |
| `shell.ts` | `POST /api/shell/exec`, `/shell/kill`, `GET /api/shell/running`. HS-8040 added the `killAllRunningShellCommands({gracePeriodMs?})` non-route export consumed by `lifecycle.ts::killShellCommands()` — SIGTERMs every entry in the module-private `runningProcesses` map, waits the grace period (default 1000 ms), SIGKILLs anything still alive. Returns `{killed: number}`. Adds each id to `killedProcesses` so the close-handler logs read "Canceled" not "Killed by SIGTERM". `_runningShellCommandCountForTesting()` exposes the map size to tests without leaking the map itself. |
| `backups.ts` | `GET /api/backups`, `POST /api/backups/restore/:id`, preview flow |
| `db.ts` | HS-7899 `GET /api/db/recovery-status` + `POST /api/db/dismiss-recovery` for the launch-time recovery banner; HS-7897 `POST /api/db/repair/find-working-backup` + `GET /api/db/repair/pg-resetwal-availability` + `POST /api/db/repair/run-pg-resetwal` for the Database Repair panel |
| `projects.ts` | `/api/projects` register/list/unregister, reorder, per-project channel-status, `/api/projects/permissions` long-poll, `/api/projects/bell-state` long-poll (HS-6638 §24.3.3 — aggregates per-project `bellPending`) |
| `terminal.ts` | `GET /api/terminal/list` (includes `bellPending`), `/status`, `/restart`, `/kill`, `/create`, `/destroy`, `/clear-bell` (HS-6638 §24.3.2), `/foreground-process` (HS-7596), `/command-suggestions` (HS-7791), `/prompt-respond`, `/prompt-dismiss`, `/prompt-resume` (HS-8034 server-side prompt scanner) |
| `git.ts` | HS-7954-7956 git status tracker — `GET /api/git/status` (branch + working-tree dirty + ahead/behind), `POST /api/git/fetch` (manual fetch), `POST /api/git/auto-fetch` (timer endpoint). 500 ms cache + chokidar watcher on `.git/index` + `.git/HEAD` |
| `notify.ts` | Shared long-poll + change-version bus (change / permission / bell waiter lists; HS-6638 added `bellVersion` + `addBellWaiter` + `notifyBellWaiters`) |
| `diagnostics.ts` | HS-8054 v3 — `POST /api/diagnostics/freeze` accepts client-detected freeze events (`coerceFreezeEntry` validates source / shape; rejects spoofed server-side tags) and forwards to `appendFreezeLog`. |
| `pages.tsx` | `GET /` server-rendered HTML |
| `validation.ts` | Zod schemas for body/query validation |
| `helpers.ts` | `parseIntParam`, header readers, etc. |

### `src/db/`

| File | Role |
|---|---|
| `connection.ts` | PGLite instance per dataDir, `initSchema()` — all `CREATE TABLE` + migrations live here |
| `queries.ts` | Aggregates/re-exports query helpers |
| `tickets.ts` | CRUD, filtering, batch, status transitions. HS-8036 `listKnownTicketPrefixes()` for cross-reference linkification. Search-counts helper (`countSearchMatchesInExcludedStatuses`) for HS-7756 include-rows |
| `attachments.ts` | Attachment row ops |
| `notes.ts` | JSON-serialized notes; `note_id` generation |
| `tags.ts` | Normalization (lowercase), extraction from `[tag]` syntax |
| `settings.ts` | Plugin-scoped DB settings (`plugin:{id}:{key}`, `plugin_enabled:{id}`) |
| `commandLog.ts` | Shell + channel + permission log entries (incl. HS-7987 `terminal_prompt_auto_allow` event_type via `addLogEntry`) |
| `stats.ts` | Rolling daily snapshot for dashboard charts |
| `sync.ts` | `ticket_sync`, `sync_outbox`, `note_sync` accessors |
| `feedbackDrafts.ts` | HS-7822 per-feedback-note draft persistence (unsent text survives reload + relaunch) |
| `repair.ts` | HS-7897 Repair Database helpers: `findWorkingBackup`, cross-platform `pg_resetwal` probe + run flow, install-hint pure helpers (was previously listed under `src/` top-level) |
| `fsyncWrap.ts` | HS-7935 explicit-fsync wrapper. `fsyncDir(path, fsyncFn?)` recursively walks a directory tree and `fs.fsyncSync`'s every regular file to close the gap left by Emscripten's NODEFS no-oping `fsync` (HS-7932 spike). Wired into `createBackup` after `CHECKPOINT` + `closeAllDatabases` after each `db.close()`. Best-effort: per-file errors are logged + counted, never thrown (was previously listed under `src/` top-level) |

### `src/client/` (browser bundle)

**Module index** — quick reference matching the table style used elsewhere in §3. Each row is one client module; the **Notable feature notes** below the table cover cross-cutting changes that touch multiple files. When you add a new file under `src/client/`, append a row here.

| File | Role |
|---|---|
| `animate.ts` | Shared FLIP / fade / slide helpers used by overlay enter / exit animations |
| `api.tsx` | `api(path, opts)` HTTP helper, file upload, network-error popup, `apiWithSecret` for cross-project routing |
| `app.tsx` | Client entry point — wires every `init*` from the other modules together |
| `backups.tsx` | Settings → Backups list + restore preview UI |
| `batch.tsx` | Multi-select toolbar + batch-action handlers |
| `bellPoll.tsx` | `/api/projects/bell-state` long-poll loop, fans out to project-tab + per-terminal indicators (HS-6638), dispatches cross-project terminal-prompt overlays (HS-8047) |
| `channelUI.tsx` | Sidebar play button, channel status, project-tab status dot, no-upnext alert |
| `clipboard.ts` / `clipboardUtil.tsx` | Cross-platform clipboard write helpers (Tauri-aware fallback) |
| `columnView.tsx` | Multi-column ticket grid layout |
| `commandEditor.tsx` | Modal editor for shell / channel commands (Settings → Commands) |
| `commandLog.tsx` | Drawer shell + Commands Log tab; delegates `terminal:<id>` tabs to `terminal.tsx` |
| `commandLogFilter.tsx` | Filter chips above the Commands Log entries |
| `commandSidebar.tsx` | Sidebar custom-command buttons + spinner (HS-8056) |
| `confirm.tsx` | `confirmDialog({...})` Tauri-safe replacement for `window.confirm` |
| `contextMenu.tsx` | Shared right-click menu primitive |
| `customViews.tsx` | Custom-view editor + autocomplete + filter wiring |
| `dashboard.tsx` | KPI / chart dashboard view |
| `dashboardHiddenTerminals.tsx` | Per-project visibility-grouping state + `applyHideButtonBadge` |
| `dashboardMode.tsx` | Layout toggle for §25 dashboard (sectioned vs flow) |
| `dbRecoveryBanner.tsx` | HS-7899 launch-time recovery banner |
| `dbRepairUI.tsx` | Settings → Database Repair (HS-7897) |
| `detail.tsx` | Detail panel — title / details / notes / megaphone / resize / stats |
| `dom.ts` | `toElement(<jsx/>)` helper — single source of JSX → DOM conversion |
| `draftRow.tsx` | New-ticket inline-create row at the top of the list |
| `drawerTabGating.ts` | Pure helper: which drawer tab to focus when set members close |
| `drawerTerminalGrid.tsx` | §36 per-project drawer grid view; delegates per-tile lifecycle to `terminalTileGrid.tsx`. **HS-8223 (2026-05-06)** — bundled the 16 pre-fix module-level `let`s into a single `drawerGridState: DrawerGridState` slot with a `freshDrawerGridState()` factory; mirrors the HS-8190 pattern. Local var named `drawerGridState` (not `state`) to avoid shadowing the imported `state` module surface. `_resetStateForTesting()` runs disposers (RAF / resize listener / drawer ResizeObserver / bell + hidden-change unsubscribes / grid handle dispose) BEFORE swapping in a fresh state. |
| `dropdown.tsx` | Generic context-dropdown menu (category / priority / etc.) |
| `editDiffPreview.tsx` | Edit-tool inline diff preview for the §47 permission popup |
| `experimentalSettings.tsx` | Settings → Experimental panel |
| `feedbackDialog.tsx` | §21 feedback-needed dialog with auto-detected partition blocks |
| `feedbackParser.ts` | Pure heuristic that splits a feedback note into `partitions` |
| `gitStatusChip.tsx` / `gitStatusPopover.tsx` | §48 sidebar git chip + expanded popover (HS-7954-7956) |
| `gridNavGeometry.ts` | Pure grid-neighbour math for HS-8028 magnified grid nav |
| `hiddenTerminalsResetUI.tsx` | "Reset hidden terminals" link in Settings → Terminal |
| `hideTerminalDialog.tsx` | Show / Hide Terminals dialog with §39 grouping tab bar |
| `iconPicker.tsx` | App-icon picker for Settings → General |
| `icons.ts` | Lucide SVG paths used as client-side icons |
| `imageProxy.tsx` | Image download / GitHub-image proxy + `appendImageDownloadLinks` |
| `json.ts` | Tiny JSON parse / stringify wrappers with default fallback |
| `longTaskObserver.tsx` | HS-8054 client-side performance observer; reports to `/api/diagnostics/freeze` |
| `markdownSetup.ts` | Marked + DOMPurify configuration shared by every markdown render path |
| `noteRenderer.tsx` | Note rendering, megaphone, ticket-ref linkification, undo wiring |
| `openFolder.tsx` | Open-folder dialog (multi-project register) |
| `permissionAllowListUI.tsx` | §47.4 channel-permission allow-list — row + modal editor (`openRuleEditor`) — `cmd-outline-row` shell post-HS-8021/HS-8026 |
| `permissionDialogShell.tsx` | Shared chrome (header, drag handle, dismiss button) for the §47 permission popup |
| `permissionOverlay.tsx` | The §47 permission popup itself: poll loop, minimize-to-pulsating-dot, live-terminal checkout (HS-8171 v2), `pendingPermissionStack` LIFO queue (HS-8219), `shouldUseLiveCheckout` heuristic (HS-8217). **HS-8190 (2026-05-06)** — every long-lived mutable lifecycle ref now lives inside a single `permissionState: PermissionOverlayState` slot with a `freshPermissionOverlayState()` factory; the 10 pre-fix module-level `let`s (`activePopupRequestId`, `activePopupOwnerSecret`, `activeCheckoutHandle`, `activeLiveTermResizeObserver`, `liveTermFitRetryTimer`, `permissionPollActive`, `permissionVersion`, `autoDismissMissCount`, `channelBusyTimeoutModule`, `setChannelBusyTimeoutRefModule`) were the substrate that fed the HS-8171 v2 / HS-8180 / HS-8182 / HS-8183 / HS-8206 / HS-8207 / HS-8217 / HS-8218 / HS-8219 regression family — bundling makes a future audit one-glance. `_resetStateForTesting()` runs disposers (resize observer, fit-retry timer, minimized timeouts) BEFORE assigning a fresh state so an in-flight handler can't leak past the swap. Collection state (`respondedRequestIds` / `dismissedRequestIds` / `minimizedRequests` / `pendingPermissionStack`) stays as separate const Set/Map/Array — those are long-lived containers, not single-slot mutable refs. Same pattern tracked for `terminalDashboard.tsx` (HS-8222), `drawerTerminalGrid.tsx` (HS-8223), `terminal.tsx` (HS-8224). |
| `permissionPreview.ts` | `formatInputPreview(tool, raw)` — strips JSON wrapper from Claude's `input_preview` |
| `persistedHiddenTerminals.ts` | Hydrate / persist `hidden_terminals` to file-settings |
| `pluginConfigDialog.tsx` / `pluginSettings.tsx` / `pluginUI.tsx` / `pluginTypes.ts` | §18 plugin host UI — config dialogs, settings panel, per-location UI extensions |
| `poll.tsx` | `/api/poll` long-poll change-version loop |
| `print.tsx` | Print-this-view dialog |
| `projectTabs.tsx` / `projectTabsFingerprint.ts` | Top-bar project tabs + change-detection fingerprint |
| `quitConfirm.tsx` / `quitConfirmSettingsUI.tsx` | §37 quit-confirm dialog + master-detail preview pane + Settings panel |
| `readerOverlay.tsx` | §49 reader-mode overlay (notes + Details) |
| `searchExtraRows.tsx` | §40 "Include {N} archive / backlog matches" gray rows |
| `settingsCategories.tsx` | Category-management UI in Settings |
| `settingsDialog.tsx` | Settings overlay shell + tab routing |
| `settingsLoader.tsx` | One-shot loader that hydrates client settings on app boot |
| `share.tsx` | §17 share prompt + toolbar button |
| `shortcuts.tsx` | Global keyboard shortcuts + `MODAL_OVERLAY_SELECTORS` registry (HS-8033 — modals own the keyboard until Esc) |
| `sidebar.tsx` | Left sidebar layout, custom-command list, search box, filter chips |
| `state.tsx` | Shared client state (active project, settings cache, per-project session-only maps) |
| `stripAnsi.ts` | `stripAnsi(text)` + `tailLines(text, n)` pure helpers |
| `tagAutocomplete.tsx` | Tag autocomplete dropdown for the new-ticket row + filter chips |
| `tags.tsx` / `tagsDialog.tsx` | Tag chip rendering + Tags-dialog editor |
| `tauriIntegration.tsx` | Tauri invoke / IPC abstraction (`getTauriInvoke`, `openExternalUrl`, `requestAttention`) |
| `terminal.tsx` | Embedded terminal: per-instance xterm mount + WebSocket + tab strip + power button + Ask-Claude (HS-7270) + history-replay refit. **Long file (~1807 lines after HS-8194 + HS-8195 + HS-8221 extractions).** **HS-8224 (2026-05-06)** — bundled the 5 pre-fix module-level `let`s (`activeTerminalId` / `currentProjectSecret` / `lastKnownConfigs` / `bellSubscribed` / `tabDragFromId`) into a single `terminalState: TerminalModuleState` slot with a `freshTerminalModuleState()` factory; mirrors the HS-8190 pattern. Local var named `terminalState` (not `state`) to avoid shadowing the imported `state` from `./state.js`. The const `instances` Map stays as a separate container. |
| `terminal/renameDialog.tsx` | HS-8195 — shared rename overlay extracted from `terminal.tsx::promptRenameTerminal` + `terminalDashboard.tsx::openDashboardTileRename`. Single `openRenameDialog({ initialValue, onApply, title?, label?, hint? })` consumer. Owns the overlay shell + Enter / Escape / backdrop / X / Cancel / Rename interactions; the surface passes its commit logic via `onApply(next)`. |
| `terminal/gutterPopover.tsx` | HS-8194 — OSC 133 gutter-glyph hover popover extracted from `terminal.tsx`. Public API: `attachGutterHoverPopover(el, term, record, { getCwd })`. Owns the four-action popover (Copy command / Copy output / Rerun / Ask Claude — gated on `isChannelAlive()` at open time), the readRecordCommand / readRecordOutput xterm-buffer readers, and the shared singleton popover state (only one visible at a time). Caller passes a `getCwd: () => string \| null` getter so the Ask-Claude prompt template reads the live shell CWD at click time. |
| `terminal/tabContextMenu.tsx` | HS-8221 — drawer-tab right-click context menu extracted from `terminal.tsx`. Public API: `showTabContextMenu({ event, clickedId, clickedIsDynamic, orderedIds, isDynamic, onClose, onCloseSet, onRename })` + `dismissTabContextMenu()`. Owns the menu DOM (Close Tab / Close Other Tabs / Close Tabs to the Left / Close Tabs to the Right / Rename...), viewport clamping, outside-click dismiss, and the disabled-state for Close Tab on configured tabs. Caller (`terminal.tsx`) supplies the data + action callbacks so the new module stays free of `instances` / `closeDynamicTerminal` / `closeTabs` / `promptRenameTerminal` coupling. |
| `terminal/stallIndicator.ts` | HS-8175 — pure helper `shouldShowStallIndicator(lastTypeTs, lastEchoTs, now, thresholdMs?)` for the per-terminal "Server slow" chip. Threshold default `STALL_INDICATOR_THRESHOLD_MS = 1500`. Returns true when the user typed a keystroke past the threshold without a PTY echo coming back. Wired in `terminal.tsx::attachStallIndicator` (subscribes to `subscribeStallState` from `terminalCheckout.tsx` + a 250 ms tick + toggles the `.terminal-stall-indicator` chip). |
| `serverBusyChip.tsx` | HS-8175 — global "Server slow" chip mounted into `document.body`. `trackServerRequest(url)` wraps every non-long-poll fetch in `api.tsx` (returned `done()` is called in the `finally` clause); chip lights when any in-flight request crosses `SERVER_BUSY_THRESHOLD_MS = 3000`. `LONG_POLL_PATTERNS` skips the documented long-poll endpoints (`/poll?`, `/projects/permissions`, `/projects/bell-state`) so the chip doesn't false-fire on every poll cycle. Exports `isLongPollUrl` + `shouldShowServerBusyChip` (pure helpers) for unit-test isolation. |
| `terminalAppearance.ts` / `terminalAppearancePopover.tsx` / `terminalDefaultAppearanceUI.tsx` / `terminalFonts.ts` / `terminalThemes.ts` | §35 terminal theme + font system (registry, three-layer resolver, gear popover, Settings → Terminal default panel) |
| `terminalCheckout.tsx` | §54 global xterm checkout / LIFO stack — `checkout({...}) → CheckoutHandle`. Single xterm per `(secret, terminalId)` shared across drawer / dashboard / popup; `peekEntryDims` (HS-8207); `noSpawn` opt-in (HS-8218); `_simulateNoSessionForTesting`. **HS-8175** added `lastTypeTs` / `lastEchoTs` per-entry plus `peekStallTimestamps(secret, terminalId)` + `subscribeStallState(secret, terminalId, handler)` so the drawer pane can paint a "Server slow" chip when the user types and the PTY hasn't echoed back within 1.5 s. |
| `terminalDashboard.tsx` | §25 cross-project dashboard view (per-project sections, slider, click-to-center, dedicated full-pane). **HS-8222 (2026-05-06)** — bundled the 22 pre-fix module-level `let`s (centeredHandle / dedicatedSearchHandle / active / toggleButton / rootElement / resizeHandler / resizeRaf / bellUnsubscribe / appearanceUnsubscribe / sizerContainer / sizeSlider / currentSnapPoints / hideButton / groupingSelect / lastSectionData / hiddenChangeUnsubscribe / columnCount / sliderValueLoadPromise / sliderPersistTimeout / layoutMode / layoutToggleButton / layoutModeLoadPromise) into a single `dashboardState: DashboardState` slot with a `freshDashboardState()` factory; mirrors the HS-8190 pattern. Local var named `dashboardState` (not `state`) to avoid shadowing the imported `state` module surface. `_resetStateForTesting()` runs disposers (RAF / resize listener / bell + appearance + hidden-change unsubscribes / persist-debounce timeout / grid-handle disposes) BEFORE swapping in a fresh state. |
| `terminalDashboardSizing.ts` | Pure tile-math helpers — `tileWidthFromColumnCount`, `perRowToSliderPosition`, `sliderPositionToPerRow`, `computeTileScale`, `computeColumnSnapPoints`, `legacySliderValueToColumnCount` (post-HS-8176 column-count semantics) |
| `terminalDedicatedState.ts` | Tracks which tile is currently in the dashboard's dedicated overlay |
| `terminalKeybindings.ts` | Pure helpers `isClearTerminalShortcut` / `isFindShortcut` / `isJumpShortcut` (platform-aware) |
| `terminalOsc133.ts` | §31-33 OSC 133 helpers — `parseOsc133ExitCode`, `findPromptLine`, `computeLastOutputRange`, `buildAskClaudePrompt` |
| `terminalOsc7.ts` | §29 OSC 7 CWD parser + `formatCwdLabel` + `cacheHomeDir` |
| `terminalPrompt/` | §52 terminal-prompt-overlay client store: `allowRulesStore.ts` (`appendAllowRule` / `removeAllowRule` / `loadAllowRules` / `subscribeToAllowRules`) |
| `terminalPromptAllowListUI.tsx` | §52 terminal-prompt allow-list (Settings → Permissions); legacy `.permission-allow-row` grid (per HS-8186 — intentional, not a typo) |
| `terminalPromptOverlay.tsx` | §52 overlay UI itself; project-tab anchored (HS-8012); channel-rule footer (HS-8210 Phase C) |
| `terminalReplay.ts` | History-replay helpers (`replayHistoryToTerm`, `applyDedicatedHistoryFrame`) |
| `terminalSearch.tsx` / `terminalSearchHistory.ts` | §34 in-pane find widget + saved-search history |
| `terminalsSettings.tsx` | Settings → Terminal outline list + edit modal for configured terminals; lazy-fetched `command-suggestions` datalist |
| `terminalTabReorder.ts` | Pure helpers for drag-to-reorder drawer tabs |
| `terminalTabSelection.ts` | `pickNearestTerminalTabId` post-close-fallback math |
| `terminalTileGrid.tsx` | Shared per-tile lifecycle (mount xterm, scale, click-to-center, dedicated view) consumed by both §25 dashboard and §36 drawer-grid. **HS-8225** — also wires the per-tile stall chip in `mountTileViaCheckout` (subscribes to `subscribeStallState` + 250 ms tick + toggles a `.terminal-stall-indicator` in the tile label area). |
| `terminalTileVirtualization.ts` | HS-7968 IntersectionObserver-driven lazy mount / 8 s-debounced dispose for off-screen tiles |
| `ticketList.tsx` / `ticketListState.ts` / `ticketRow.tsx` | Ticket-list rendering, drag-and-drop, row-state cache |
| `ticketRefDialog.tsx` / `ticketRefs.ts` | §55 cross-reference linkifier + stacking modal |
| `toast.tsx` | Shared toast helper (used by §27 OSC 9 + miscellaneous status messages) |
| `uiTimings.ts` | HS-8191 named timing constants — `TOAST_AUTOHIDE_MS`, `BUTTON_BUSY_MS`, `POPOVER_CLOSE_DELAY_MS`, `SHAKE_DURATION_MS`, `COPIED_GLYPH_FLASH_MS`, `BLUR_DEBOUNCE_MS` |
| `undo/` | Undo-action types + handlers (notes / status / category) |
| `upgradeNudge.tsx` | §50 npm → Tauri upgrade nudge overlay |
| `visibilityGroupings.ts` / `visibilityGroupingSelect.tsx` | §39 visibility-grouping pure state helpers + `<select>` wiring |
| `xtermTheme.ts` | `readXtermTheme()` builds an xterm `ITheme` from CSS vars + `withAlpha` |

### Notable feature notes

The paragraphs below cover cross-cutting changes that touch multiple files in the table above. Treat them as deep-dives — the table is the orientation index, these are the recent-history annotations.

**Skill `/clear` prefix removed (HS-7992 → HS-8022):** A previous build added a per-project `hotsheet_skill_clear_context` toggle in Settings → General that prepended `/clear` to the generated `/hotsheet` skill body. The mechanism never worked — skill bodies are returned to the model as Skill *tool output*, not typed at the REPL prompt, so the Claude Code CLI never re-parsed the prefix as a slash command and the model itself cannot invoke slash commands. The skill frontmatter spec has no `clear-context` equivalent, and skill hooks fire shell commands rather than slash commands. **Do not re-add this** without first confirming a first-class skill / MCP context-clear API exists. HS-8022 deleted: the toggle in `pages.tsx` (was `#settings-hotsheet-skill-clear-context`), the change-handler in `settingsDialog.tsx`, the conditional `clearPrefix` in `skills.ts::mainSkillBody`, and the regen-on-PATCH block in `routes/settings.ts`. `SKILL_VERSION` bumped 8 → 9 so existing files re-author themselves through the normal `updateFile` upgrade path on next boot. `regenerateMainSkill` is kept exported for any future explicit-user-action regen flow. The HS-7992 unit tests in `src/skills.test.ts` were replaced by 3 HS-8022 regression tests that assert `/clear` never appears in the body, including when a stale `hotsheet_skill_clear_context: true` value is present in the per-project settings (back-compat — old users won't accidentally resurrect the prefix). Workaround for users who want a fresh context per `/hotsheet`: type `/clear` yourself before invoking the skill, or remap `/hotsheet` to a personal alias. `docs/6-markdown-sync.md` §6.8 rewritten to document the removal + the underlying mechanism limitation.

**Permission allow rules — UI cleanups (HS-8021 / HS-8026):** Pre-fix the §47.4 channel-permission allow-list row was a 4-column grid (tool / pattern / "overlay · 4/29/2026" / red `×`) with the pattern column ellipsis-truncated and no way to view the full text or edit a rule in place. Per the user feedback, the row layout was rewritten to mirror the custom-command + terminal settings rows: `<div class="cmd-outline-row permission-allow-rule-row">` with tool name + pattern code chip + pencil-edit button + trash-delete button, matching the `cmd-outline-edit-btn` / `cmd-outline-delete-btn` icon set used by `terminalsSettings.tsx` and `commandEditor.tsx`. The whole row is `cursor: pointer` and `role="button"` — clicking anywhere on the row OR on the pencil opens the new modal rule editor (`openRuleEditor` in `permissionAllowListUI.tsx`), which reuses the `.cmd-editor-overlay` / `.cmd-editor-dialog` shell with a tool select + pattern textarea (3 rows) + inline validation + Cmd/Ctrl+Enter to submit. Trash button still confirms then PATCHes the rule out. The legacy inline +Add form (select + input + button + error paragraph) was removed from `routes/pages.tsx` in favour of a single `+ Add rule` button at the bottom of the list (`renderAddButton()`) that opens the same editor in `add` mode — add and edit now share one validation path. The `Date Added` and `Source (overlay / settings)` columns were dropped (the `added_by` field is preserved on the rule for audit but no longer surfaced — users found the "overlay" wording confusing). Long patterns now stay discoverable via the `title` tooltip on the pattern code chip + the editor dialog. Class rename: the new rows use `.permission-allow-rule-row` (not `.permission-allow-row`) so the §52 `terminalPromptAllowListUI.tsx` keeps its existing grid styling unchanged — that surface gets the same treatment in a future follow-up. `formatRuleMeta` was deleted (no longer needed). 12 new happy-dom tests in `permissionAllowListUI.test.ts` covering empty-state + Add button, row-renders-pencil-and-trash, long-pattern title-tooltip, click-row-opens-editor, click-pencil-opens-editor, trash-confirms-and-PATCHes; openRuleEditor add / edit modes + blank-pattern validation + Cancel + backdrop-click + idempotent re-open. Sub-ticket HS-8027 tracks the bigger piece (per-rule auto-allow stats panel with chart-line button + last-30-day usage graph).

**Quit-confirm rich ANSI preview (HS-7969 follow-up #2):** Pre-fix the §37 master-detail preview pane painted plain ANSI-stripped text — every visual cue from coloured prompts, error highlights, syntax-coloured tool output was lost despite the theme-aware background. New parallel server field `textWithAnsi` on `GET /api/terminal/scrollback-preview` (powered by `buildScrollbackPreviewWithAnsi` in `src/terminals/scrollbackSnapshot.ts` + `getTerminalScrollbackPreviewWithAnsi` in `src/terminals/registry.ts`) keeps SGR / CSI sequences in the response. New client module `src/client/ansiSpans.ts::ansiToSafeHtml` walks the byte stream and emits `<span style="color:…;background:…;font-weight:bold;…">` markup against the resolved theme palette (`paletteFromTheme(theme)` maps `TerminalTheme` to an `AnsiPalette`). Handles SGR codes 0/1/3/4/7/22/23/24/27 (styles), 30-37/40-47/90-97/100-107 (basic + bright fg/bg), 38;2;R;G;B / 48;2;R;G;B (true-colour, rendered exactly), 38;5;N / 48;5;N (256-colour, degraded to nearest basic-8 because the preview palette doesn't carry a 256-entry table), 39 / 49 (default fg / bg). Reverse video (code 7) swaps the active fg / bg before emitting. All text fragments are HTML-escaped (`<`, `>`, `&`, `"`) before concatenation, so assigning to `pre.innerHTML` is safe — no untrusted markup can survive the escaper. Non-SGR CSI / OSC / SIMPLE-ESC sequences are dropped (cursor moves don't make sense in a static preview). 28 new tests in `src/client/ansiSpans.test.ts` (escapeHtml × 3, plain text × 3, basic SGR × 5, reset behaviour × 3, 256-/true-colour × 4, escapes-inside-spans × 1, non-SGR drop × 4, multi-fragment × 3, reverse video × 2). 5 new tests in `scrollbackSnapshot.test.ts` for `buildScrollbackPreviewWithAnsi` (empty, preserve-SGR-and-tail, coloured-prompt-pass-through, drop-backspace-and-bare-CR, multi-byte-UTF-8). The original `text` field is still returned for back-compat; `quitConfirm.tsx::paintPreviewContent` falls back to it when `textWithAnsi` is empty.

**Quit-confirm master-detail preview + theme honour (HS-7969 follow-up):** §37's quit-confirm dialog used to have per-row click-to-expand previews — users found that visually noisy and the previews didn't honour the live terminal's theme. Rewritten as a two-column master-detail layout: left list of rows + project headings, right `<pre>` preview pane that swaps content when the user selects a row. Server-side `/api/terminal/scrollback-preview` extended to return `{ text, maxLines, theme, fontFamily, fontSize }` (resolution layers: terminal config → project default → null); helper `resolveAppearanceForResponse(dataDir, terminalId)` in `src/routes/terminal.ts` does the merge. Client-side `quitConfirm.tsx::applyAppearanceToPreview` looks the ids up via the existing client-side `getThemeById` / `getFontById` registries and paints background / foreground / fontFamily / fontSize on the `<pre>`; `loadGoogleFont` ensures the right face is loaded before the colour swap. Token-counter guard against out-of-order async resolutions when the user clicks rows faster than the network responds. First row auto-selected on dialog open. Dialog widened from 480 → 880 px to give the preview pane room. Removed the `.quit-confirm-row-chevron` accordion + `.quit-confirm-row-container` wrapper + `.quit-confirm-row-preview` block; new `.quit-confirm-master-detail` grid + `.quit-confirm-master` / `.quit-confirm-detail` / `.quit-confirm-detail-preview` SCSS. `docs/37-quit-confirm.md` §37.4 and `docs/ai/requirements-summary.md` updated.

**Centered tile selection disabled + hint chip (HS-8010):** §25.7 / §36 single-click "centered" tile rendered xterm at its NATURAL pixel dims (e.g. 80 cols × 8 px cellWidth = 640 px wide) and applied `transform: scale(0.7–1.25)` to fit the centered slot — `terminalTileGrid.tsx::applyTileScale`. xterm.js's mouse handling reads `event.clientX - rect.left` (in scaled pixels) and divides by `cellWidth` (natural — never updated when transform changes) to compute the cell, so a click-drag inside the centered tile painted a selection offset by `1 - 1/scale` from the cursor. Dedicated view (double-click) doesn't have this bug because it mounts a fresh xterm sized to fit the pane via `FitAddon` — natural cellWidth matches visual rendering. Stop-gap fix (option B from the worklist note): `.terminal-dashboard-tile.centered .terminal-dashboard-tile-xterm` and `.drawer-terminal-grid-tile.centered .drawer-terminal-grid-tile-xterm` flipped from `pointer-events: auto; user-select: text;` → `pointer-events: none; user-select: none;` so xterm's selection service never receives mousedowns; small "Double-click to select text" chip rendered via `::after` on the centered preview (top-right, 11 px, 0.55 black bg, 75 % opacity). Single-click on the xterm body now falls through to the tile-root click handler, which uncenters — same behaviour as backdrop click. Double-click still routes to the dedicated view. Keyboard input is unaffected because `term.focus()` puts focus on `.xterm-helper-textarea` programmatically and keyboard events ignore `pointer-events`. Long-term path (FitAddon-based centered tile that selects fine) deferred to a later ticket — moderate refactor (second xterm instance + FLIP-vs-fit timing). `docs/25-terminal-dashboard.md` §25.7, `docs/36-drawer-terminal-grid.md` §36.5, and `docs/manual-test-plan.md` §terminal-fundamentals updated.

**Terminal-prompt overlay → project-tab anchor (HS-8012):** §52's `terminalPromptOverlay.tsx` used to mount inside the terminal pane (drawer `.terminal-body` / dashboard `.terminal-dashboard-dedicated-body` / drawer-grid dedicated-body) via `position: absolute; bottom: 12px; left: 12px; right: 12px` — popup visually lived inside the bottom drawer. Users wanted it to share spatial convention with the channel-permission popup (`.permission-popup`) and appear below the active project tab instead. Fix: API renamed `anchor: HTMLElement` → optional `projectSecret?: string` (the secret of the project whose terminal raised the prompt); overlay mounts on `document.body` with `position: fixed; z-index: 3000`; new `attachOverlayToBody(overlay, projectSecret)` helper finds `.project-tab[data-secret=...]` and applies tab-bottom-anchor positioning math (mirrors `permissionOverlay.tsx`'s `tabRect.bottom + 4` logic). When the tab isn't found / has zero-size (dashboard mode hides tabs), the overlay keeps the SCSS default `top: 56px; left: 50%; transform: translateX(-50%)` for a sensible top-center fallback. Cleanup query broadened from `opts.anchor.querySelectorAll(...)` to `document.querySelectorAll('.terminal-prompt-overlay')` so the idempotency guarantee survives the new mount target. `terminal.tsx::kickPromptDetector` passes `inst.wsSecret`; `terminalTileGrid.tsx::enterDedicatedView` passes `tile.entry.secret`. The local `promptOverlayAnchor` capture in `enterDedicatedView` is no longer needed and was removed (the surrounding `dedicatedBody` query stays — it's still used to apply per-theme background colour). SCSS `.terminal-prompt-overlay` switched from `position: absolute; bottom/left/right: 12px; z-index: 30` → `position: fixed; top: 56px; left: 50%; transform: translateX(-50%); z-index: 3000`. `docs/52-terminal-prompt-overlay.md` §52.5 updated.

**Streaming shell output — twin-pre running-shell row + click-to-expand (HS-8015 follow-up #2):** The HS-8015 + HS-8015-follow-up commit landed live partial output in the Commands Log entry only, with a flicker-free hydrate over the 5 s poll. But the user reported they STILL didn't see data coming in as it streamed — root-cause two compounding bugs in `commandLog.tsx`'s `renderLogEntry` running-shell branch. (1) The single `<pre class="command-log-detail command-log-shell-partial">` had a CSS cascade conflict: `.command-log-detail` is declared LATER in `styles.scss` than `.command-log-shell-partial` and at equal specificity, so its `display: -webkit-box; -webkit-line-clamp: 3; overflow: hidden` won — the live preview was pinned to the FIRST 3 lines with no scroll, so a chatty command's most recent output was never visible. (2) The click handler gated expansion on `hasMore`, computed from `entry.detail` (the still-being-written command line, typically <300 chars / 1 line) — so clicking the row did nothing while the command ran. Fix mirrors the completed-shell pattern: each running-shell row now mounts TWO partial pres (preview + full), both sharing `data-shell-partial-id` so the live writer + hydrate find both, plus a `data-shell-partial-mode` attribute (`preview` / `full`) that selects what each pre receives. New pure helper `writePartialIntoPre(pre, partial)` reads the attribute and writes either `tailLines(stripAnsi(buffer), 3)` (preview) or the full `stripAnsi(buffer)` (full). The full pre carries `.command-log-shell-partial-full` with `max-height: 240px; overflow-y: auto` so a chatty long-running command doesn't push every other entry off-screen. The `.command-log-shell-partial` rule lost its `max-height/overflow-y` in favour of the more-specific `.command-log-detail-full.command-log-shell-partial-full`, ending the cascade conflict. Click handler now treats `isExpandable = hasMore || isRunningShell`, so clicking a running-shell row toggles `.expanded` and the existing `.command-log-detail` ↔ `.command-log-detail-full` display swap reveals the full pre. The expanded state survives the post-completion re-render because `expandedEntryIds` is keyed on entry id. `tailLines` was restored to `src/client/stripAnsi.ts` (HS-8015 had deleted it alongside the sidebar preview). 13 new unit tests across `commandLog.test.ts` (writePartialIntoPre × 4 — preview-mode / full-mode / no-mode-fallthrough / strip-ansi-both-modes; applyShellPartialEvent twin-pre × 2 — both-pres-updated / chunks-stay-in-sync; hydrateRenderedShellPartials × 1 — repaints-both-after-rerender) + `stripAnsi.test.ts` reuses the existing 8 stripAnsi tests against the now-restored `tailLines` import. New e2e `e2e/shell-streaming-live.spec.ts` runs a 7-second emit-and-sleep streaming command, verifies the preview pre fills with `stream-line-N` mid-flight + the row is initially collapsed, clicks to expand, asserts the full pre is now visible with all chunks emitted so far, captures three screenshots (collapsed / expanded / completed) for visual evidence. Second e2e test verifies the preview shows the trailing 3 lines (NOT the first 3) — the visual half of the bug. `docs/53-streaming-shell-output.md` §53.5 Phase 3 bullet list extended with the twin-pre mechanic.

**Streaming shell output — Commands-Log only (HS-8015):** Reverted the HS-7983 sidebar partial-output preview. Pre-fix, every running custom-shell command had a faded italic `tailLines(stripAnsi(partial), 2)` preview wedged into the sidebar directly beneath the originating button (`attachShellPreviewToButton` mounted a `<div class="channel-command-preview">` after the button + listened for `hotsheet:shell-partial-output`). Users wanted live updates *only* in the Commands Log entry — "just like they do when the command is finished" but live — without the sidebar duplicate. Fix removes `attachShellPreviewToButton` from `commandSidebar.tsx`, drops the `btnEl` parameter on `runShellCommand`, drops the `onComplete` callback on `startShellPoll`, and deletes the `.channel-command-preview` SCSS block. The lone surviving consumer is `applyShellPartialEvent` in `commandLog.tsx`, which now also fires the first-use discoverability toast (`maybeFireShellStreamFirstUseToast` — moved here from the deleted sidebar listener). Toast text updated from "streams as it arrives" → "streams live in the Commands Log — Settings → Experimental to disable." so users know where to look. The client-local `tailLines` in `src/client/stripAnsi.ts` was its only caller and was removed alongside it; the corresponding 5 `tailLines` test cases in `stripAnsi.test.ts` were deleted. `stripAnsi` itself stays — Commands Log live render still strips ANSI before writing the partial. Server-side buffering + event dispatch is unchanged. Setting gate (`shell_streaming_enabled`) still works (gates Commands Log render only). 72/72 tests pass across `commandSidebar.test.ts` + `commandLog.test.ts` + `stripAnsi.test.ts` + `shortcuts.test.ts`. `docs/53-streaming-shell-output.md` §53.5 / §53.7 updated.

**Modal dialogs are first-class for keyboard input (HS-8033):** Pre-fix every global shortcut in `shortcuts.tsx` ran whenever the keyboard event landed on a non-form-input element — so Cmd+A while the settings / feedback / confirm dialog was open silently selected every ticket behind the backdrop, and a fast Cmd+A → Backspace deleted them. Three new exports in `src/client/shortcuts.tsx`: `MODAL_OVERLAY_SELECTORS` (the canonical registry of modal backdrops — `#open-folder-overlay`, `#settings-overlay`, `.confirm-dialog-overlay`, `.cmd-editor-overlay`, `.custom-view-editor-overlay`, `.feedback-dialog-overlay`, `.grouping-prompt-overlay`, `.hide-terminal-dialog-overlay`, `.print-dialog-overlay`, `.quit-confirm-overlay`, `.reader-mode-overlay`, `.tags-dialog-overlay`, `.quicklook-overlay`); `findVisibleModalOverlay(root?)` (walks the registry and returns the first mounted+visible match, where "visible" means `!hidden && style.display !== 'none' && isConnected && computed.display !== 'none' && computed.visibility !== 'hidden'`); `shouldBailForActiveModal(target)` (true whenever any modal is open, regardless of whether the target is inside or outside it — modals own the keyboard until Esc dismisses them; modal-internal shortcuts like Cmd+Enter to save are wired on the input element directly so they fire BEFORE this document-level bail). Wired into both keydown listeners in `bindKeyboardShortcuts`: the capture-phase Cmd+Z / Cmd+Shift+Z handler bails so input-level native undo wins inside dialogs, AND the bubble-phase main shortcut block bails right after the Esc handler (Esc still closes overlays since the bail comes after). Non-modal popups (`.terminal-prompt-overlay`, `.permission-popup`, context menus, dropdowns) are deliberately excluded from the registry — they don't take focus from the underlying surface so global shortcuts coexist with them. 15 new unit tests in `shortcuts.test.ts` (`findVisibleModalOverlay` × 9 covering empty / settings-by-id / open-folder-by-id / display-none-skip / class-based confirm + feedback + quit-confirm / hidden-attribute skip / non-modal-popups-not-matched / registry-shape; `shouldBailForActiveModal` × 6 covering no-modal / target-outside / target-inside / null-no-modal / null-with-modal / non-modal-popup-no-bail). `docs/4-user-interface.md` §4.11 keyboard table updated with an HS-8033 callout. **When introducing a new modal dialog, add its overlay class to `MODAL_OVERLAY_SELECTORS`.**

**Plain Esc reaches the terminal (HS-8011):** Three Hot Sheet handlers used to swallow Esc when xterm had focus — (1) `shortcuts.tsx`'s blur-input/clear-selection branch (xterm's helper textarea matches `isEditableTarget`, so `active.blur()` fired on every Esc), (2) `terminalDashboard.tsx`'s capture-phase Esc routing (dedicated → centered → exit), (3) `drawerTerminalGrid.tsx`'s capture-phase Esc routing (dedicated → centered → exit). The result was that programs running in the terminal (`claude code` interrupt, `vim` insert-mode exit, `less` quit, `claude code` permission cancel-via-Esc) never saw the keystroke. Fix is two new pure helpers in `shortcuts.tsx`: exported `isElementInTerminal(target)` (was the private `isTerminalFocused()` body) and `shouldEscapeBypassHotsheet(target, altKey)` which returns true when terminal is focused AND Alt is NOT pressed. All three handlers now early-return when the gate fires; xterm's natural Esc → `\x1b` PTY forward stands. Opt+Esc (macOS) / Alt+Esc (Linux/Windows) is the explicit escape hatch — runs Hot Sheet's normal Esc behaviour without the user having to click out of the terminal first. 9 new tests in `shortcuts.test.ts` (`isElementInTerminal` × 6 covering xterm container, drawer-pane, root match, outside, non-HTML, null + `shouldEscapeBypassHotsheet` × 5 covering plain-Esc-in-terminal, Opt-Esc-in-terminal, both-Alt-states-outside-terminal, null target). Settings → Terminal docs §22.18 table updated with the new Esc rows.

**Terminal prompt overlay Phase 1 (HS-7971 + HS-7980):** New client subsystem that watches the active terminal for interactive prompts (initially Claude-Ink numbered choices like the dev-channels warning + the Edit-tool diff confirmation) and surfaces a Hot Sheet overlay anchored to the terminal pane so the user can answer with the mouse instead of typing into the raw PTY. Three new modules:

- `src/shared/terminalPrompt/parsers.ts` — pure parser registry (`PROMPT_PARSERS` ordered array; first non-null `match()` wins). **HS-8029 Phase 1 (2026-04-30) moved this from `src/client/terminalPrompt/parsers.ts` to `src/shared/terminalPrompt/parsers.ts`** so the new server-side scanner (`src/terminals/promptScanner.ts`) can import it; six client importers (`terminalPromptOverlay.tsx`, `terminalPrompt/{allowRules,allowRules.test,autoAllow,detector,parsers.test}.ts`) updated to the new path. No shape change, no test change. Phase 1 ships only `claudeNumberedParser` matching the `❯ N. …` / `> N. …` / `Enter to confirm · Esc to cancel` shape (HS-7995 widened the cursor character set to `[>❯▶►]` after a script(1) capture confirmed modern Claude Code renders the highlighted-row marker as `❯` U+276F, not the legacy ASCII `>` — pre-fix the highlighted option line failed the regex, leaving only one parsed choice and tripping the ≥ 2 contiguous-choice guard). Helpers: `hashQuestion` (djb2 8-hex stable hash for allow-rule signatures), `trimRows`, `isClaudeNumberedFooter`, `buildNumberedPayload(choices, chosenIndex)` (computes arrow-key-nav delta from the highlighted index then `\r`), `buildNumberedCancelPayload()` (`\x1b`). `MatchResult` carries `signature = parser_id:hashQuestion(question):chosenIdx` so Phase 3 allow-rules can key on it from day one. HS-7980 follow-up: parser walks the question region from the numbered block back to the top of the scan window preserving blank-row separators so an inline diff above the choices keeps its structure in `questionLines`. **HS-8050 (2026-05-01) follow-up:** capped `claudeNumberedParser`'s upward question-region walk so the framed-`<pre>`-context block stops at any run of 2+ consecutive blank rows (visual section break) AND at a hard cap of `MAX_QUESTION_CONTEXT_ROWS = 15`. Pre-fix the walk pulled every row above the numbered block back to row 0 of the visible scan window — when claude code's TUI was running concurrently, the framed block ended up showing the legitimate prompt body PLUS the surrounding TUI decorations ("Listening for channel messages...", `?` for shortcuts, `high - /effort` status, etc.). User's exact complaint: "everything after 'Experimental ...' is just the claude code prompt decorations and input." Single-blank diff separators (HS-7980) still preserved — the gate is 2+ in a row. 4 new unit tests cover section-break stop / single-blank diff preservation / 15-row cap / dev-channels regression. **HS-8037 (2026-04-30) follow-up:** added two new exported helpers — `isDecorativeLine(line)` (true for rows whose only non-whitespace characters are box-drawing / horizontal-rule glyphs) and `pickTitleLine(lines)` (last `?`-terminated line wins for diff-shape prompts, otherwise the first non-blank, non-decorative line) — and switched `claudeNumberedParser`'s `question` field from `questionLines.join(' ')` to `pickTitleLine(questionLines)`. The pre-fix join produced a wall-of-text title that duplicated everything already shown verbatim in the framed `<pre>` context block below; the user flagged this on HS-8037 ("redundantly shows … with a bunch of horizontal lines before it"). The overlay's `openNumberedOverlay` runs the new local `stripContextLines(questionLines, question)` helper before joining for the framed block, removing both the title-line and any pure-decoration rows; the framed block only renders when content remains after the strip. New regression coverage: 2 dev-channels-shape + decorative-border parser tests, 9 helper unit tests for `isDecorativeLine` + `pickTitleLine`, and a new `src/client/terminalPromptOverlay.test.ts` (4 happy-dom tests covering the full overlay render, including the title-line-stripped-from-context invariant + the title-only-prompt-omits-context-block fallback).
- `src/client/terminalPrompt/detector.ts` — one detector per `TerminalInstance`, debounced 100 ms (`SCAN_DEBOUNCE_MS`) scan of the last 30 rows (`SCAN_ROW_COUNT` — wide enough for HS-7980's diff context). Pure `decideDispatch(rows, state)` helper returns `{match, nextLastSig}` so unit tests don't need xterm. State machine: `overlayOpen` skips while overlay mounted, `suppressed` honours "Not a prompt" until the next user keystroke, `lastDispatchedSignature` skips re-dispatching the same prompt across cursor-blink redraws. Public hooks: `createDetector`, `notifyChunk`, `disposeDetector`, `markDetectorClosed`, `markDetectorSuppressed`, `notifyUserKeystroke`.
- `src/client/terminalPromptOverlay.tsx` — `openTerminalPromptOverlay({match, anchor, onSend, onClose, onDismissAsNonPrompt})` mounts a non-modal overlay inside the passed `anchor` (`.terminal-body` for the drawer pane). Phase 1 only renders the `numbered` shape; `yesno` / `generic` no-op until Phase 2. Multi-line context block (HS-7980) renders in `<pre class="terminal-prompt-overlay-context">` only when `questionLines.length > 1` so simple prompts stay compact. Choice rows tinted with `--accent` when `highlighted`. Capture-phase Escape sends cancel payload and beats `shortcuts.tsx`'s blur-input handler. `onSend` returning false surfaces an inline disconnect error without closing. **HS-8210 Phase C (§58.6)** — `onSend` signature extended to `(payload, extras?)` where `extras = { choiceIndex?, choiceLabel?, dontRememberChannel? }`. `openNumberedOverlay` checks `match.channel !== undefined` and replaces the always-allow checkbox row with a muted footer line `'This channel will be auto-approved next time. [ ] Don't remember'` (`.terminal-prompt-overlay-channel-rule-row`) — the always-allow checkbox is fully subsumed by the channel rule. The opt-out checkbox state flows through `extras.dontRememberChannel` to the dispatcher; choice clicks always pass `choiceIndex` / `choiceLabel` so callers can build a channel-keyed rule without re-deriving them from the keystroke payload. New SCSS block `.terminal-prompt-overlay-channel-rule-row` (muted text, smaller font, opt-out checkbox right-aligned).

Wiring in `src/client/terminal.tsx`: `kickPromptDetector(inst)` lazy-creates the detector on first WebSocket message (so the xterm handle is live), `notifyUserKeystroke(inst.promptDetector)` fires from `term.onData`, `disposeDetector(inst.promptDetector)` runs in `teardown()`. SCSS block at `styles.scss:8502+` (`.terminal-prompt-overlay`, `-header`, `-title`, `-close`, `-context`, `-choices`, `-choice` with `is-highlighted` modifier, `-cancel`, `-not-a-prompt`, `-error`). 38 unit tests across `terminalPrompt/parsers.test.ts` (32) + `detector.test.ts` (6) covering hash stability, footer detection, claude-numbered happy paths (dev-channels warning + 3-choice + multi-line + HS-7980 diff-with-3-choices), parser negatives (no footer / single option / digit gaps / 0-index / empty rows / footer-mid-buffer / markdown lists), payload builders (Enter-only / forward / backward / no-highlight fallback / cancel), detector lifecycle (overlayOpen skip / suppressed skip / signature dedupe / signature clears on no-match). Phases 2 / 3 / 4 deferred via HS-7986–HS-7988; doc-sync via HS-7989. See `docs/52-terminal-prompt-overlay.md`.

**Magnified terminal-grid navigation (HS-8028 + 2026-04-30 follow-up):** Shift+Cmd+Arrow (macOS) / Shift+Ctrl+Arrow (Linux/Windows) switches the magnified target to the next tile in the indicated direction while a tile is centered or dedicated in the §25 dashboard or §36 drawer-grid. New `isMagnifiedNavShortcut(e, isMac)` in `src/client/terminalKeybindings.ts` follows the same `hasPlatformPrimaryModifier` pattern as `isJumpShortcut` but layers Shift on top so the two don't collide. New pure helper module `src/client/gridNavGeometry.ts` exports `pickGridNeighbourIndex(from, candidates, direction)` — strict grid-neighbour algorithm: for `left` / `right` the candidate must overlap with `from` vertically (same row) AND lie strictly in the indicated half-plane; for `up` / `down` same shape with horizontal overlap. Among qualifying candidates the smallest edge-gap wins. Returns `-1` when nothing qualifies. Pre-fix used a perpendicular-weighted cone metric that would jump rows when no same-row neighbour existed; the user's HS-8028 follow-up reply pinned the corrected behaviour ("if i click left, it should show the next terminal that is immediately to the left of the current one — ignored if none"). New helpers in `src/client/terminalTileGrid.tsx`: `bindMagnifiedNavHandler()` / `unbindMagnifiedNavHandler()` install a single capture-phase document keydown listener (idempotent); `findNextTileInDirection(from, direction)` is now a thin wrapper around `pickGridNeighbourIndex`; `magnifyTile(next, mode)` performs the swap (centered: synchronous `finishUncenterTile` + `centerTile(next)`; dedicated: `dedicated.priorCenteredTile = null` + `exitDedicatedView()` + `enterDedicatedView(next, null)` so exit doesn't animate through a stale centered state). Lifecycle: bind on `centerTile` + `enterDedicatedView`, unbind on `uncenterTile` (only when `dedicated === null`) + `exitDedicatedView` (only when `view.priorCenteredTile === null`) + `teardownAll`. Capture phase + `e.preventDefault()` + `e.stopPropagation()` ensure xterm doesn't translate the chord into shell escape sequences. 13 tests in `terminalKeybindings.test.ts` cover the chord recognition; 15 tests in the new `gridNavGeometry.test.ts` cover the strict grid-neighbour algorithm (3×2 grid happy paths × 4 directions, edge no-ops, zero-size skip, partial-row inclusion, touching-but-not-crossing exclusion, far-vs-near tiebreak, 3×3 4-neighbour fixture). End-to-end DOM nav between tiles deferred to manual spot-check / future Playwright. See `docs/56-magnified-grid-nav.md`.

**Ticket cross-references (HS-8036, 2026-04-30):** new `src/client/ticketRefs.ts` post-processes rendered HTML to wrap ticket-number patterns (e.g. `HS-1234`, `BUG-42`) in `<a class="ticket-ref" data-ticket-number="…" href="javascript:void(0)">…</a>` anchors. Prefix set is fetched once at app init from `GET /api/tickets/prefixes` (DB-scanned distinct prefixes from `tickets.ticket_number` + the project's `ticketPrefix` from `settings.json` + `HS` fallback). Self-references skipped via `currentTicketNumber` parameter. Wired into `noteRenderer.tsx::renderNotes`, `detail.tsx::renderDetailsMarkdown`, `readerOverlay.tsx::openReaderOverlay`, and the stacking dialog itself (`ticketRefDialog.tsx`). Document-level click handler (`bindTicketRefGlobalClickHandler`) intercepts `.ticket-ref` clicks anywhere in the DOM and dispatches to `openTicketRefDialog(ticketNumber)`. `ticketRefDialog.tsx` mounts a stacking modal — each click on a reference inside an open dialog pushes a new dialog with a 30 px translate-offset; backdrop click + Escape pop the top one only; "Open in detail panel" CTA closes the entire stack and switches the main detail view via `openDetail(id)`. Server-side: new `GET /api/tickets/by-number/:number` endpoint in `src/routes/tickets.ts` for cross-project / cache-miss lookups; `listKnownTicketPrefixes()` in `src/db/tickets.ts` runs `SELECT DISTINCT ticket_number FROM tickets` and extracts unique prefixes via `^([A-Z][A-Z0-9_]*)-\d+$`. **Read-only in v1** — full editable behaviour deferred to a follow-up (requires `detail.tsx` refactor — ~30 `getElementById` callsites). 13 unit tests in `src/client/ticketRefs.test.ts` covering regex builder + linkify edge cases (word boundaries, multiple matches, attribute-value safety, self-reference skip, empty-prefix fallback, link-inside-code-blocks). SCSS at `styles.scss:end` for `.ticket-ref` (thin-underlined accent token, hover chip-ish background) and `.ticket-ref-dialog-*` (mirroring `.reader-mode-*` shape with z-index range 2600+). See docs/55-ticket-cross-references.md.

**Global terminal-checkout module (HS-8031 Phase 1 + HS-8041 Phase 2.1 + HS-8042 Phase 2.2 + HS-8048 Phase 2.2b + HS-8044 Phase 2.4 + HS-8045 Phase 2.5 cleanup):** new `src/client/terminalCheckout.tsx` owns one xterm.js instance per `(projectSecret, terminalId)` keyed map. Sole exported function `checkout(opts: CheckoutOptions): CheckoutHandle` claims the live xterm into the caller's `mountInto`; the returned handle exposes `release()`, `term`, `isTopOfStack()`. LIFO stack tracks who's currently rendering — most recent caller wins (live xterm DOM-reparents into their `mountInto`); previous owners get a plain `Terminal in use elsewhere` placeholder painted into their `mountInto` with the Lucide `terminal-square` icon. Per HS-8030's locked decisions: (1) skip `term.resize` + `ws.send({type:'resize'})` when cols/rows match the previous owner so TUI programs don't see SIGWINCH on every same-size handoff; (2) plain placeholder, not a frozen snapshot — xterm's canvas glyph metrics + box-drawing alignment + font kerning don't translate cleanly to a CSS-painted `<pre>`; (6) virtualization fully disposes the xterm + closes the WebSocket on empty stack so the dashboard tile virtualization stays memory-safe across projects. The next `checkout()` for the same key is identical to a first-ever attach — the server's `attach()` returns `history` and the WebSocket `'history'` control message replays scrollback before live data. xterm.js requires `term.open(container)` once at construction; the module opens into an offscreen `terminal-checkout-parking-sink` div in `document.body` so the first checkout can DOM-reparent the xterm element via `appendChild` without dealing with a "open() not yet called" race. `entryCount()` exposes the entry-map size for sanity checks; `peekEntryDims(secret, terminalId)` returns the entry's current `(cols, rows)` or null (HS-8207 — used by the §47 popup to pre-pick existing dims for `checkout(...)` so the swap-time resize is a no-op); `_inspectStackForTesting()` returns a deep snapshot for assertions; `_resetForTesting()` drops every entry without going through dispose so unit-test cleanup doesn't bleed across tests. SCSS at `styles.scss:end` for `.terminal-checkout-placeholder` (flex-centered, muted-text + bg-secondary background, 32px icon, 12px text). 17 unit tests in `terminalCheckout.test.ts` (happy-dom — module detects `typeof WebSocket === 'undefined'` and bails to `ws=null` so the stack semantics are testable without a real socket): single consumer × 4 (create / mount / dispose / idempotent-release) + LIFO stack × 4 (push / restore-top / non-top-release / dispose-on-last) + resize policy × 4 (updates dims / **skips on same-size** via `vi.spyOn(term, 'resize')` / fires on different-size / restore re-applies dims) + cross-project independence × 2 + re-checkout-after-dispose × 1 + `_inspectStackForTesting` shape × 2. **Phase 1 (HS-8031) shipped infrastructure-only.** **Phase 2.1 (HS-8041, 2026-04-30) wired the quit-confirm preview pane** — `src/client/quitConfirm.tsx::selectRow` now calls `checkout({ projectSecret, terminalId, cols: 80, rows: 30, mountInto: previewEl })` (constants `QUIT_PREVIEW_COLS` / `QUIT_PREVIEW_ROWS` are the initial seed only); the rAF after checkout calls `handle.fit.fit()` + `handle.resize(handle.term.cols, handle.term.rows)` so the canvas fills the preview pane regardless of theme / font / dialog width (HS-7969 follow-up 2026-04-30 — pre-fix the static 80×30 left empty background bands on the right + bottom of the pane). `finish()` releases the live handle before tearing down the overlay; the `<pre class="quit-confirm-detail-preview">` is now a `<div>` since xterm needs a block container. Critical contract: cancel-then-checkout ordering — `release()` fires before `checkout()` for the new row so the LIFO stack never briefly holds two handles for the same `mountInto` (would flash a placeholder + write the new xterm OVER it). `paintPreviewContent` / `paletteFromTheme` / `fetchScrollbackPreview` / `applyAppearanceToPreview` are KEPT in `quitConfirm.tsx` per the HS-8041 ticket scope until the cleanup sub-ticket (HS-8045 — sub-ticket 5 of HS-8032) bulk-deletes the §37 ANSI-spans preview path; three `void <fn>;` references at the bottom of the file defeat unused-vars without an inline disable. `showQuitConfirmDialog` was previously private; now exported so the dismiss-while-loading race regression in `quitConfirm.test.ts` can drive the dialog directly. 5 new tests in the `quit-confirm preview pane checkout (HS-8041 §54.5.2)` describe block cover auto-select-on-mount / row-swap-cancel-then-checkout / rapid-clicks / cancel-button release / Quit-Anyway release. **Phase 2.2 partial (HS-8042, 2026-04-30)** migrated the **dedicated-view path** in the shared `terminalTileGrid.tsx` module — `enterDedicatedView` calls `checkout(...)` instead of constructing its own xterm + WebSocket; `exitDedicatedView` calls `view.checkout.release()` (auto-restores tile mount or disposes entry). Two new public additions on `CheckoutHandle`: `fit: FitAddon` (exposed for dedicated's `fit.fit()` calls) and `resize(cols, rows)` (mid-checkout dim change with skip-on-same-size guard, routes through internal `applyResizeIfChanged`). The history-frame WS handler in checkout gained a **resize-first-write** step when the message carries `cols`/`rows` — closes a latent HS-8041 bug where long-scrollback terminals would render at wrong dims. **Phase 2.2b (HS-8048, 2026-04-30) shipped the tile preview migration** — `mountTileXterm` + `connectTileSocket` consolidated into a single `mountTileViaCheckout(tile)`; `tile.term`/`tile.ws`/`tile.xtermRoot` replaced with `tile.checkout: CheckoutHandle | null` (xtermRoot kept as a DOM container for the live xterm element). Per-consumer xterm config unified to checkout's defaults (`cursorBlink: true, scrollback: 10_000`) — pre-fix tile had `cursorBlink: false, scrollback: 1000` from HS-7990, but per-swap runtime overrides via `term.options =` were too fragile (scrollback reduction at runtime can lose history) so the 10× scrollback for tiles was accepted. CSS-scaling preserved via `onRestoredToTop` callback that calls `tile.checkout.resize(TILE_INITIAL_COLS, TILE_INITIAL_ROWS)` then `reapplyTileScaleFromPreview` then re-attaches the screen `ResizeObserver` (factored into new `attachScreenObserver(tile)` helper). Virtualization integration: `softDisposeTile` / `disposeTile` now call `tile.checkout?.release()` instead of disposing local term/ws — the empty-stack dispose path in checkout (§54.3.3) handles xterm + WS teardown. New top-of-stack guard in `resyncTilePtyFromCellMetrics` (`if (!tile.checkout.isTopOfStack()) return`) prevents the tile's screen ResizeObserver from resizing the live xterm out from under another consumer (dedicated, quit-confirm) that's holding it. **`term.onData` keystroke wiring moved into `terminalCheckout.tsx::openCheckoutWebSocket`** — closes a hidden HS-8042 regression where dedicated-view typing was silently broken (HS-8042 removed dedicated's local `term.onData(ws.send)` on the wrong assumption that checkout was already doing it). 5 new tests in `src/client/terminalTileGrid.test.ts` (the module's first unit-test file — was Playwright-only): rebuild-creates-entry, non-alive-tiles-skip-entry, dispose-returns-entryCount-to-zero, rebuild-disposes-old-mounts-new, cross-project-independence. happy-dom's `IntersectionObserver` doesn't fire entries so the test setup stubs `globalThis.IntersectionObserver = undefined` to force the eager-mount fallback the source already supports. **Phase 2.3 (HS-8043) reduced to verification** — `terminalDashboard.tsx` uses the same shared `mountTileGrid()` from `terminalTileGrid.tsx` so the dashboard automatically inherits the migration. **Phase 2.4 (HS-8044, 2026-04-30)** migrated `src/client/terminal.tsx` (~2097 lines pre-fix) — `mountXterm` + `connect` collapsed into `mountInstanceViaCheckout(inst, secret)` which calls `checkout(...)` and wires the drawer's per-instance chrome (SearchAddon, OSC 0/2/7/8/9/133, theme, custom key bindings, bell, title, CWD chip, prompt-resume) post-checkout. The drawer's `connect()` + `scheduleReconnect()` paths deleted entirely — checkout's new module-driven reconnect handles WS-close → reconnect for any entry with a non-empty consumer stack. New `intentionallyClosing: boolean` flag on `StackEntry` so the close-event listener can distinguish explicit `release()` from network drop. New `onControlMessage` callback on `CheckoutOptions` lets the drawer subscribe to parsed JSON control messages (`'history'`, `'exit'`) without owning the WS — fans out to ALL stack consumers (not just top) so bumped-down consumers like the drawer-while-dashboard-dedicated still track exit / status. Term-level handler disposers captured in `inst.termHandlerDisposers` so re-mounts don't stack duplicate handlers atop the shared term. Hidden HS-8042 keystroke regression closed: `term.onData` registration moved into `createEntry` (was inside `openCheckoutWebSocket` and closed over the original WS reference; would dispatch keystrokes to the OLD dead WS after reconnect) — now reads `entry.ws` dynamically. **Phase 2.5 cleanup (HS-8045, 2026-04-30)** bulk-deleted the §37 ANSI-spans preview path: `src/client/ansiSpans.ts` + `ansiSpans.test.ts` (full file deletes); `paintPreviewContent`, `paletteFromTheme`, `fetchScrollbackPreview`, `applyAppearanceToPreview` + `ScrollbackPreviewResponse` interface in `quitConfirm.tsx` (kept dormant since HS-8041 with `void <fn>` references; deleted along with those references); `GET /api/terminal/scrollback-preview` route + `getTerminalScrollbackPreview` + `getTerminalScrollbackPreviewWithAnsi` registry helpers + `buildScrollbackPreview` + `buildScrollbackPreviewWithAnsi` snapshot helpers. `stripAnsi` and `tailLines` in `scrollbackSnapshot.ts` stay (pure utility functions with existing test coverage). — drawer / dashboard / drawer-grid surfaces continue to use their existing per-surface xterm management. Playwright e2e for Phase 1 was deferred — the existing per-surface e2es become integration coverage as Phase 2 sub-tickets wire real consumers.

**Server-side prompt scanner (HS-8029 Phase 1):** new `src/terminals/promptScanner.ts` runs the same shared parser registry server-side, per `TerminalSession`. Owns a small `@xterm/headless` instance (`SCANNER_COLS=200`, `SCANNER_ROWS=30` matching `SCAN_ROW_COUNT`, `SCANNER_SCROLLBACK=50`) instead of a full xterm.js render path — per the HS-8030 user directive ("option 2 sounds reasonable, we shouldn't need much back buffer, start off-the-shelf"). `@xterm/headless` is the official Node-friendly xterm build (zero deps, ~2 MB unpacked, no canvas / DOM). Imports via default-import + destructure (`import xtermHeadless from '@xterm/headless'; const { Terminal } = xtermHeadless;`) because the package's `main` is CJS with no `exports` field — Node ESM can't reach the named export directly. `createPromptScanner({onMatch})` returns `{ingest, notifyUserKeystroke, setSuppressed, resize, dispose}` plus two test-only async helpers (`_runScanNowForTesting`, `_readRowsForTesting`) that flush xterm's write queue via the empty-write callback before reading `buffer.active` (xterm queues writes asynchronously; without the callback `getLine()` returns stale rows). Wired into `src/terminals/registry.ts`: `createSession` constructs the scanner with a closure that stashes matches into `session.pendingPrompt`; the PTY `onData` hook calls `session.promptScanner.ingest(chunk)` alongside the existing scrollback push; `writeInput` calls `notifyUserKeystroke` + clears `pendingPrompt` so the user typing into the live terminal also clears the server-side flag; `restartTerminal` disposes + recreates the scanner so PTY-restart state doesn't leak; `destroyTerminal` / `destroyProjectTerminals` / `destroyAllTerminals` all dispose the scanner. **Phase 1 shipped infrastructure only.** Phase 2 (HS-8034, see next paragraph) added the cross-project surface + auto-allow gate. 15 new unit tests in `src/terminals/promptScanner.test.ts` cover ingest / debounce / dedup-by-signature / suppression / dispose / Buffer + Uint8Array + UTF-8-split chunk shapes / subscriber-error containment.

**Server-side auto-allow + cross-project surface (HS-8034 Phase 2):** allow-rule helpers moved from `src/client/terminalPrompt/allowRules.ts` to `src/shared/terminalPrompt/allowRules.ts`. `payloadForAutoAllow(match, rule)` (was in client `autoAllow.ts`) moved to shared too so the server can call it. Five client importers (`terminalTileGrid.tsx`, `terminal.tsx`, `terminalPromptAllowListUI.tsx`, `terminalPrompt/{allowRulesStore.ts, autoAllow.ts}`) updated to the new path; `allowRules.test.ts` + `allowRules.test.test.ts` moved alongside.

`src/terminals/registry.ts::handleScannerMatch(state, secret, dataDir, match)` is the new server-side gate. Reads `terminal_prompt_allow_rules` from `<dataDir>/settings.json` via `readFileSettings(dataDir)` + `parseAllowRules`, runs `findMatchingAllowRule(match, rules)`. On a hit: `payloadForAutoAllow` builds the keystroke payload (returns null for generic shape OR rule choice_index out of range — caller falls through to overlay), `state.pty.write(payload)` sends it directly, `addLogEntry('terminal_prompt_auto_allow', ...)` appends the audit entry (built by the new exported `buildAutoAllowAuditStrings` helper — channel-keyed rules append `(channel <match_channel>)` to the summary + `Channel:` line to the detail per HS-8210 Phase D §58.8; non-channel rules keep the legacy `(rule <id>)` shape), and `pendingPrompt` stays null so no overlay surfaces. On no hit: stash the match on `state.pendingPrompt` and call `notifyPromptWaiters(secret, terminalId)` (lazy dynamic import of `notifyBellWaiters` from `routes/notify.js` — same wake mechanism as bell-pending). `createSession`'s signature gained a `secret` parameter so the closure has it; `attach`, `ensureSpawned`, `restartTerminal` all pass it through. The `restartTerminal` recreate-scanner path uses the same `handleScannerMatch` closure so the auto-allow gate works after a `/restart`.

`GET /api/projects/bell-state` long-poll response extended from `{anyTerminalPending, terminalIds, notifications}` per project to also include `pendingPrompts: { [terminalId]: MatchResult }` (empty object when no matches). New registry helpers: `listPendingPromptsForProject(secret)` walks every session for the project and returns `Array<{terminalId, match}>`; `clearPendingPrompt(secret, terminalId)` drops the stashed match; `writePtyInput(secret, terminalId, payload)` is a thin write-to-PTY-and-return-bool; `setScannerSuppressed(secret, terminalId, suppressed)` toggles the scanner's "Not a prompt" flag.

Three new endpoints under `terminalRoutes` (mounted at `/api/terminal`):
- `POST /api/terminal/prompt-respond` — body `{terminalId, payload}`. Calls `writePtyInput` → `clearPendingPrompt` → `notifyBellWaiters`. Returns 404 when the PTY is dead so the client can re-mount the overlay.
- `POST /api/terminal/prompt-dismiss` — body `{terminalId, suppress?}`. Calls `clearPendingPrompt`; when `suppress: true` also calls `setScannerSuppressed(true)` so the "Not a prompt" dismiss path moves server-side.
- `POST /api/terminal/prompt-resume` — body `{terminalId}`. Calls `setScannerSuppressed(false)` (Resume chip on the terminal toolbar — HS-8035 wired the chip click to this endpoint after deleting the in-process detector suppression).

Cross-project secret resolution: each endpoint uses `c.get('projectSecret')` populated by `src/server.ts` from the `X-Hotsheet-Secret` header. The client sends the **affected** project's secret (not the active project's) via `apiWithSecret(path, secret, opts)` — works regardless of whether the affected terminal is currently mounted client-side.

Client-side dispatcher in `src/client/bellPoll.tsx::dispatchPendingPrompts` runs on every long-poll tick. Tracks `lastDispatchedPromptSignatures: Map<key, signature>` keyed on `${secret}::${terminalId}` AND a single `activeOverlayKey: string | null` slot (HS-8047 follow-up — pre-fix the dispatcher iterated every project's pendingPrompts and called `openTerminalPromptOverlay` once per project; that helper REMOVES every existing `.terminal-prompt-overlay` from the document before mounting, so each subsequent project's overlay obliterated the previous project's overlay without going through `onClose` and the user saw popups flash by one after another). Now collects candidates, sorts by `secret::terminalId`, opens at most one overlay; subsequent ticks pick up the next candidate after the active overlay closes (`onClose` clears `activeOverlayKey`). Also tears down the active overlay if its underlying server-side prompt cleared (e.g. user responded via terminal directly), and re-fires when the same key gets a new signature. Generic-shape matches deliberately skip the cross-project overlay (low confidence — would interrupt other-project work). `onSend` POSTs to `/api/terminal/prompt-respond` via `apiWithSecret(secret, ...)`; `onClose` POSTs to `/api/terminal/prompt-dismiss` and clears the dispatched signature + the active-overlay slot so the next queued prompt can surface; `onAddAllowRule` still uses `appendAllowRule` from `allowRulesStore.ts` (writes to the affected project's settings.json so the next match auto-allows server-side). 7 happy-dom unit tests in `src/client/bellPoll.test.ts` cover the serialization rules.

**Client-side detector deleted (HS-8035, 2026-04-30):** `src/client/terminalPrompt/detector.ts` + `detector.test.ts` + `src/client/terminalPrompt/autoAllow.ts` are gone. `src/client/terminal.tsx` lost the `kickPromptDetector` function, the `inst.promptDetector` field on `TerminalInstance`, the `notifyUserKeystroke` call in `term.onData`, the `disposeDetector` block in `teardown()`, and the entire detector-construction closure (the auto-allow gate + overlay-mount path that lived in `kickPromptDetector::onMatch`). `src/client/terminalTileGrid.tsx` lost the dedicated-view detector wiring (the `kickDedicatedPromptDetector` closure, the `promptDetector` field on `DedicatedView`, the dispose in `exitDedicatedView`). Both files also lost the now-unused imports (`buildAllowRule` / `appendAllowRule` / `tryAutoAllow` / `openTerminalPromptOverlay` for terminal.tsx; same set + `state` for terminalTileGrid.tsx). Server-side scanner (HS-8029 + HS-8034) is now the only detection path. The terminal-toolbar `.terminal-prompt-resume-chip` POSTs `/api/terminal/prompt-resume` instead of clearing in-process detector suppression — the server's scanner clears its own suppression on PTY-byte arrival (`writeInput` calls `notifyUserKeystroke`), so the client's `term.onData` hook just hides the chip locally without a separate POST. New e2e regression coverage in `e2e/terminal-prompt-cross-project.spec.ts` (2 tests): cross-project numbered-prompt overlay surfaces + responds via `apiWithSecret('/terminal/prompt-respond')`; generic-shape pendingPrompts deliberately do NOT auto-surface. The 6 deleted client-side detector tests are subsumed by the 15 server-side `src/terminals/promptScanner.test.ts` tests.

Tests: 6 new tests for `payloadForAutoAllow` in `src/shared/terminalPrompt/allowRules.test.ts` (numbered / yes / no / generic-skip / out-of-range / negative-index). 1 new test in `src/routes/projects.test.ts` covering the bell-state `pendingPrompts` shape. New `src/routes/terminal.test.ts` (9 tests) covering `/prompt-respond` (success / missing-terminalId / missing-payload / dead-PTY-404), `/prompt-dismiss` (default / with-suppress / missing-terminalId), `/prompt-resume` (success / missing-terminalId). 110 → 117 across `terminalPrompt/` + `routes/projects` + scanner; 117 + 19 + 9 = relevant suite green.

**Terminal prompt overlay Phase 1.5 (HS-7985):** Per-dedicated-view detector + overlay anchor so prompts surface correctly when the user is in the dashboard's full-pane view or the drawer-grid's dedicated tile (Phase 1 only handled the drawer pane). Three changes:

- New `src/client/terminalDedicatedState.ts` exports `hasOpenDedicatedTerminalView()` — pure DOM query for `.terminal-dashboard-dedicated, .drawer-terminal-grid-dedicated`. 5 unit tests in `terminalDedicatedState.test.ts` (no-mount / dashboard / drawer-grid / unmount / similar-classname-rejection).
- `src/client/terminalTileGrid.tsx::enterDedicatedView` now creates its own `Detector` (lazy-init on first WS chunk, captured via closure over the dedicated view's `term`/`ws`/`encoder`). Overlay anchors to `.terminal-dashboard-dedicated-body` (or `.drawer-terminal-grid-dedicated-body`) — both bodies already have flex layout that allows absolute-positioned children. `term.onData` calls `notifyUserKeystroke` on the dedicated detector; the WS message handler calls `kickDedicatedPromptDetector()` after every `term.write` and after every history-frame replay. `exitDedicatedView` calls `disposeDetector` before tearing down the term. New `promptDetector` field on the internal `DedicatedView` interface.
- `src/client/terminal.tsx::kickPromptDetector`'s `isActive` callback now AND-s `!hasOpenDedicatedTerminalView()` so the drawer detector stays silent while a dedicated view is open — guarantees one overlay per prompt across the multi-surface case. Grid tiles remain detector-free (too small for an overlay; would also explode the detector count under 50-tile dashboards).

**Terminal prompt overlay Phase 2 (HS-7986):** Second wave of parsers + the generic-fallback overlay shape + the per-instance suppression "Resume" chip on the terminal header. Three threads:

- `src/client/terminalPrompt/parsers.ts` extended with `yesNoParser` (regex `[[(]\s*(y(?:es)?)\s*\/\s*(n(?:o)?)\s*[\])]/i` matching the trailing visible line; rejects markdown / numbered-list / shell-comment lines via `looksLikeDocsLine()`; surfaces `yesIsCapital` / `noIsCapital` flags on the `YesNoMatch`) and `genericParser` (trailing-`?` heuristic on the last visible line, also routed through `looksLikeDocsLine()` so docs / numbered-list / quoted lines don't fire; emits `GenericMatch` with full `rawText` and `questionLines`). Registry priority: claude-numbered → yesno → generic so a Claude-Ink prompt that also contains `[y/n]` text wins claude-numbered. New payload helpers: `buildYesNoPayload(match, 'yes' | 'no')` returns lowercase `y\r` / `n\r` (most CLIs accept either case), `buildYesNoCancelPayload()` / `buildGenericCancelPayload()` return Esc, `buildGenericPayload(text)` appends `\r`. Generic-fallback responses are NEVER allow-listed in Phase 3 — design captured in §52.1 and the parserId is set to `'generic'` for the future auto-allow gate to short-circuit on.
- `src/client/terminalPromptOverlay.tsx` refactored from "numbered-only with early-return" into a `switch` on `match.shape`. Shared chrome (X / Cancel / Esc / "Not a prompt" link / error-line) wired by `wireSharedOverlay()` so each shape only declares its own choices area: `openNumberedOverlay` (existing logic), `openYesNoOverlay` (two side-by-side buttons with green/red border tints — `.terminal-prompt-overlay-yes`/`.terminal-prompt-overlay-no`), `openGenericOverlay` (verbatim monospaced `<pre>` of the visible rows + a free-form `<textarea>` + Submit button; Enter submits, Shift+Enter newline; textarea auto-focused via `queueMicrotask`).
- `src/client/terminalPrompt/detector.ts` exposes `clearDetectorSuppression(detector)` (explicit "Resume" intent — separate API from `notifyUserKeystroke` even though the effect is the same, so call sites read cleanly) and `isDetectorSuppressed(detector)` (pure read). `src/client/terminal.tsx` adds `.terminal-prompt-resume-chip` to the terminal header (hidden by default), captured into `inst.promptResumeChip`. Overlay's `onDismissAsNonPrompt` callback flips it visible; `term.onData` flips it back when the user types; click handler calls `clearDetectorSuppression` + hides the chip. SCSS: `.terminal-prompt-resume-chip` (accent-tinted pill) + `.terminal-prompt-overlay-yesno`/`.terminal-prompt-overlay-generic` variants extending the base block. ~32 new unit tests (yesno parser × 10, generic parser × 7, registry priority × 4, payload builders × 7, suppression helpers × 4).

**Terminal prompt overlay Phase 3 (HS-7987):** Per-project always-allow rules + auto-allow gate + audit-log endpoint. Five additions:

- `src/file-settings.ts` adds `'terminal_prompt_allow_rules'` to `JSON_VALUE_KEYS` so the array round-trips natively through settings.json. Mirrors the §47.4 `permission_allow_rules` precedent.
- `src/client/terminalPrompt/allowRules.ts` — pure helpers: `TerminalPromptAllowRule` shape (`{id, parser_id, question_hash, question_preview?, choice_index, choice_label?, created_at}`), `parseAllowRules(raw)` (tolerantly parses array OR stringified-JSON), `findMatchingAllowRule(match, rules)` (returns null on `generic` shape — see §52.1 — or no match), `buildAllowRule(match, choiceIndex, choiceLabel)` (throws on generic shape; minted id `tp_<base36-ts>_<hex6>`). 18 unit tests.
- `src/client/terminalPrompt/allowRulesStore.ts` — async cache: `loadAllowRules()` fetches `/file-settings` + populates the in-memory cache + notifies subscribers; `getAllowRules()` synchronous read for the auto-allow gate (kicks a background hydration on first call); `appendAllowRule(rule)` re-reads to avoid clobbering concurrent writes then PATCHes; `removeAllowRule(id)` mirror for the future Phase 4 delete affordance; `subscribeToAllowRules(cb)` for Phase 4's UI. App boot wires `void loadAllowRules()` into `reloadAppState()` so per-project rules hydrate on every project switch. **HS-8210 Phase C** — `appendAllowRule`'s pre-write dedupe key extended to include `(rule.match_channel ?? '')` so two channel-keyed rules for distinct channels (both with empty `question_hash`) don't collapse. `bellPoll.tsx::openCrossProjectOverlay::onSend` calls `buildChannelAllowRule(match, choiceIndex, choiceLabel)` + `appendAllowRule(rule, secret)` when `match.shape === 'numbered'`, `match.channel !== undefined`, `extras.dontRememberChannel !== true`, AND `extras.choiceIndex !== undefined` (cancel-payload paths skip).
- `src/client/terminalPrompt/autoAllow.ts` — `tryAutoAllow({match, send})` runs BEFORE the overlay mounts in both detector callsites. Reads `getAllowRules()`, calls `findMatchingAllowRule`, builds the payload via the existing `buildNumberedPayload`/`buildYesNoPayload` (skipping when the rule's `choice_index` is now out-of-range, e.g. Claude renamed/removed the choice since the rule was created), writes to the PTY, fire-and-forget POSTs the audit entry. Returns `{applied: boolean}` so the caller knows whether to mount the overlay or skip.
- `POST /api/terminal-prompt/audit` server route in `src/routes/commandLog.ts` — bounded (`SUMMARY_MAX_LEN=200`, `DETAIL_MAX_LEN=4000`, `PARSER_ID_MAX_LEN=64`), validates `parser_id` + `rule_id` are non-empty, writes a `terminal_prompt_auto_allow` event_type entry via `addLogEntry`. 5 server unit tests in `src/routes/commandLog.test.ts` (happy path + missing parser_id + missing rule_id + malformed JSON + truncation).
- Overlay extension in `terminalPromptOverlay.tsx`: optional `onAddAllowRule(choiceIndex, choiceLabel)` callback in the options, `renderAllowRuleCheckbox(opts)` renders the "Always allow this answer" checkbox row only when (a) the caller provided the callback AND (b) the match shape isn't `generic`. Numbered + yesno mounters call the callback before `send()` when the checkbox is checked. New SCSS block `.terminal-prompt-overlay-allow-rule-row`.

**Terminal prompt overlay Phase 4 (HS-7988):** Settings → Permissions sub-section + master detection toggle. Four threads:

- `state.tsx` adds `terminal_prompt_detection_enabled: boolean` to `AppSettings` (default `true`). Persists via the existing `/settings` PATCH path (DB-backed, string-serialised — `'true'` / `'false'`). `settingsLoader.tsx` parses on app boot.
- `pages.tsx` extends the existing `data-panel="permissions"` panel with a `.settings-section-divider`, a "Terminal prompts" header, the master-toggle checkbox `#settings-terminal-prompt-detection`, and the rule-list host `#terminal-prompt-allow-list`. New rules are added FROM THE OVERLAY (Phase 3) — the management UI is review-and-cleanup only.
- `terminalPromptAllowListUI.tsx` — `loadAndRenderTerminalPromptAllowList()` fetches rules via the Phase 3 store (`loadAllowRules`), subscribes to changes (`subscribeToAllowRules`) so the list re-renders when overlay clicks add new rules, and renders each rule as a `.permission-allow-row` (re-using §47.4's grid styling: parser_id / question_preview / "→ choice · date" / delete-button). Delete button calls `removeAllowRule(id)`. Empty state shows the "no rules configured" hint with the "click 'Always allow' on the overlay" CTA. When the master toggle is off, wraps the list in `.terminal-prompt-allow-rule-wrapper.is-disabled` (opacity 0.55 + `pointer-events: none`) so users see what's configured but can't interact. Lazy-imported from `settingsDialog.tsx` when the Permissions tab is first activated. **HS-8210 Phase D (§58.7)** — channel-keyed rules render `Channel: <match_channel>` (e.g. `Channel: server:hotsheet-channel`) in the question column instead of the question preview, so the user sees what the rule actually keys off. The inspector dialog also flips its label from `Question` → `Channel` and renders the bare channel value in the `<pre>` block.
- `settingsDialog.tsx` populates the master-toggle checkbox on dialog open + wires its `change` handler to PATCH `/settings` and re-render the rule list (so the disabled-state class flips). Both detector callsites (`terminal.tsx::kickPromptDetector` + `terminalTileGrid.tsx::enterDedicatedView`) AND-in `state.settings.terminal_prompt_detection_enabled` to their `isActive()` short-circuit so a flipped-off toggle prevents the parser registry from running entirely. 8 new unit tests in `allowRulesStore.test.ts` covering hydrate / pre-hydrate empty / append round-trip / preserve-prior / remove / unknown-id no-op / subscriber notify on hydrate+append+remove.

**Drawer-tab Tauri gating (HS-7977):** Plug for the gap left by HS-6437. Two paths used to spawn xterm + open PTY WebSockets in plain-browser mode: `commandLog.tsx::openPanel()` and `applyPerProjectDrawerState()` both call `loadAndRenderTerminalTabs()` unconditionally; if the saved `drawer_active_tab` was `terminal:<id>`, `switchDrawerTab()` would activate that pane. Now (1) `terminal.tsx::loadAndRenderTerminalTabs()` early-returns when `getTauriInvoke() === null`, and (2) `switchDrawerTab()` runs every requested tab through the new pure helper `resolveDrawerTabForTauri(tab, isTauri)` in `src/client/drawerTabGating.ts` which silently rewrites `terminal:*` → `commands-log` in a browser. 8 unit tests in `drawerTabGating.test.ts` (Tauri / browser × commands-log / configured / dynamic, plus negative tests for the prefix anchor).

**Reader mode overlay (HS-7957 design / HS-7961 impl):** New `src/client/readerOverlay.tsx` exports `openReaderOverlay({title, markdown})` — almost-full-viewport read-only markdown overlay (90vw / 90vh, max-width `min(960px, 90vw)`, z-index 2400) used by both the per-note book-icon button and the Details book-icon button. Three pure exports beside it: `buildNoteReaderTitle(createdAt)` ("Note from <local-time>" or "Note"), `buildDetailsReaderTitle(ticketNumber, ticketTitle)` ("Details for HS-X: title" / fallbacks), `syncDetailReaderButton()` (toggles `#detail-reader-btn` disabled state from textarea emptiness). Trigger wiring: `noteRenderer.tsx` adds a `.note-actions` cluster on every non-empty note's `.note-timestamp-row` containing the book button (left of the megaphone when shown); `routes/pages.tsx` converts the Details `<label>` to a `.detail-details-label` flex row with `#detail-reader-btn`; `app.tsx::bindDetailReaderButton` clicks → `openReaderOverlay({title, markdown: detailsArea.value})` (snapshot at click time so a mid-edit reader shows the working state). Disabled-state sync runs from `bindDetailAutoSave`'s input handler + `detail.tsx::loadDetail`/`loadPreviewDetail`. Dismiss: X / Escape (capture-phase) / backdrop click. SCSS: `.reader-mode-overlay`, `.reader-mode-dialog`, `.reader-mode-header`, `.reader-mode-body` (reuses `.note-markdown` for content), `.note-reader-btn`, `.note-actions`, `.detail-details-label`, `.detail-reader-btn`. 18 unit tests in `readerOverlay.test.ts`. New requirements doc `docs/49-reader-mode.md`.

**Drawer-tab drag-to-reorder (HS-7827):** Each `.drawer-terminal-tab` in `terminal.tsx` is `draggable="true"` with `dragstart` / `dragover` / `drop` handlers wired by `attachTabDragHandlers(tabBtn, terminalId)`. Module-level `tabDragFromId` tracks the dragged id; on drop, `reorderTabAfterDrop` re-appends tabs + matching panes in the new order (move-via-append, no flicker), then PATCHes `/file-settings` with the configured-id subset of the new strip order via the pure helpers in `src/client/terminalTabReorder.ts` (`reorderIds`, `configuredSubsetInStripOrder`, `reorderConfigsById` — 16 unit tests). Dynamic tabs can be dragged within a session for visual rearrangement but aren't persisted (per the ticket: configured terminals only). The dashboard (§25) iterates `/terminal/list` which reads from `listTerminalConfigs(dataDir)` so the new order propagates to the dashboard on its next list fetch automatically. SCSS: `.drawer-terminal-tab.dragging` (0.4 opacity) + `.drawer-terminal-tab.drag-over` (inset accent border on the leading edge).

**Search include archive + backlog rows (HS-7756):** When the user types a search query, gray "Include {N} backlog items" / "Include {N} archive items" rows appear under the multi-select toolbar (in `<div id="search-extra-rows">`) when the search has matches in those buckets. Click toggles the matching `include_backlog` / `include_archive` flag in `state` and reloads. New `countSearchMatchesInExcludedStatuses(search)` helper in `src/db/tickets.ts`; new `GET /api/tickets/search-counts` route returns `{backlog, archive}`. `GET /api/tickets` extended with `include_backlog` / `include_archive` query-string flags that OR-in those statuses to the WHERE clause's status filter. Client side: new `src/client/searchExtraRows.tsx` exposes `renderSearchExtraRows(reload)`, `clearSearchIncludeState()` (called by `loadTickets` whenever `state.search === ''` to reset flags + restore the saved layout), and `clearIncludeFlagsOnly()` (called by `bindLayoutToggle` when the user manually flips back to columns while includes are on — per the spec "restart the search"). View-mode auto-switch: on first include-toggle while in column view, save `state.viewModeBeforeSearchInclude = 'columns'` and force `'list'` (column view groups by status; mixing backlog/archive in wouldn't fit). 4 new unit tests in `src/db/queries.test.ts`. New requirements doc `docs/40-search-include-rows.md`.

**Note markdown — escape raw HTML (HS-7855, HS-7857):** `src/client/markdownSetup.ts` configures the `marked` singleton once with `setOptions({ breaks: true })` plus a custom `html` renderer that escapes the tag's text rather than passing it through. Imported (for side effects) by every consumer of `marked` — `noteRenderer.tsx`, `detail.tsx`, `feedbackParser.ts`. Without this, completion notes mentioning tag-like fragments inline (`<span class=hide-btn-badge>`, `<input type=text>`) used to render as actual HTML — HS-7857 surfaced as an absolutely-positioned badge `<span>` capturing every following character and bleeding out of the notes area to the top of the window; HS-7855 surfaced as live `<input>` controls inside note text. Markdown-native syntax (links, code, bold, lists, tables, images) still renders normally because none of those go through the `html` renderer. 5 unit tests in `markdownSetup.test.ts`.

**Context-menu icons + toolbar polish (HS-7832, HS-7833, HS-7834, HS-7835):** HS-7832 removed the leading `scaling` icon from the dashboard + drawer-grid sizer slider (the range thumb is enough). HS-7833 moved the dashboard layout-toggle from its original position between the dashboard toggle and the slider to between the slider and the eye icon (the user's preferred ordering). HS-7834 reordered the dashboard tile context menu so "Hide in Dashboard" sits right under "Close Tab" (now renamed "Close Terminal" since the dashboard renders tiles, not tabs). HS-7835 added Lucide icons to every context menu in the app — new icon constants in `src/client/icons.ts` (`ICON_X`, `ICON_X_CIRCLE`, `ICON_CLOSE_LEFT`, `ICON_CLOSE_RIGHT`, `ICON_CLOSE_OTHERS`, `ICON_SETTINGS`, `ICON_POWER`, `ICON_GLOBE`, `ICON_UNINSTALL`, `ICON_STAR`, `ICON_STAR_FILLED`, `ICON_EXTERNAL_LINK`) wired into `terminal.tsx` (drawer tab strip), `terminalDashboard.tsx` (dashboard tile menu), `drawerTerminalGrid.tsx` (drawer-grid tile menu), `noteRenderer.tsx` (delete note + delete draft), `pluginSettings.tsx` (Configure / Enable / Disable / Enable on All / Disable on All / Uninstall / Show in Finder), `commandLog.tsx` (Copy), `hideTerminalDialog.tsx` (Rename / Delete grouping), `projectTabs.tsx` (Close Tab / Others / Left / Right), `contextMenu.tsx` (Up Next now uses `ICON_STAR_FILLED` when active and `ICON_STAR` when not — replaces the unicode `★`/`☆` prefix). All entries flow through the established `<span class="dropdown-icon">` + `<span class="context-menu-label">` markup the main context menu already used.

**PTY-activity busy detection (HS-6702):** Server-side `lastOutputAtMs` + `lastSpinnerAtMs` per `SessionState` (in `src/terminals/registry.ts`). Spinner detection via `containsClaudeSpinner(text)` pure helper in `src/terminals/claudeSpinner.ts` — checks for the six Claude busy-spinner glyphs (`· ✢ ✳ ✶ ✻ ✽` from the HS-6702 ticket note). Both timestamps reset on PTY restart. `/api/terminal/list` annotates each entry with both fields. Client side: `src/client/channelUI.tsx` runs a 2-second poll while `isChannelBusy()` is true, computes the most recent `lastSpinnerAtMs` across all active-project terminals, feeds it to `shouldShowDegradedBusy(channelBusy, lastSpinnerAtMs, now)` (pure helper) — when true the indicator renders a "Claude idle (channel busy)" label with paused spinner + muted colour. New SCSS rule `.channel-status-indicator.busy.degraded`. 11 unit tests in `claudeSpinner.test.ts`.

**Visibility groupings cross-project fan-out (HS-7826 follow-up):** The Show / Hide Terminals dialog used to write every grouping mutation + visibility toggle against `dialogScope` (= the first project's secret) — fine in single-project mode, wrong in global mode where the dashboard reads each project's OWN active grouping. Symptom: dialog said "hidden" for a Hot Sheet > Claude row but the dashboard kept showing the tile because the toggle landed in Small Tale's state instead. Fix: new `dialogScopes(opts)` collects every secret in the dialog, and grouping CRUD (add / rename / delete / reorder / activate) fans out across every scope. New `addGroupingWithId(state, id, name)` pure helper + `addGroupingForProjectWithId(secret, id, name)` + `generateGroupingIdAcrossProjects(secrets)` mint a single id that's safe in every per-project state, so the same grouping lands under the same id everywhere. Visibility row toggles now use `group.secret` (the terminal's own project) instead of `dialogScope`. The dashboard's `<select>` wiring picks up the new `getAdditionalSecrets` callback in `GroupingSelectOptions` so picking a tab in the dropdown also fans out across every section. Drawer-grid (`'single-project'` mode) is unchanged — `dialogScopes` returns one entry. 8 new unit tests across `visibilityGroupings.test.ts` + `dashboardHiddenTerminals.test.ts`. New §39.7 in `docs/39-visibility-groupings.md`.

**Visibility groupings (HS-7826):** Named visibility configurations per project — each grouping carries its own `hiddenIds`. Pure helpers in `src/client/visibilityGroupings.ts` (`addGrouping`, `renameGrouping`, `deleteGrouping`, `reorderGroupings`, `setActiveGroupingId`, `toggleHiddenInGrouping`, `parsePersistedState`, `pruneStaleIdsInGroupings`) drive every state transition; 42 unit tests in `visibilityGroupings.test.ts`. State lives in `src/client/dashboardHiddenTerminals.ts` (refactored from the pre-HS-7826 flat `Map<secret, Set>`). Pre-HS-7826 public API (`isTerminalHidden`, `setTerminalHidden`, `filterVisible`, etc.) all delegate to the active grouping. Persistence in `persistedHiddenTerminals.ts` writes new keys `visibility_groupings` + `active_visibility_grouping_id` plus a legacy `hidden_terminals` mirror for back-compat. Migration on first read: when only `hidden_terminals` is present, `parsePersistedState` synthesises a Default grouping seeded with the legacy ids. `prunedVisibilityGroupings` in `src/file-settings.ts` is the parallel of `prunedHiddenTerminals` for the new shape (called from `/file-settings` PATCH handler when terminals[] changes). Dialog UI in `hideTerminalDialog.tsx` gains a tab bar with `+` button, right-click rename/delete context menu, drag-to-reorder. New helper `src/client/visibilityGroupingSelect.ts` (`refreshGroupingSelect`, `wireGroupingSelectChange`) drives the `<select>` dropdowns next to the eye icons (dashboard + drawer-grid), shown only when groupings.length > 1. New requirements doc `docs/39-visibility-groupings.md`.

**Persisted hidden terminals (HS-7825):** New per-project file-settings key `hidden_terminals: string[]` (added to `JSON_VALUE_KEYS` in `src/file-settings.ts`) survives reload + relaunch. New module `src/client/persistedHiddenTerminals.ts` owns hydration on app boot + debounced PATCH per-project on every `subscribeToHiddenChanges` fire. New helpers in `src/client/dashboardHiddenTerminals.ts`: `isConfiguredTerminalId(id)` (returns `!id.startsWith('dyn-')`) and `hydratePersistedHiddenForProject(secret, ids)` (seeds in-memory map, structurally equal to dodge no-op notify). Dynamic terminals (`dyn-*`) intentionally session-only — filtered out at both read and write paths. `initPersistedHiddenTerminals(projects)` is called from `initProjectTabs` + `refreshProjectTabs` in `src/client/projectTabs.tsx`. New requirements doc `docs/38-terminal-visibility.md`. 8 unit tests in `persistedHiddenTerminals.test.ts` + 4 in `dashboardHiddenTerminals.test.ts` for the hydrate path.

**Stale hidden_terminals cleanup on terminal deletion (HS-7829):** New pure helper `prunedHiddenTerminals(currentHidden, configuredIds)` in `src/file-settings.ts` returns a filtered array (or `null` for no-op) when one or more hidden ids no longer exist in `terminals[]`. The `/file-settings` PATCH handler in `src/routes/settings.ts` calls it whenever the patch contains a `terminals` key (alongside the existing eager-spawn step) and writes the pruned list when needed. Tolerates legacy stringified-JSON shape and skips malformed input. 8 unit tests in `file-settings.test.ts`.

**Reset visibility from Settings (HS-7830):** New "Reset visibility" button in Settings → Terminal (below the Default terminals list) clears the project's persisted hidden state without opening the Show / Hide Terminals dialog. Disabled when nothing is hidden; status hint above shows the live count. Click confirms via `confirmDialog` then calls `unhideAllInProject(secret)` — the existing `persistedHiddenTerminals.ts` subscription PATCHes `hidden_terminals: []` automatically. New module `src/client/hiddenTerminalsResetUI.tsx` exposes `loadAndWireHiddenTerminalsReset()`, called from `settingsDialog.tsx`'s on-open handler.

**Hidden-count badge prune-on-list (HS-8016):** Pre-fix, the §25 / §36 eye-icon hidden-count badge kept counting terminal ids that no longer existed — closing a hidden terminal from the drawer X-button or deleting one in Settings → Terminal never reconciled the per-project `hiddenIds` array, so the badge (and the persisted `hidden_terminals` setting) carried the stale id forward across reloads. New `pruneHiddenForProject(secret, knownIds)` in `src/client/dashboardHiddenTerminals.ts` walks every grouping in the project's `ProjectVisibilityState` and drops any id that isn't in the live `knownIds` set; notifies subscribers exactly once when the diff is non-empty, so the badge re-paints + `persistedHiddenTerminals.ts` PATCHes the new shape in the same pass. Wired into the two surfaces that own a fresh `/terminal/list` payload: `terminal.tsx::loadAndRenderTerminalTabs` (active project — fires after every drawer-tab close + every Settings save) and `terminalDashboard.tsx::fetchProjectSections` (all-projects fan-out — fires every dashboard open / refresh). 6 new unit tests in `dashboardHiddenTerminals.test.ts` (drops missing ids / single notify / no-fire-when-unchanged / no-state-no-op / non-active-grouping prune / cross-project zero count).

**Hide-button hidden-count badge (HS-7823):** Added a `.hide-btn-badge` (small accent-colored numeric pill, top-right of the eye icon, `99+` cap) to both the Terminal Dashboard's hide button (`#terminal-dashboard-hide-btn`, scope = all projects) and the drawer-grid's hide button (`#drawer-grid-hide-btn`, scope = active project). New helpers in `src/client/dashboardHiddenTerminals.ts`: `countHiddenAcrossAllProjects()`, `countHiddenForProject(secret)`, and `applyHideButtonBadge(button, count)` (idempotent — removes badge when count hits 0). Subscribed to via the existing `subscribeToHiddenChanges` so the badge updates in lockstep with the dashboard / drawer-grid repaint; drawer-grid additionally calls `refreshHideBtnBadge()` from `showGridChrome()` so a project switch reflects the right count. 7 unit tests in `dashboardHiddenTerminals.test.ts`.

**Flow-layout colored-dot removal (HS-7824):** Removed the per-tile colored project-badge dots that flow-layout mode rendered on every tile (kept the `{ProjectName} ›` prefix on the first tile of each project's run — that's what actually clarified grouping). `TileEntry.projectBadge` simplified from `{ color, name? }` to `{ name? }`; `terminalTileGrid.tsx`'s `renderTile` no longer emits the `.terminal-dashboard-tile-badge` span; `paintFlowLayout` in `terminalDashboard.tsx` no longer sets `projectBadge` on subsequent tiles in a project run. Removed the `projectBadgeColor()` helper + the `.terminal-dashboard-tile-badge` SCSS block. e2e regression test in `terminal-dashboard-flow-layout.spec.ts` flipped from `.terminal-dashboard-tile-badge` `toHaveCount(3)` to `toHaveCount(0)`.

**Auto-show feedback draft on relaunch (HS-7822):** Pre-fix, `detail.tsx`'s auto-show fired `showFeedbackDialog` synchronously while the `/feedback-drafts` fetch was fire-and-forget — so on relaunch the user always saw the bare original prompt, never their saved draft. Fix: new pure helper `pickDraftForFeedbackNote(drafts, activeFeedbackNoteId)` in `feedbackDialog.tsx` (parent-match wins → free-floating fallback → null) + reworked the auto-show in `detail.tsx` to await the drafts fetch before deciding which form to show. The `shouldAutoShowFeedback` gate is checked inside the `.then` so a fetch failure doesn't leak the gate. 7 unit tests in `feedbackDialog.test.ts` + 1 new e2e in `feedback-drafts.spec.ts` covering the relaunch flow.

**Edit Terminal command combobox (HS-7791):** The Settings → Terminal Edit Terminal dialog's Command field is now a combobox: a free-text `<input>` paired with a `<datalist>` populated from `GET /api/terminal/command-suggestions` (in `src/routes/terminal.ts`). Server helper `collectCommandSuggestions()` builds an ordered, deduplicated list of `{{claudeCommand}}` first, then `process.env.SHELL` / `%COMSPEC%`, then any additional shells discovered via `/etc/shells` on Unix or well-known PowerShell + cmd locations on Windows (only entries that actually exist on disk are surfaced). Client caches the response in `terminalsSettings.tsx`'s `loadCommandSuggestions()` for the session; populates the datalist after the dialog mounts (Safari is finicky about detached datalists). 5 unit tests in `terminalCommandSuggestions.test.ts`.

**Quit-confirm dialog labels (HS-7789):** `/api/projects/quit-summary` now resolves dynamic-terminal labels via the new `listDynamicTerminalConfigs(secret)` export from `src/routes/terminal.ts` in addition to `listTerminalConfigs` so `dyn-…` ids appear in the dialog under their friendly names instead of the raw id.

**Foreground-process basename normalization (HS-7790):** `parsePsOutput` in `src/terminals/processInspect.ts` now applies a new `normalizeComm()` helper that strips directory prefix, leading login-shell dash, and `.exe` suffix from each `comm` value. macOS `ps -o comm` emits the executable's full path (e.g. `/bin/zsh`), and login shells additionally get a leading dash (`-zsh`) — without normalization the SHELL_BASENAMES + exempt-list comparisons silently failed and the user's exempt-list configuration was ignored.

**Tile xterm scrollback (HS-7990):** The shared `mountTileXterm` in `terminalTileGrid.tsx` was constructing the per-tile xterm with `scrollback: 0`, so mouse-wheel scrolling did nothing in either the grid view or the centered (FLIP) overlay — only the dedicated view's separately-constructed xterm (10_000 lines) supported scrolling. Bumped to `scrollback: 1000` so users can mouse-wheel through the back-buffer when a tile is magnified. Memory is bounded by actual output (xterm allocates lazily), worst case ~5 MB per tile at 160 cols × 1000 lines × 32 bytes/cell, and the HS-7968 virtualization disposes off-screen tiles so only the visible subset holds scrollback.

**Tile virtualization (HS-7968):** `IntersectionObserver`-driven lazy mount/dispose of xterm + WebSocket per tile in the shared `terminalTileGrid.tsx` module (used by both the dashboard §25 and the drawer-grid §36). Pure state machine in `src/client/terminalTileVirtualization.ts` exposes `initialTileState`, `onTileEnter`, `onTileExit`, `onDisposeTimerFired` — each takes state + event and returns `{next, actions}` (mount / dispose / scheduleDispose / cancelDispose). 12 unit tests in `terminalTileVirtualization.test.ts` cover the lifecycle + the quick-scroll-no-churn case + the long-off-screen dispose-then-remount case.

Wiring in `terminalTileGrid.tsx::mountTileGrid`: one `IntersectionObserver` per grid (200 px `rootMargin` so mount fires slightly before the user reaches the tile), one `Map<tileId, TileVirtualState>` + one `Map<tileId, timerId>` for pending disposes, one `Map<Element, tileId>` for the observer-target → tile-id lookup. Eager-mount in `createTile` REPLACED with `virtObserver.observe(root)`; the observer's intersection callback runs `onTileEnter` (mount-if-not-mounted only when `tile.state === 'alive'`) or `onTileExit` (schedules an 8 s dispose timer via `VIRT_DEFAULT_DEBOUNCE_MS`). Re-entering before the timer fires triggers `cancelDispose` so quick scrolls don't churn the renderer. New `softDisposeTile(tile)` helper recycles xterm renderer + WebSocket but leaves the PTY + scrollback alive server-side; restores the `renderPreviewContent` placeholder so the off-screen-then-back-on tile doesn't briefly show an empty white box during the re-mount window. New `forgetVirtualization(tile)` helper unhooks the observer + clears any pending timer; called from `disposeTile` on full teardown so the registry stays clean. New `ensureTileMounted(tile)` defends the click-before-IO race in `centerTile` — if the user clicks a freshly-rendered alive tile before the IntersectionObserver has fired (microsecond window), force-mount before the centering animation runs. `spawnAndEnlarge` similarly flags the new tile as mounted in the virtualization state so an immediate viewport-exit triggers the dispose-debounce flow.

When `IntersectionObserver` is undefined (some test envs without a polyfill), the grid falls back to the pre-virtualization eager-mount behaviour so unit tests don't have to install a polyfill. `teardownAll` clears every pending dispose timer + disconnects the observer. Bell indicators + status dots stay correct because both read from `/api/terminal/list` polling, not from xterm — independent of mount state. Pushes the practical client-side terminal-count ceiling from "total terminals in the project" to "terminals visible on screen at once" (typically 20–50 in dashboard view, 8–20 in drawer-grid). See `docs/25-terminal-dashboard.md` §25.9.1 + `docs/36-drawer-terminal-grid.md` §36.10.5.

**Quit-confirm row preview (HS-7969):** Each `.quit-confirm-row` in `quitConfirm.tsx::showQuitConfirmDialog` is now a clickable `<button>` that toggles an inline `.quit-confirm-row-preview` `<pre>` showing the last ~30 ANSI-stripped lines of that terminal's scrollback. Preview text fetched lazily via `GET /api/terminal/scrollback-preview?terminalId=<id>&maxLines=<n>` (new route in `src/routes/terminal.ts`, capped at 200 lines server-side, default 30) backed by `getTerminalScrollbackPreview(secret, terminalId, maxLines)` in `src/terminals/registry.ts`. Pure helpers in `src/terminals/scrollbackSnapshot.ts`: `stripAnsi(input)` (handles CSI / OSC-BEL / OSC-ST / SIMPLE-ESC / backspace / bare-CR collapse), `tailLines(text, max)` (drops single trailing newline before slicing), `buildScrollbackPreview(buf, max)` composes them. Cached per-row after the first fetch via `previewEl.dataset.state`. New SCSS `.quit-confirm-row-container` (wrapper) + `.quit-confirm-row` button reset + `.is-expanded .quit-confirm-row-chevron` rotate-90 + `.quit-confirm-row-preview` 280px-max scrolling pre. 16 new unit tests in `scrollbackSnapshot.test.ts`.

**Quit-confirm flow (HS-7591 spec / HS-7596 impl):** `src/terminals/processInspect.ts` exposes pure helpers (`parsePsOutput`, `descendantChain`, `pickForegroundProcess`, `inspectForegroundProcess`) that walk the OS process tree from a PTY's pid via `ps -o pid,ppid,comm -A` (Unix; Windows safe-defaults to "prompt"), pick the foreground child via the §37.3 one-level-deeper rule, and check the user's exempt list (default macOS Terminal.app's `{screen, tmux, less, more, view, mandoc, tail, log, top, htop}`). `SHELL_BASENAMES` enumerates the major shells (`bash`, `zsh`, `fish`, `sh`, `dash`, `ash`, `ksh`, `tcsh`, `pwsh`, `powershell`, `cmd`, `cmd.exe`). Routes: GET /api/terminal/foreground-process?terminalId= (per-terminal) and GET /api/projects/quit-summary (cross-project aggregator in `src/routes/projects.ts`). Registry helpers added: `getTerminalPid(secret, terminalId)` + `listAliveTerminalsAcrossProjects()`. Two new file-settings keys per project: `confirm_quit_with_running_terminals: 'always' | 'never' | 'with-non-exempt-processes'` (default `'with-non-exempt-processes'`) + `quit_confirm_exempt_processes: string[]` (added to `JSON_VALUE_KEYS` so file-settings stores it as native JSON). Tauri side: `QuitConfirmed: AtomicBool` + three converging gates in `src-tauri/src/lib.rs` — `on_window_event` `WindowEvent::CloseRequested` (red traffic-light + Alt+F4), a custom `app-quit` `MenuItemBuilder` with `CmdOrCtrl+Q` accelerator that replaces the predefined `.quit()` so ⌘Q routes through `on_menu_event` (the predefined item maps to `NSApp::terminate:` on macOS and bypassed `RunEvent::ExitRequested`), and `RunEvent::ExitRequested` as the safety net for programmatic `app.exit()` + the macOS dock-menu Quit. All three emit the same `quit-confirm-requested` event. New `confirm_quit` Tauri command flips the flag + calls `app.exit(0)` so every quit trigger converges on a single exit path. Client side: `src/client/quitConfirm.tsx` exposes `evaluateQuitDecision` (pure §37.5 logic, 9 unit tests) + `runQuitConfirmFlow` (fetches /api/projects/quit-summary + shows dialog) + `initQuitConfirm` (Tauri event listener — `getTauriEventListener` helper in `tauriIntegration.tsx` wraps `window.__TAURI__.event.listen`). Settings UI: `src/client/quitConfirmSettingsUI.tsx` populates the radio + textarea + Reset link in Settings → Terminal "Quit confirmation". CLI: `src/cli.ts` `--close` extended with `confirmCloseAgainstQuitSummary` step + new `--force` flag for non-interactive use. "Don't ask again" checkbox in the dialog flips every project's setting to `'never'`. 22 unit tests in `processInspect.test.ts` + 9 in `quitConfirm.test.ts`. See docs/37-quit-confirm.md.

**Tile-grid sizing convergence (HS-8051, 2026-05-01, four-iteration history — final fix in `applyResizeIfChanged`):** `attachScreenObserver(tile)` in `terminalTileGrid.tsx` was called immediately after `mountTileViaCheckout`, but `.xterm-screen` is created lazily by xterm on its first render. First fix added an rAF-retry-until-screen-exists loop. Second fix added a `scheduleResyncRetry(tile, budget=30)` chained-rAF loop because the larger-font Domotion tile still got stuck at the initial 80 × 60 (user's third "Not working" log: `screenW: 1692, screenH: 1200`). Both prior fixes failed in the same way — the chained retry oscillated because it re-derived `cellW = screen.offsetWidth / term.cols` on every tick, but `term.cols` updates synchronously on `term.resize` while screen dims only update on the NEXT paint, so between paints the ratio gives a wrong cellW, the algorithm produces a wrong target, the next resize uses that wrong target, and the loop never lands on (61, 48). User's fourth "Not working" log: `screenW: 841, screenH: 1200` (cols=40 — non-converging). **HS-8051 follow-up #2 (2026-05-01):** replaced the chained-rAF retry with a **`term.onRender` listener** wired in `mountTileViaCheckout`. `onRender` fires AFTER xterm commits a paint to DOM, so `term.cols` and `screen.offsetWidth` are guaranteed consistent at that moment — the only signal that doesn't race a pending paint. The handler `handleTileRender(tile)` caches cellW/cellH on `tile.cachedCellW/H`, re-applies the CSS scale via `reapplyTileScaleFromPreview`, and (when top-of-stack) issues at most one resize per render to converge on the cell-metric-derived 4:3 target. Convergence cycle: render 1 measures cellW + issues `checkout.resize(native)`; render 2 (caused by render 1's resize) sees `term.cols === native.cols` and short-circuits; subsequent renders are no-ops. Two iterations, deterministic. The `attachScreenObserver` ResizeObserver simplifies to JUST re-applying the visual CSS scale — convergence on tile-native cols × rows is owned entirely by `term.onRender`. **HS-8051 final fix (2026-05-01, fourth iteration after user's "still broken" feedback):** root cause was actually in `terminalCheckout::applyResizeIfChanged`, NOT the tile's convergence loop. The function compared the requested `(cols, rows)` against `entry.lastAppliedCols/Rows` — a separate bookkeeping number — to decide whether to skip `term.resize` + WS-send. But the history-frame handler in `attachWebSocketToEntry` calls `entry.term.resize(...)` directly without updating `lastApplied` (deliberately, per HS-8042's design — so consumers' later resize calls aren't skipped after replay). When a tile's `term.onRender` loop converged to native dims (lastApplied = (61, 48)) and the WS history-frame replay then mutated term to capture-time dims (term.cols = 80) without touching lastApplied, the next consumer-driven `handle.resize(61, 48)` saw lastApplied = (61, 48), bailed, and term was stuck at the wrong size. The user's question — "are we not able to detect if/when resizing was actually applied?" — was the diagnostic key: the source of truth IS `entry.term.cols/rows` (xterm's actual internal state), not a separate bookkeeping that can drift. Fix: rewrote `applyResizeIfChanged` so the term-resize gate compares against `entry.term.cols/rows` (asymmetric with the WS-send gate which still uses `lastApplied` since it correctly tracks server PTY state). Two new regression unit tests in `terminalCheckout.test.ts` pin the divergence-skip behaviour. New e2e test in `e2e/dashboard-tile-divergence-resize.spec.ts` opens the dashboard with three terminals (one with `fontSize: 22` for the larger-cell-metric regime where the user's Domotion bug manifested), bakes the config in BEFORE `page.goto('/')` (vs the existing `terminal-dashboard-tile-rendering.spec.ts` HS-8051 follow-up #2 case which reconfigures at runtime, exposing a separate spawn-on-late-config flake unrelated to this fix), waits for every tile to render its first `TOP-STATUS-BAR` row, asserts every tile's natural aspect is within 5 % of 4:3. Old helpers `tileNativeDimsFromXterm`, `resyncTilePtyFromCellMetrics`, and `scheduleResyncRetry` deleted. Disposer for the onRender listener captured into `tile.termHandlerDisposers` so a soft-dispose / release cleans up properly. New e2e test in `e2e/terminal-dashboard-tile-rendering.spec.ts` ("every tile converges to ≈4:3 natural aspect even with mixed per-terminal font sizes (HS-8051 follow-up #2)") configures 5 terminals with one explicit `fontSize: 22` (mirrors user's Domotion repro), opens the dashboard, asserts every tile's `.xterm-screen` natural aspect is within 5 % of 4:3 — a regression in the convergence path (returning to chained-rAF, removing the onRender listener, breaking cellW caching) shows up here. **HS-8028 follow-up #2 (2026-05-01):** while in `terminalTileGrid.tsx`, fixed the magnified-grid-nav (Shift+Cmd+Arrow) "doesn't work at all anymore" regression — `findNextTileInDirection` was using `tile.root.getBoundingClientRect()` for the `from` reference, but a centered tile's root is at `position: absolute` floating in the middle of the viewport (a big rect spanning ~80 % of the screen). Comparing other grid-position tiles against THAT rect made every candidate fail the strict-half-plane gate that `pickGridNeighbourIndex` uses. Fix: use `tile.slotPlaceholder.getBoundingClientRect()` when the placeholder exists (= centered mode); the placeholder is a ghost div sitting in the original grid cell, so its rect reflects the tile's natural grid position. Dedicated mode unchanged — the dedicated overlay doesn't reparent the tile root. New e2e ("Shift+Cmd+Right (magnified-nav) from centered tile moves to next tile (HS-8028 follow-up #2)") configures two tiles, centers the leftmost, presses Shift+Cmd+Right, asserts the centered class moved. **HS-7969 follow-up #2 (2026-05-01):** while in `quitConfirm.tsx`, fixed the preview pane's xterm escaping the pane's bounds — added a `ResizeObserver` on the preview pane that re-runs `fit.fit() + handle.resize(...)` on every pane size change. The single rAF-after-checkout fit caught the FIRST layout pass but the dialog goes through a SECOND pass when the master-list flexbox children settle their intrinsic widths; the original fit-once code missed that second pass. Mirror of the dedicated-view pattern in §54.7. Observer disconnected on dialog close. **HS-8055 (2026-05-01):** the HS-7969 follow-up #2 ResizeObserver had a feedback loop — every fire called `fit.fit()` + `handle.resize(...)` unconditionally, mutating xterm's internal DOM (canvas + accessibility rows + helper textarea), the resulting layout pass ticked the observer again, and so on. Visible only in DevTools (the dialog body looked stable but `document.body.scrollHeight` + xterm-helper-textarea `top` grew unbounded over time per the user's Inspector screenshot showing `top: 4096px`). Fix shorts the callback when `proposeDimensions()` matches the term's current cols/rows — pane geometry hasn't changed in a way that would change the fit output, so re-running fit is wasted work that feeds the loop. Added a `pendingFit` rAF guard so same-frame fires coalesce to one tick. Belt-and-braces: `.quit-confirm-detail-preview` SCSS switched from `overflow: auto` → `overflow: hidden` since the xterm has its own `.xterm-viewport` scrollback scrolling — an outer scrollbar was a no-value fallback that toggled on sub-pixel layout shifts and fed the loop. New e2e regression test in `e2e/quit-confirm-dialog-growth.spec.ts` opens the quit dialog via a Tauri-event stub (`__TAURI__.event.listen` records the listener; the test fires `quit-confirm-requested` from `page.evaluate`), waits for the preview xterm to render, snapshots `bodyScrollHeight` + max absolute `top` value + descendant count under the overlay, waits 2 s, snapshots again, asserts no monotonic growth (>100 px is the regression). **HS-8055 follow-up (2026-05-01):** the v1 fix wasn't enough — the `proposeDimensions` gate never settled because the pane was actually expanding on every tick. Root cause: `.quit-confirm-master-detail` is a CSS grid (`minmax(220px, 320px) 1fr`) and the `1fr` track for `.quit-confirm-detail` has implicit `min-width: auto`. The xterm's absolutely-positioned `.xterm-screen` element has width = `cols * cellWidth`, and when `cols` grew by one tick it pushed the grid track wider via min-content sizing — `proposeDimensions` then read the wider parent and proposed even larger cols on the next tick. User's Inspector screenshot caught `.xterm-screen` at 6745 px wide inside a ~400 px pane (16× ratio). Fix: added `min-width: 0` + `overflow: hidden` to `.quit-confirm-detail` so the grid track is structurally pinned at its 1fr allocation. Same belt-and-braces `min-width: 0` on `.quit-confirm-detail-preview`. E2e test rewritten to (a) configure 3 PTYs so row-switching is exercised, (b) sample `.xterm-screen.clientWidth` + pane `clientWidth` over time across idle waits AND active row-switch cycles, (c) assert `.xterm-screen` width never climbs by >50 px and the `xtermScreenWidth / previewClientWidth` ratio stays < 2 (pre-fix the ratio climbed past 16×). Sanity-checked by reverting just the SCSS fix — the test then catches `.xterm-screen` growing by 2179 px during idle (the v1 JS gate alone is insufficient).

**Shell custom-command-button spinner + stop (HS-8060 Phase 1, 2026-05-01):** implements `docs/57-shell-command-button-spinner.md`. `src/client/commandSidebar.tsx::renderButton` learns a `is-running` branch keyed on `_runningButtonsForTesting.has(commandKey(cmd))`; new `commandKey(cmd)` (`target::name::prompt`) is the stable lookup key (CustomCommand has no id field). Running buttons get a 14×14 px `<span class="channel-command-btn-spinner">` appended (`background: inherit`, `position: absolute; right: 6px`) so the spinner punches the button colour and doesn't reflow against long labels — per the user's no-reflow directive. Click-while-running calls `confirmDialog` from `src/client/confirm.tsx` (Tauri-safe per CLAUDE.md) with `{title: 'Stop running command?', confirmLabel: 'Stop', cancelLabel: 'Keep running', danger: true}`; on Stop fires `POST /api/shell/kill` with the running id; on Cancel no-op. The pre-fix single-id-per-timer `startShellPoll(id)` was rebuilt as a parameterless poll that watches every entry in the `runningButtons: Map<commandKey, logId>` map and drops completed entries on each tick; replaces the global `shellAutoShowLog: boolean` with a per-id `autoShowLogById` map so concurrent commands' auto-show flags don't clobber each other. The existing global `setShellBusy()` indicator now reflects `runningButtons.size > 0` (busy = any button running). `runShellCommand` now takes a full `CustomCommand` (was: `command: string, name?: string`) so `commandKey(cmd)` can be derived. SCSS `.channel-command-btn` got `position: relative` (was unset) so the absolute-positioned spinner anchors to the button; new `@keyframes spin` reused (already defined in `styles.scss:2470`). 11 new unit tests in `commandSidebar.test.ts` cover: `commandKey` invariants × 3 (same `(target,name,prompt)` produces same key, different target → different key, undefined target normalises to `'claude'`), idle-button-no-spinner, running-button-has-spinner-with-ring-and-stop-glyph, click-running-opens-confirm-fires-kill-with-id, click-running-cancel-no-kill-no-state-change, two-concurrent-buttons-both-show-spinner, one-running-one-idle-only-running-shows-spinner, defensive-claude-target-never-spinners-even-if-key-in-map.

**Ticket-prefix cache invalidation on project switch (HS-8053, 2026-05-01):** `cachedPrefixes` in `src/client/ticketRefs.ts` was populated once at app boot from whichever project the user landed on (`loadTicketPrefixes()` cached for the page's lifetime), and never invalidated. So when the user switched to a project with a different `ticketPrefix` setting (Domotion's `DM`), the cache still held the previous project's prefixes (just `['HS']`) and `DM-123` references stayed plain text. New `reloadTicketPrefixes()` export clears the cache synchronously then runs `loadTicketPrefixes()` to repopulate; wired into `app.tsx::reloadAppState` alongside the existing `loadAllowRules()` / `loadCategories()` per-project resets. Synchronous clear is important — any concurrent `linkifyWithCachedPrefixes(...)` call between the clear and the resolve safely sees `null` and returns the input unchanged (vs. linking against the stale set). 4 new unit tests in `ticketRefs.test.ts` under `reloadTicketPrefixes (HS-8053)` cover: re-fetch-with-populated-cache, mid-flight-cache-is-null, network-error-fallback, round-trip-across-multiple-projects.

**Always-choose-this cross-project secret routing (HS-8057, 2026-05-01):** `appendAllowRule(rule)` in `src/client/terminalPrompt/allowRulesStore.ts` was unconditionally calling `api('/file-settings', ...)`, which routes through the active project's secret. Cross-project prompts (HS-8047) come from non-active projects — the user's screenshot showed a Hot Sheet project parked at the dev-channels WARNING while the user was active on a different tab — so the rule was being persisted into the wrong project's `<dataDir>/settings.json` and the server-side scanner gate at `src/terminals/registry.ts::findMatchingRuleForProject` (which reads the originating project's settings via its `dataDir`) never found a match. The "Always choose this" checkbox effectively did nothing for cross-project prompts; the WARNING re-surfaced on every restart. Fix: `appendAllowRule(rule, secret?)` accepts an optional originating-project secret and routes through `apiWithSecret('/file-settings', secret, ...)` when provided, skipping the global in-memory cache update (the cache only tracks the active project so the Settings UI for the originating project re-hydrates from `loadAllowRules()` on next open). `removeAllowRule(id, secret?)` got the same secret parameter for symmetry. `bellPoll.tsx::openCrossProjectOverlay::onAddAllowRule` now forwards the per-prompt `secret` (which the dispatcher already had — it's the project the long-poll tick reported the prompt from). 4 new unit tests: 3 in `allowRulesStore.test.ts` covering cross-project routing + cache-skip + back-compat (omitted secret keeps the active-project path), 1 in `bellPoll.test.ts` covering the integration (clicking "Always choose this" + a choice on a cross-project overlay produces fetch calls to `/api/file-settings` carrying the originating project's `X-Hotsheet-Secret` header).

**History-replay scrollback-resize bug (HS-8064, 2026-05-01):** `terminalCheckout.tsx`'s server-side `'history'` control message handler resized the term to capture-time dims so xterm wrapped the bytes correctly, but pre-fix never restored the term to the consumer's intended dims afterwards. The original HS-8042 design assumed the consumer's `term.onResize → handle.resize` echo would bounce the term back automatically — but xterm's `onResize` fires with the SYNTHETIC capture-time dims, not the consumer's intended dims, so the echo just acknowledged the capture-time resize and never restored the pane size. Result: term stuck at capture-time dims (e.g. 80×24 from a server-side scrollback snapshot) inside a drawer pane that fit (100×30), the visible scrollback never reflowed to fit, leaving a band of empty pane outside xterm's canvas until the user manually dragged the drawer to re-trigger fit. The user reported "scrollback isnt resizing". Fix: extracted the history-replay logic into a named `applyHistoryReplay(entry, msg)` helper that captures `lastAppliedCols/Rows` BEFORE the synthetic capture-time `term.resize`, writes the bytes, then re-applies the snapshot dims so xterm reflows to the consumer's pane width. The synthetic `term.onResize` echo from the second resize also brings `lastApplied` and the server PTY back to the consumer's dims via `applyResizeIfChanged`'s asymmetric gate. 4 new unit tests in `terminalCheckout.test.ts` under `applyHistoryReplay — restores consumer dims after replay (HS-8064)` cover: writes-bytes-then-snaps-back-to-consumer-dims, no-ops-on-malformed-history-no-bytes, skips-synthetic-resize-when-msg-cols-rows-missing-or-zero, and the timing-contract pin that the snapshot is captured BEFORE the synthetic resize so the onResize echo cannot poison it (asserts term + lastApplied both end up at consumer dims, NOT at capture-time dims). New test-only export `_getEntryForTesting(secret, terminalId)` returns the live `StackEntry` so the test can drive `applyHistoryReplay` against a real entry without standing up a WebSocket.

**Quit-confirm preview bg race fix (HS-8059 follow-up, 2026-05-01):** the §37 quit-confirm preview pane's bg-cascade (originally HS-8058) read the bg from `currentCheckout.term.options.theme.background` synchronously after `checkout(...)` returned. That only worked when ANOTHER consumer (drawer pane) had already mounted the same terminal and run `applyAppearanceToTerm` against the shared xterm — the appearance application is async (awaits `loadGoogleFont`), so a fresh-checkout-only path saw `term.options.theme === undefined` and the gutter fell back to the SCSS `var(--bg)` gray. User reported "in one of the screenshots the background area matches the color of the terminal. in the other, its using a gray background. it's specifically the `quit-confirm-detail-preview` element whose background color is only getting set sometimes. maybe a race condition but unclear" (5/1, 12:59). Fix: `/api/projects/quit-summary` now includes per-entry `theme?/fontFamily?/fontSize?` (read from `listTerminalConfigs`) and per-project `terminalDefault?` (read from `settings.terminal_default`) on the response. The client (`quitConfirm.tsx::showQuitConfirmDialog`) pre-resolves a `bgByRowKey: Map<\`${secret}::${terminalId}\`, string>` synchronously via `resolveAppearance({projectDefault, configOverride}) → resolveAppearanceBackground(...)` BEFORE rendering. `selectRow` writes `previewEl.style.background = bgByRowKey.get(...)` instead of reading from the term. Deterministic regardless of checkout order. 3 new tests in `quitConfirm.test.ts` (re-titled `(HS-8058 / HS-8059)`) pin per-entry override → bg, `terminalDefault` fallback when entry has no override, and the regression assertion that the bg paints WITHOUT another consumer ever calling `applyAppearanceToTerm`. 1 new test in `routes/projects.test.ts` pins the new payload shape on `/api/projects/quit-summary`.

**Tile-preview theme-bg cascade (HS-8059, 2026-05-01):** `mountTileViaCheckout` in `src/client/terminalTileGrid.tsx` now writes `tile.preview.style.backgroundColor = themeData.background` at mount time so the tile-preview frame around the scaled `.xterm-screen` reads as part of the terminal — mirrors the §22 drawer's HS-7960 treatment, the §25 dedicated-view's HS-7960 treatment, and the §37 quit-confirm preview's HS-8058 treatment. Pre-fix `.terminal-dashboard-tile-preview` and `.drawer-terminal-grid-tile-preview` painted `var(--bg)` (white in the default app theme), producing a visible colour seam between the preview frame and the xterm canvas under any non-white theme (the screenshot user attached to HS-8059 showed a Dracula-themed Claude Code TUI inside a tile with a white frame around it). The inline bg is cleared on the placeholder transitions in `softDisposeTile` + `spawnAndEnlarge`'s `Starting…` branch so cold / exited / starting placeholders still paint with `--bg-secondary`. Same shared `terminalTileGrid.tsx` mount path serves both the §25 dashboard and the §36 drawer-grid (different `cssPrefix` only), so both surfaces get the treatment in one change. SCSS comments on `.terminal-dashboard-tile-preview` + `.drawer-terminal-grid-tile-preview` updated to note the `--bg` declaration is now a pre-paint fallback rather than the painted value. 4 new unit tests in `terminalTileGrid.test.ts` under `terminalTileGrid — preview bg cascade (HS-8059)` cover: alive-tile-paints-theme-bg, two-tiles-with-different-themes-get-different-bgs, non-alive-placeholder-leaves-inline-bg-empty (so the SCSS `--bg` fallback wins), drawer-grid-tile-prefix-also-gets-the-treatment.

**Long-task observer (HS-8054, 2026-05-01; v2 follow-up 2026-05-01; v3 follow-up 2026-05-04):** v3 added a file-based freeze.log system on top of v1/v2's console + toast surfacing, after the user reported on 2026-05-04 that they still didn't see any log messages — and that the freeze may not even be the client locking up, but the WS connection between client and server (or server and a PTY). New server module `src/diagnostics/freezeLogger.ts` exports (1) `appendFreezeLog(dataDir, entry)` — single-flight per-dataDir queue that appends a JSONL line to `<dataDir>/freeze.log` (paste-ready, machine-readable AND grep-friendly); (2) `startServerEventLoopHeartbeat(dataDir)` — Node-process equivalent of the client heartbeat using `process.hrtime.bigint()` + 50ms `setInterval`, logs `source: 'server-heartbeat'` entries when the event loop blocks ≥ 100ms; (3) `instrumentSync(dataDir, label, fn)` / `instrumentAsync(...)` wrappers for callers to flag suspicious blocks (PTY write paths, etc) with `source: 'server-instrument-{sync,async}'`. Companion route `POST /api/diagnostics/freeze` in `src/routes/diagnostics.ts` accepts client-detected events (with input-shape validation via `coerceFreezeEntry` — only `client-observer` / `client-heartbeat` source tags accepted from clients, server tags are server-produced only) and forwards them through `appendFreezeLog`. The user gets a single file with both client-side AND server-side hangs interleaved by timestamp. Heartbeat starts from `cli.ts::startAndConfigure` immediately after `startServer` resolves; stops in `lifecycle.ts::runShutdownPipeline` after `closeDatabases` so a slow `db.close` still gets logged. Client wire-in: `longTaskObserver.tsx::recordLongTask` now `void postFreezeLog(...)` — fire-and-forget POST to the endpoint after the existing console.error + toast paths. Tests: 14 in `src/diagnostics/freezeLogger.test.ts` (append correctness + concurrent-write serialisation + unwritable-dir error handling + sync/async instrumentation duration gating + heartbeat detection of a synthetic 250ms event-loop block) + 8 in `src/routes/diagnostics.test.ts` (`coerceFreezeEntry` validation: well-formed entries pass, server-source tags rejected, malformed durationMs rejected, optional extra field preserved/dropped per shape).

---

**Long-task observer (HS-8054, 2026-05-01; v2 follow-up 2026-05-01):** new `src/client/longTaskObserver.tsx` module subscribes to `PerformanceObserver({ type: 'longtask' })` AND runs a `setInterval(50ms)` heartbeat detector ("did this 50ms timer take much longer than 50ms to fire?") so long-task surfacing works even when the PerformanceObserver path is unsupported or silently dropping events. Pre-v2 the user reported "still happens sometimes but i dont see any log messages" — the v1 observer-only build was missing the user's hangs (likely WebKit/WKWebView spotty support OR the threshold/timing not crossing the longtask boundary). v2 also: (a) prints a startup line `[hotsheet longtask] init — observer:on/off heartbeat:on threshold:100ms` so the user can verify the instrumentation is wired; (b) switched `console.warn` → `console.error` so the line stands out against console-log noise during init; (c) tags each detection with `[observer]` or `[heartbeat]` so the user can tell which detector caught the hang; (d) shows a small toast on every detected long-task ≥ 500 ms (rate-limited to once per 10 s) so the user notices the instrumentation working without DevTools open. Long-task console format: `[hotsheet longtask] HH:MM:SS.mmm — Nms [source] (recent: <interactions>)`. Companion ring buffer of recent UI interactions (`recordInteraction(label)` exported helper) lets each long-task log line include the interactions that fired in the window leading up to the block — tab clicks, project switches, terminal activations, detail opens — pointing at the offending caller without requiring a DevTools profile. Wire-in points: `projectTabs.tsx::switchProject` (`project-switch:<name>`), `commandLog.tsx::switchDrawerTab` (`drawer-tab:<id>`), `terminal.tsx::activateTerminal` (`activate-terminal:<id>:mount` vs `:reuse` so the log differentiates first-mount cost from steady-state activation), `detail.tsx::openDetail` (`open-detail:<id>`). Three diagnostic globals — `window.__hotsheetGetLongTasks()`, `window.__hotsheetClearLongTasks()`, `window.__hotsheetGetInteractions()` — for grabbing the in-memory buffers from DevTools. Inert when `PerformanceObserver` or the `longtask` entry type is unavailable (older Safari / WKWebView, happy-dom). 11 unit tests in `longTaskObserver.test.ts` cover ring-buffer truncation × 2, long-task recording + console format × 4, idempotent-init, the window-globals export path, and feature-detect inertness when `PerformanceObserver` is undefined / when `longtask` isn't in `supportedEntryTypes`. **The user reported intermittent multi-second UI hangs** ("usually when switching tabs but I think sometimes doing other interactions as well"); without a repro the root cause couldn't be attributed, so this is the wild-catch instrumentation. The user will paste log output back when they next observe the hang.

**Tile-grid bug fixes (HS-7602, HS-7603):** HS-7602 fixed the `.terminal-dashboard-dedicated` overlay anchoring to the wrong element when the dashboard was scrolled — `position: absolute; inset: 0` inside the scrollable `.terminal-dashboard` container scrolled with content, so double-clicking a tile near the bottom rendered the dedicated view above the visible viewport with the grid still scrollable underneath. Changed to `position: fixed; inset: 0; z-index: 600` (above the centered tile's z-index 500). Safe because `body.terminal-dashboard-active` hides every other surface, so the dashboard always occupies the full viewport whenever the dedicated overlay can mount; the dashboard's prior scroll position is preserved naturally on exit. HS-7603 fixed offscreen tiles getting stuck at the fallback 80×60 cols/rows: the per-tile `.xterm-screen` ResizeObserver in `mountTileXterm` (terminalTileGrid.tsx) now also drives a new `resyncTilePtyFromCellMetrics(tile)` helper alongside the existing `reapplyTileScaleFromPreview` call. The earlier wiring fired the cell-metrics → term/PTY resize logic only from (1) the initial rAF after `term.open()` and (2) the WebSocket history-frame handler — both could race xterm's first paint, leaving `screen.offsetWidth === 0` and the term + PTY stuck at the fallback even after the user scrolled the tile into view. Routing through the screen ResizeObserver — which fires on the 0×0 → native first-paint transition that xterm itself drives — pushes the term + PTY to authoritative cell metrics whenever they become available. The helper's idempotency gate compares against BOTH `term.cols/rows` (current shape) AND `tile.targetCols/Rows` (last value sent to server) so the call is safe across all three entry points: skip only when both are at native, otherwise resize the term + send the WS resize message. Regression tests: `terminal-dashboard.spec.ts` "dedicated view covers the visible viewport even when the dashboard is scrolled (HS-7602)" + `terminal-dashboard-tile-rendering.spec.ts` "tiles below the fold end up at native dims, not the fallback 80×60 (HS-7603)".

**Flow layout mode (HS-7662):** `terminalDashboard.tsx` gains a second layout mode alongside the default sectioned-per-project rendering. Toggle `#terminal-dashboard-layout-toggle` (Lucide `text-wrap`) sits in the dashboard toolbar between the dashboard toggle and the size slider; pressed-state mirrors `.active` on the dashboard toggle. State is persisted to `/file-settings` under `dashboard_layout_mode: 'sectioned' | 'flow'` (default `'sectioned'`); a cached promise (`loadLayoutMode()`) reads the value once at init so first paint reflects the saved mode without flicker. `paintDashboardSections` branches into `paintSectionedLayout` (existing per-project section rendering — heading, `+` add-terminal button, `(N terminals)` count, per-section grid + handle keyed by project secret) or `paintFlowLayout` (single `.terminal-dashboard-grid.terminal-dashboard-grid-flow` container with one `TileGridHandle` keyed by the sentinel `__flow_handle__` in `gridHandles`). Flow mode flattens every project's terminals into one tile list in registered-project order; empty projects are dropped entirely. Each tile gets a project-color badge derived from a stable hash of the project secret → HSL hue (`projectBadgeColor(secret)`); the FIRST tile of each project's run additionally gets the `{ProjectName} ›` label prefix. `TileEntry.projectBadge` (new optional field — `{ color: string; name?: string }`) drives both the dot and the prefix in the shared `terminalTileGrid.tsx` `renderTile`; sectioned mode passes undefined, so the existing label markup is unchanged. The bell long-poll fan-out checks the `__flow_handle__` sentinel and forwards the union of every project's pending bell IDs (sectioned handles still get their per-secret subset). Per-tile callbacks (right-click context menu, dedicated-bar mount, on-tile-enlarge / -shrink) recover the originating project via a `Map<terminalId, ProjectInfo>` built at paint time. SCSS additions: `.terminal-dashboard-tile-badge` (8 px circle, color from inline-style hue), `.terminal-dashboard-tile-project` (muted-color `{Project} ›` prefix), `.terminal-dashboard-tile-name` (the bare terminal name), `.terminal-dashboard-layout-toggle` (26 × 26 muted-pill matching the §22 hide-terminal eye icon, with `.active` accent treatment). Drawer-grid parallel classes are emitted for symmetry but the drawer-grid module doesn't span projects so they're currently unused. 4 e2e tests in `e2e/terminal-dashboard-flow-layout.spec.ts` cover toggle visibility + reload persistence, flat-grid rendering with badge / project-prefix counts, no-fetch round-trip on toggle (cached lastSectionData), and empty-projects-dropped behavior. See docs/25-terminal-dashboard.md §25.10.5.

**Shared tile-grid module (HS-7595):** `terminalTileGrid.tsx` (~960 lines) consolidates the per-tile lifecycle that `terminalDashboard.tsx` (§25) and `drawerTerminalGrid.tsx` (§36) previously held in near-identical copies. Public API: `mountTileGrid({container, cssPrefix, centerSizeFrac, centerScope, centerReferenceEl, getSliderValue, onContextMenu?, onTileEnlarge?, onDedicatedBarMount?, onTileShrink?})` returns a `TileGridHandle` with `{rebuild(entries), applySizing(), recenterTile(), uncenterTile(), exitDedicatedView(), syncBellState(pendingIds), focusDedicatedTerm(), isCentered(), isDedicatedOpen(), dispose()}`. Each callsite owns its surrounding chrome (project sections / drawer toolbar / slider) and just delegates per-tile rendering + center + dedicated state. Key parameterisations: `cssPrefix` distinguishes class names (`terminal-dashboard` vs `drawer-terminal-grid`) so styles can evolve independently; `centerSizeFrac` is 0.7 for the dashboard's full-viewport overlay vs 0.9 for the drawer's cramped pane; `centerScope` is `'viewport'` for the dashboard (centered tile uses fixed positioning + body-attached backdrop) vs `'container'` for the drawer (centered tile positions against a reference rect + backdrop attaches inside that element so it doesn't dim the whole window); `onDedicatedBarMount` is the dashboard's hook for adding the project breadcrumb + mounting the search widget in the app-header search slot. The shared module owns: tile DOM (state-class + preview + label + optional CWD chip via `cwdLabel`/`cwdRaw` fields on TileEntry), xterm mount with HS-6307 appearance resolution + Google Font load + theme application, the WebSocket attach + history-replay + HS-7097 PTY-resize-to-tile-native flow, the `.xterm-screen` ResizeObserver-driven scale, single-vs-double-click disambiguation (220 ms `SINGLE_CLICK_DELAY_MS`), the FLIP center animation with grey slot placeholder + cubic-bezier transition, the dedicated-view overlay with FitAddon + HS-7063 post-replay refit + HS-7098 inner-pane wrapper, lazy/exited placeholder + spawn-on-enlarge via `/terminal/restart`, bell `.has-bell` toggle + `POST /api/terminal/clear-bell` on enlarge. Net cleanup: §25 dashboard 1847 → 688 lines, §36 drawer-grid 1101 → 370 lines, +962 lines new shared module = 2020 total (down from 2948, ~31% reduction). See docs/25-terminal-dashboard.md, docs/36-drawer-terminal-grid.md, and the existing e2e test suites (terminal-dashboard.spec.ts, terminal-dashboard-tile-rendering.spec.ts, drawer-terminal-grid.spec.ts) which cover both surfaces' integration.

**Drawer terminal grid (HS-6311):** `drawerTerminalGrid.tsx` is the per-project version of the terminal dashboard (§25), scoped to the drawer. Since HS-7595 the per-tile lifecycle lives in `terminalTileGrid.tsx`; this file owns the drawer-toolbar chrome (toggle + slider) + per-project state + on-list-update wiring + Esc routing. `initDrawerTerminalGrid({onExitGrid})` wires the Tauri-only toggle `#drawer-grid-toggle` + the `#drawer-grid-sizer` slider in `.drawer-tabs-end`; the grid container `#drawer-terminal-grid` lives alongside `.drawer-tab-content` panels inside `#command-log-panel`. `onTerminalListUpdated(entries)` is called by `terminal.tsx`'s `loadAndRenderTerminalTabs` on every `/terminal/list` refresh — it updates the toggle's enabled state (disabled when `entries.length < 2` per §36.7), auto-exits grid mode if the project drops below the 2-terminal minimum mid-use, and rebuilds the tile list when grid is active (covers project switch restoration too). `isDrawerGridActive()` + `exitDrawerGridMode()` are exported so `terminal.tsx`'s `activateTerminal` and `commandLog.tsx`'s `switchDrawerTab` auto-exit grid mode before activating a regular drawer tab (§36.3 mirrors §25.3 rule 3). Tile math reuses `terminalDashboardSizing.ts` verbatim — same `tileWidthFromColumnCount` / `computeTileScale` / `tileNativeGridFromCellMetrics` / `computeColumnSnapPoints` / `perRowToSliderPosition` / `sliderPositionToPerRow` helpers (HS-8176 replaced the pre-fix `tileWidthFromSlider` / `computeSliderSnapPoints` / `maybeSnapSliderValue`). Per-project state lives in `state.tsx`: `projectGridActive: Map<secret, boolean>` + `projectGridColumnCount: Map<secret, number>` with `getProjectGridActive` / `setProjectGridActive` / `getProjectGridColumnCount` / `setProjectGridColumnCount` accessors (HS-8176; pre-fix `projectGridSliderValue`); `clearPerProjectSessionState(secret)` also wipes these maps. Tiles mirror the dashboard's tile lifecycle — `mountTileXterm` opens an xterm at fallback 80×60 then resizes to tile-native 4:3 via the `.xterm-screen` ResizeObserver pattern; `connectTileSocket`'s history handler replays at PTY dims then resizes both the local xterm AND the PTY (HS-7097 follow-up). Single-click → FLIP-animated centered overlay at 90 % of the drawer body (`CENTER_SIZE_FRAC = 0.9` vs §25.7's 0.7 because the drawer is short). Double-click → `.drawer-terminal-grid-dedicated` inner overlay with `FitAddon.fit()` + post-replay refit via `applyDedicatedHistoryFrame`; `exitDedicatedView` re-sends the tile's target cols × rows so the PTY snaps back to tile-native dims on return. Bell indicators subscribe to `subscribeToBellState` from `bellPoll.tsx` and paint `.has-bell` (bounce keyframe + 2 px accent outline); `clearTileBell` fires `POST /api/terminal/clear-bell` on center / dedicated enter. Placeholder tiles (lazy / exited) render a muted play-glyph box and `spawnAndEnlarge(tile, 'center' | 'dedicated')` POSTs `/terminal/restart` for exited ones then mounts the xterm + transitions. 3 Playwright e2e tests in `e2e/drawer-terminal-grid.spec.ts` cover ≤1-terminal disabled state, toggle on/off, slider persistence across exit+re-enter, and tab-click auto-exit. 8 unit tests for the per-project grid state in `state.test.ts` (default false/33, set/clear, project-switch round-trip, clearPerProjectSessionState wipe, no-leak between projects). See docs/36-drawer-terminal-grid.md.

**Terminal appearance (HS-6307):** `terminalThemes.ts` (ordered registry of 11 `TerminalTheme` records — `default` is built fresh from CSS vars every call to track live dark-mode / accent changes, the other 10 are static ANSI palettes: Dracula, Solarized Dark/Light, Nord, Gruvbox Dark, Monokai, One Dark, Tomorrow Night, GitHub Dark/Light; `themeToXtermOptions` maps to xterm's `ITheme` shape; `getThemeById` returns null on unknown id so callers fall back explicitly; `DEFAULT_THEME_ID = 'default'`. 13 unit tests in `terminalThemes.test.ts`), `terminalFonts.ts` (ordered registry of 11 `TerminalFont` records — System + 10 Google Fonts monospaced faces: JetBrains Mono, Fira Code, Source Code Pro, IBM Plex Mono, Roboto Mono, Inconsolata, Ubuntu Mono, Space Mono, Anonymous Pro, Cascadia Code. `loadGoogleFont(font)` idempotently appends `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=…&display=swap" data-terminal-font-id="<id>">` to `<head>` and awaits `document.fonts.load('13px "<family>"')`; concurrent calls for the same font id share one promise via module-scope `loadedFonts: Map<id, Promise<void>>`; System short-circuits with no network fetch; `clampFontSize(v)` clamps to `[MIN_FONT_SIZE=8, MAX_FONT_SIZE=32]` with NaN → `DEFAULT_FONT_SIZE=13`; `buildGoogleFontsUrl(family)` + `_resetFontCacheForTests()` exported for tests. 16 unit tests in `terminalFonts.test.ts`), `terminalAppearance.ts` (three-layer resolver — `resolveAppearance({projectDefault, configOverride, sessionOverride})` walks each field independently from session > config > default > `FALLBACK_APPEARANCE = {theme: 'default', fontFamily: 'system', fontSize: 13}`; unknown theme/font ids fall back to default at resolve time. `applyAppearanceToTerm(term, appearance)` is async — loads the font first so xterm's canvas doesn't flash System glyphs for a frame, then assigns `term.options.theme / .fontFamily / .fontSize`. Module-scope `sessionOverrides: Map<terminalId, Partial<TerminalAppearance>>` with `getSessionOverride` / `setSessionOverride(id, partial)` (partial with `field: undefined` deletes that field only; empty map entry is auto-cleaned) / `clearSessionOverride(id)`. Project-default state lives in the same module — `loadProjectDefaultAppearance()` fetches `/file-settings`, parses `terminal_default` via `parseProjectDefault(raw)` (type-checks each field, unknown-shape → empty), and fires `hotsheet:terminal-default-changed` on change. `subscribeToDefaultAppearanceChanges(handler)` is what the drawer + dashboard both listen on. 20 unit tests in `terminalAppearance.test.ts`), `terminalAppearancePopover.tsx` (gear-button popover anchored via `getBoundingClientRect()` below the `.terminal-appearance-btn` on the terminal toolbar; `mountAppearancePopover({anchor, terminalId, isDynamic, onApply})` builds three controls — theme select, font select, font-size numeric input with −/+ steppers — plus a "Reset to project default" link. Changes apply instantly: for dynamic terminals via `setSessionOverride`; for configured terminals by fetching `/file-settings`, patching the matching `terminals[i]` entry, and PATCH-ing back. Dismissed on outside click, Escape, or a second gear click. Only one open at a time — a new mount dismisses the previous. `onApply` callback lets the caller re-run `applyAppearanceToTerm` against the live xterm), `terminalDefaultAppearanceUI.tsx` (Settings → Terminal "Default appearance" panel — `loadAndWireTerminalDefaultAppearance()` is called on dialog open to refresh the cached project default, re-render the three selects from the current `TERMINAL_THEMES` / `TERMINAL_FONTS` arrays, and attach listeners once. Changes write to `terminal_default` via `/file-settings` PATCH + merge into the module cache + fire `hotsheet:terminal-default-changed` so every mounted xterm re-resolves without a page reload). Wiring: `terminal.tsx`'s `mountXterm` constructs the xterm with `themeToXtermOptions(getThemeById(resolved.theme))` so first paint is correct, then `void reapplyAppearance(inst)` post-`open()` loads the font and assigns `options.fontFamily` + `options.fontSize`; `initTerminal` subscribes to `hotsheet:terminal-default-changed` + re-applies on every live instance; `loadAndRenderTerminalTabs` also calls `loadProjectDefaultAppearance()` on every list refresh so project switches re-fetch. `terminalDashboard.tsx` uses `resolveTileAppearance(tile)` (wraps `resolveAppearance` with the tile's `listEntry` override fields) for both `mountTileXterm` and `enterDedicatedView`; `initTerminalDashboard` subscribes to the event to re-apply across every live tile + the dedicated view. `TerminalConfig` (`src/terminals/config.ts`) gains optional `theme` / `fontFamily` / `fontSize` fields — `normalizeConfig` type-checks each so a malformed `settings.json` entry can't break the mount. `TerminalListEntry` carries the same fields so `/terminal/list` hands them to the tile render path. `file-settings.ts`'s Zod schema is already `.loose()` so `terminal_default` passes through unchanged. See docs/35-terminal-themes.md.

**Entry + infra:** `app.tsx`, `api.tsx`, `state.tsx` (HS-7360: exports `clearPerProjectSessionState(secret)` + extends `setActiveProject` to save/restore `state.search` in a per-project `projectSearches` Map keyed by secret; fresh projects start empty, session-only state), `dom.ts`, `icons.ts` + `lucide-icons.json`, `animate.ts`, `poll.tsx`, `xtermTheme.ts` (HS-6866 — shared `readXtermTheme()` used by the drawer terminal and both dashboard xterm instances; reads `--bg` / `--text` / `--accent` off `:root`. HS-7330: also emits `selectionBackground` (accent @ 40 % alpha) and `selectionInactiveBackground` (accent @ 20 % alpha) via `withAlpha(color, alpha)` — without these xterm falls back to a near-white translucent default that's invisible on the white `--bg` and users reported "can't select text in terminals" even though the selection range was registered correctly), `terminalKeybindings.ts` (HS-7329 / HS-7459 / HS-7460 — `isClearTerminalShortcut(e, isMac?)`, `isFindShortcut(e, isMac?)`, and `isJumpShortcut(e, isMac?) → 'prev' | 'next' | null` pure helpers shared by every xterm mount site (drawer, dashboard tile, dashboard dedicated view) plus the global `shortcuts.tsx` Cmd/Ctrl+F handler. All three go through a private `hasPlatformPrimaryModifier(e, isMac)` predicate that requires the platform-correct modifier (Cmd on macOS / Ctrl elsewhere) without the other-platform modifier also held and without Alt/Shift. `isMac` defaults to `navigator.userAgent.includes('Mac')`. HS-7459 originally fixed `Ctrl+K` on macOS being hijacked away from readline's `kill-line`; HS-7460 extends the same platform-specific match to `Ctrl+F` (preserves macOS readline `forward-char`) and `Ctrl+Up/Down` (preserves macOS tmux pane resize / vim / fish-shell history-token-search). 40 unit tests in `terminalKeybindings.test.ts` cover all three helpers across mac/non-mac branches, both-modifiers-held rejection, Alt/Shift passthrough, other-key rejection, non-keydown rejection, and the runtime-detection default), `terminalSearch.tsx` (HS-7331 — `mountTerminalSearch(term, addon, opts?)` builds the collapsible search widget hosted in the drawer toolbar's `.terminal-search-slot` and the dashboard dedicated view's `#terminal-dashboard-search-slot`; the pure helper `focusActiveTerminalSearch()` routes Cmd/Ctrl+F from the global shortcuts handler to the most recently active handle whose root is still in the DOM, falling through to the ticket search on a no-match; amber highlight palette avoids the HS-7330 accent selection collision. HS-7426: three match-mode toggles (Match case / Whole word / Regex via Lucide `case-sensitive` / `whole-word` / `regex` icons) in a `.terminal-search-toggles` group; per-mount `activeSearchOptions` object; click handler flips the boolean + `syncToggleButtons()` (mirrors state into `aria-pressed` + `.is-active`) + re-runs the find with `incremental: false` so highlights refresh from scratch; regex mode validates `new RegExp(q)` up-front, sets `.is-invalid` + count `err` on `SyntaxError`, skips the addon call; `onDidChangeResults` early-returns when `.is-invalid` so the addon can't overwrite `err` with `0/0`; `resetToggles()` + `setInvalidRegex(false)` fire on `closeBox`. HS-7427: per-`XTerm` `historyByTerm: WeakMap<XTerm, string[]>` records Enter / Shift+Enter submissions only (incremental input is excluded), delegates push semantics to `pushHistory(history, query, cap=10)` from `terminalSearchHistory.ts` (MRU-at-tail, dedup-before-cap, empty/whitespace skip), and walks the ring on ArrowUp/ArrowDown via `navigateHistory(history, cursor, direction, currentDraft)`. The mount-time integration captures `currentDraft` the first time the user navigates away from `cursor === history.length` so ArrowDown back to the tail restores the in-flight query; the readline-style edge-position guard only fires while still in draft mode so subsequent presses keep walking. Reset on Enter / × close / typing into the input. Session-only — the `WeakMap` entry is reclaimed when the xterm is GC'd on PTY restart or drawer-tab destroy), `terminalSearchHistory.ts` (HS-7427 — pure `pushHistory` and `navigateHistory` helpers; 14 unit tests in `terminalSearchHistory.test.ts` cover push MRU / cap / custom cap / cap-of-zero / dedup-bumps-tail / dedup-before-cap / empty-query-noop / no-mutation, and navigateHistory empty / first-up / walk-up / oldest-stays / walk-down / draft-restore / out-of-range-clamp / no-mutation / round-trip-draft. DOM-level integration covered by 7 new tests in `terminalSearch.test.ts` and an e2e in `e2e/terminal-search.spec.ts`), `bellPoll.tsx` (HS-6603 §24.4.1 cross-project bell long-poll against `/api/projects/bell-state`; exports `startBellPolling()`, `getBellState()`, `subscribeToBellState(cb)` — terminal.tsx subscribes to keep in-drawer indicators in sync. HS-7272: `maybeFireNotificationToast` now also calls `fireNativeNotification(projectName, message)` when `isAppBackgrounded()` — toast + native share the `recentlyToasted` dedupe so an unchanging message fires exactly once across both channels), `shortcuts.tsx`, `tauriIntegration.tsx` (HS-7272 exports `requestNativeNotificationPermission()` / `fireNativeNotification(title, body)` / `isAppBackgrounded()` — permission is primed once at boot against `window.__TAURI__.notification`; native invoke falls through to `false` in a browser context so callers need no `if (tauri)` guard), `projectTabs.tsx` (includes `updateProjectBellIndicators(bellStates)` + `.project-tab-bell` span injection for HS-6603 §24.4.2), `pluginTypes.ts`.

**List / rows / details:** `ticketList.tsx`, `ticketListState.ts`, `ticketRow.tsx`, `draftRow.tsx`, `detail.tsx`, `columnView.tsx`, `contextMenu.tsx`, `batch.tsx`, `dropdown.tsx`.

**Sidebar / search / nav:** `sidebar.tsx`, `customViews.tsx`, `dashboardMode.tsx`, `dashboard.tsx`, `share.tsx`.

**Tagging / notes / print / clipboard:** `tags.tsx`, `tagsDialog.tsx`, `tagAutocomplete.tsx`, `noteRenderer.tsx`, `print.tsx`, `clipboard.ts`, `clipboardUtil.tsx`, `imageProxy.tsx`.

**Settings / backups / icons:** `settingsDialog.tsx`, `settingsLoader.tsx`, `settingsCategories.tsx`, `experimentalSettings.tsx`, `iconPicker.tsx`, `backups.tsx`, `dbRecoveryBanner.tsx` (HS-7899 launch-time prompt), `dbRepairUI.tsx` (HS-7897 Settings → Database Repair panel), `openFolder.tsx`.

**Dialogs / overlays:** `confirm.tsx` — `confirmDialog({message, title?, confirmLabel?, cancelLabel?, danger?}) → Promise<boolean>` in-app replacement for `window.confirm`, which is a silent no-op in Tauri WKWebView (always returns false without showing a dialog). Use this for every yes/no prompt.

**Feedback drafts (HS-7599):** new `src/db/feedbackDrafts.ts` (CRUD over the `feedback_drafts` table — schema in `src/db/connection.ts`: `id TEXT PK`, `ticket_id INTEGER FK ON DELETE CASCADE`, `parent_note_id TEXT NULLABLE`, `prompt_text`, `partitions_json`, timestamps; `idx_feedback_drafts_ticket` + `idx_feedback_drafts_parent_note`). Storage is local-only — drafts are NOT plumbed through any plugin sync engine. Server API: GET/POST/PATCH/DELETE `/api/tickets/:id/feedback-drafts[/:draftId]` in `src/routes/tickets.ts`, validated by `FeedbackDraftCreateSchema` / `FeedbackDraftUpdateSchema` in `src/routes/validation.ts`. Client: `src/client/feedbackDialog.tsx` `showFeedbackDialog()` gains an optional `draftSeed` parameter that uses the saved blocks verbatim (NOT a fresh `parseFeedbackBlocks`) so future heuristic changes don't reshape an existing draft when re-opened — also gains `parentNoteId` arg + Save Draft button + `overlayHasAnyText` / `collectPartitions` / `partitionsHaveText` helpers + click-outside-the-dialog dismissal gated on "any input has text"; `src/client/noteRenderer.tsx` exposes `setTicketDrafts(ticketId, drafts)` + `getTicketDrafts(ticketId)` accessors and `buildDraftEntry(ticketId, draft, notes)` for rendering draft cards inline below their `parent_note_id` (or free-floating at the end if the parent's gone); `src/client/detail.tsx` GETs `/feedback-drafts` on detail-panel-open and re-renders the notes list once they arrive. Visual: dashed-bordered card with a "Draft" badge + one-line preview; right-click → Delete Draft context menu. Tests: 7 unit in `src/db/feedbackDrafts.test.ts` + 4 e2e in `e2e/feedback-drafts.spec.ts`. See docs/21-feedback.md §21.2.3.

**Channel / commands / feedback:** `channelUI.tsx`, `permissionOverlay.tsx` (permission popup, poll loop, HS-6637 minimize-to-pulsating-dot flow — exports `reopenMinimizedForSecret(secret)` called from the project tab click, `getMinimizedPermissionSecrets()` read by `updateStatusDots`. HS-7266: popup is non-modal; the capture-phase outside-click handler was removed so rest-of-UI clicks are delivered normally, and a new **Minimize** text link inside the popup is the explicit way to drop it into the pulsating dot. `.permission-popup-links` / `.permission-popup-links-sep` style the Minimize · No-response-needed row. HS-8183 extracted the per-poll body into an exported `processPermissionPollResponse(data)` (called from the `startPermissionPolling` poll loop), added `AUTO_DISMISS_MISS_THRESHOLD = 2` so a single transient `checkAll` failure doesn't rip the popup, and wrapped `showPermissionPopupBody` in a try/catch that resets `activePopupRequestId` + `activeCheckoutHandle` + removes any partial-mount DOM before rethrowing — pre-fix a partial-mount throw left `activePopupRequestId` set, blocking every subsequent show via the `if (activePopupRequestId !== null) return;` gate. HS-8206 v1 replaced the single-rAF post-checkout `proposeDimensions + resize` with a `ResizeObserver` mirroring `quitConfirm.tsx::HS-8055`; v2 replaced the observer's inline rAF callback with a module-level `runLiveTermFitWithRetry(handle)` that polls `proposeDimensions()` up to 30 times at 16 ms intervals (~480 ms budget) until xterm's renderer has measured cell dims, since for a fixed-CSS-size popup container the v1 observer fires once on initial observe + bails on undefined, and no follow-up size-change events ever fire (the term stayed at 100×30). `clearLiveTermFitRetryTimer()` runs from `releaseActiveCheckoutIfAny()` + `_resetStateForTesting()` so a popup-close-mid-retry can't leak. Companion fix in `terminal.tsx::mountInstanceViaCheckout` adds `onRestoredToTop: () => requestAnimationFrame(() => doFit(inst))` so the drawer pane refits to its actual container width when the popup releases and the live xterm reparents back — pre-fix `applyResizeIfChanged` inside the §54 `releaseInternal` resized to the drawer's ORIGINAL checkout cols/rows (80×24) not its current geometry, leaving the drawer at the popup's narrow cols with content wrapping. **HS-8207** (2026-05-05) added two further reliability fixes to close out the user-reported "starts blank → shows some content → shows completely different content → disappears entirely" multi-phase symptom. (a) New `peekEntryDims(secret, terminalId)` export from `terminalCheckout.tsx` is used to pass the EXISTING entry's dims (or 80×24 default when no entry exists) to `checkout(...)` so the swap-time `applyResizeIfChanged` is a no-op — pre-fix the popup hardcoded `cols: 100, rows: 30` which fired one SIGWINCH + claude redraw at checkout, then HS-8206 v2's fit-retry fired a second SIGWINCH + redraw to popup-fit dims; the back-to-back redraws WERE the "shows some content → shows completely different content" symptom. (b) New module-level `activePopupOwnerSecret` in `permissionOverlay.tsx` tracks the popup's owner project so `processPermissionPollResponse`'s auto-dismiss path can distinguish "owner missing from `data.permissions`" (channel-server unreachable transiently — DON'T tick counter) from "owner present-with-null" (confirmed not pending — tick). Companion server-side change in `routes/projects.ts::checkAll` OMITS a project entirely from the response when the per-project channel-fetch throws, instead of returning `null` (which pre-fix collapsed transient-unreachable into confirmed-not-pending). Counter is preserved (not reset) on unreachable mid-streak so a "null then unreachable then null" sequence still eventually dismisses on the second confirmed null. `_resetStateForTesting()` + `_inspectStateForTesting()` exposed for `permissionOverlay.test.ts` (32 cases). `e2e/permission-popup-live.spec.ts` (8 tests) drives the real polling loop end-to-end via `page.route()` mocks: chrome rendering, no-churn-on-same-id, single-null-doesn't-dismiss, double-null-dismisses, **owner-missing-from-response-doesn't-dismiss**, Allow / Deny click → respond payload, truncated-input → live-checkout body slot. **HS-8218 (2026-05-06)** closes a follow-up failure mode HS-8216 surfaced: pre-fix the popup hardcoded `terminalId: 'default'` for live-checkout, and when the project's claude was running in some *other* terminal id (and `'default'` had never been started), the WS attach landed in the `attach()` create-session-and-spawn branch — spawning a brand-new `claude --dangerously-load-development-channels` PTY into the popup body that stole the channel-server's MCP connection from the user's actual claude. User repro: "blank terminal in popup briefly → claude channels permission check briefly → new empty claude prompt → permissions check disappears". Fix: new `noSpawn` opt-in across the attach pipeline. `attach()` returns `{ alive: false, noSession: true, history: empty }` without creating a session when `noSpawn === true` and no session exists; `routes/.../websocket.ts` reads `?noSpawn=1` and on `noSession: true` sends an empty history frame + closes the socket with code 1000. `terminalCheckout.tsx` accepts `noSpawn` in `CheckoutOptions`, plumbs it onto a per-entry flag (so reconnects keep the contract), and adds an `onNoLiveSession` callback that fires on the no-session history frame. The popup passes `noSpawn: true` and an `onNoLiveSession` handler that calls a new `fallbackToNonLivePreview()` — releases the checkout (cleanly tears the entry down via `disposeEntry`) and swaps the popup body from the empty live-terminal container to the same flat / diff preview the non-live code path renders. New test-only export `_simulateNoSessionForTesting(secret, terminalId)` in `terminalCheckout.tsx` mirrors what the prod WS message handler does on `noSession: true`. Tests: 4 new in `registry.test.ts` under `attach with noSpawn (HS-8218)` (noSession returned + no spawn / live-session pass-through / exited-session not noSession / default-still-spawns); 5 new in `websocket.test.ts` (3 authenticate parsing + 2 roundtrip — noSession+close-1000 / noSpawn-on-live-session-finds-existing-PTY); 4 new in `permissionOverlay.test.ts` under `noSpawn fallback (HS-8218)` (popup checkout carries noSpawn / fallback swaps body to flat preview / chrome and Allow-Deny intact across the swap / non-noSpawn entry leaves the simulator a no-op so live xterm survives a drawer-pane-then-popup checkout). **HS-8217 (2026-05-06)** broadened the live-borrow gate so the popup uses terminal borrowing for any non-trivial preview, not just truncated ones. Pre-fix the gate fired only on `flatTruncated || diffTruncated`; user reported the static colour-coded HTML diff path was hard to follow vs the actual claude TUI's coloured output ("the text is hard to follow. in the terminal, the edits are color coded so it's easier to see what's being added / removed... when possible, we should use the terminal borrowing method for the longer cases"). New exported pure helper `shouldUseLiveCheckout(editDiff, previewText)` returns true on any of: parseable Edit/Write diff regardless of size (the TUI rendering is always richer), flat truncation `…` (HS-7999), Edit-diff `truncated: true` (HS-8139), multi-line flat preview, or single-line flat preview longer than `LIVE_CHECKOUT_PREVIEW_CHAR_THRESHOLD = 80` chars. Short single-line bash one-liners (`ls -la`, `git status`, single-file `Read`) stay on the static `<pre>` path. The HS-8218 `noSpawn` fallback ensures the broadened gate stays safe — when no live `'default'` PTY exists, the popup gracefully falls back to the static preview via `onNoLiveSession`. 9 new unit tests under `shouldUseLiveCheckout — pure heuristic (HS-8217)` lock the helper (incl. boundary at exactly 80 chars + one-char-over flip); 2 integration tests under `non-truncated Edit triggers live checkout (HS-8217)`; 1 negative-regression under `short bash stays static (HS-8217)`. **HS-8219 (2026-05-06)** — user reported "it's sometimes showing multiple permissions popups at once -- it should only show one at a time -- using a stack data structure". Pre-fix the gate `if (activePopupRequestId !== null) return;` dropped new permissions arriving while one was active; the polling loop re-introduced them on the next 100 ms tick, but a partial-mount race could still leak a second `.permission-popup` into the DOM. Fix: introduced literal stack `pendingPermissionStack: { secret, perm }[]` — `showPermissionPopup` pushes onto the stack instead of dropping when another popup is active (with duplicate-id guard so the polling loop's repeated for-each doesn't grow the stack); every popup-close path calls a new `mountNextFromPendingStack()` that pops + skips stale entries + mounts the next valid one; end-of-poll GC walks the stack and drops entries whose `request_id` is no longer in `data.permissions` (channel server resolved elsewhere); every `querySelector('.permission-popup')?.remove()` flipped to `querySelectorAll(...).forEach(el => el.remove())` for defence-in-depth. New exported pure helper `shouldSkipPermission(requestId)` centralises the responded / dismissed / minimized lookup; new `getQueuedPermissionRequestIds()` exposes the stack's request-id list. LIFO semantics — most recently pushed pops first per the user's "stack data structure" ask. 9 new unit tests under `pendingPermissionStack — single-popup contract (HS-8219)` lock the contract: first-active+others-queued / repeated-polls-don't-re-push / GC-on-server-resolve / mount-next-on-Allow / mount-next-on-X / LIFO-ordering / skip-stale-queued-on-pop / querySelectorAll-defensive-cleans-up-stray / `shouldSkipPermission` lookup)), `permissionPreview.ts` (`formatInputPreview(tool, raw)` — strips JSON wrapper off Claude's `input_preview`, with forgiving partial-JSON fallback for truncated Bash commands, HS-6634), `commandLog.tsx` (drawer shell, delegates `terminal:<id>` tabs; exports `previewDrawerTab(tab)` that returns a restorer — used by Settings → Terminal delete to reveal the doomed terminal before the confirm), `commandLogFilter.tsx`, `commandSidebar.tsx`, `commandEditor.tsx`, `feedbackDialog.tsx`.

**Terminal dashboard (HS-6272):** `terminalDashboard.tsx` + `terminalDashboardSizing.ts` (pure math: post-HS-8176, `tileWidthFromColumnCount(perRow, rootWidth)` is the manual sizing entry point — `(rootWidth - (perRow - 1) * TILE_GAP) / perRow`, integer perRow in `[MIN_TILES_PER_ROW, MAX_TILES_PER_ROW]` (1..10), default `DEFAULT_TILES_PER_ROW = 4`. The slider element is LTR (1 left, 10 right) and the user-facing column count is the inverse via `perRowToSliderPosition` / `sliderPositionToPerRow` so the leftmost slider position renders the most tiles per row. `computeColumnSnapPoints(rootWidth)` returns one snap point per integer (precomputed `tileWidth` for tooltip rendering). `legacySliderValueToColumnCount(legacy0to100)` migrates pre-HS-8176 persisted `dashboard_slider_value` floats to the new integer-keyed `dashboard_columns_per_row` setting on read. Pre-HS-8176 used `tileWidthFromSlider(0..100, rootWidth)` — linear interp between `SLIDER_MIN_TILE_WIDTH` (~133 px) and `rootWidth` — plus `computeSliderSnapPoints` / `maybeSnapSliderValue` for dynamic snap-points; all three helpers deleted in HS-8176 along with `SLIDER_SNAP_THRESHOLD`. `computeTileWidth()` retained for its existing tests but no longer called at runtime since HS-7031 replaced auto-fit with the explicit slider, `computeTileScale()` for per-tile xterm-root scale + explicit width/height per HS-6865, `computeTileGridDims()` (HS-7097 follow-up brought this back at runtime — see `tileNativeGridFromCellMetrics()` which wraps it so tile xterms resize to 4:3-native dims post-replay), `tileNativeGridFromCellMetrics(cellW, cellH)` (thin wrapper so tile-native intent reads clearly at call sites)) — Tauri-only second top-level view that displaces the ticket UI with a grid of every configured terminal across every registered project. HS-6832 (foundation): `initTerminalDashboard()` wires up `#terminal-dashboard-toggle` (lucide `square-terminal` since HS-7030 — was `layout-panel-left`, pre-rendered into `.app-header` before `#app-title-area` with `display:none`), the module-level `active` flag, `body.terminal-dashboard-active` class that CSS uses to hide the sidebar / header-controls / batch toolbar / detail panel / drawer / banners / footer, and Esc handling (skipped when a center-overlay or dedicated-view child is mounted so §25.7 / §25.8 can consume Esc first). `exitDashboard()` is called from `projectTabs.tsx`'s tab click handler so clicking any project tab auto-exits and navigates to the ticket view. HS-6833 (grid + sizing): on enter, fetches `/api/projects` then a per-project `/api/terminal/list` via `apiWithSecret()`, renders `<section>` per project with a heading + either a flex-wrap `.terminal-dashboard-grid` of tile skeletons or an empty-state row; `applyTileSizing()` picks a single global tile width via `computeTileWidth()` (pure module, iterates from 480 px down to the 100 px-preview-height floor of 133 px-width; honours viewport height; root vertical-scrolls when even the floor doesn't fit) and writes preview width/height inline on every tile so a resize observer can retune in-place. HS-6838 (placeholder spawn-on-enlarge): `/api/terminal/list` additionally annotates `exitCode` alongside `state`. Tiles whose state is `not_spawned` or `exited` render a `.terminal-dashboard-tile-placeholder-cold` inside the preview (big lucide `play` glyph + `Not yet started` or `Exited (code N)` status) and the dashboard DashboardTile record is still created so click handlers wire the same way as live tiles. Single-click on a placeholder → `spawnAndEnlarge(tile, 'center')`; double-click → `spawnAndEnlarge(tile, 'dedicated')`. `spawnAndEnlarge()` shows a "Starting…" placeholder, POSTs `/api/terminal/restart` if the tile was `exited` (lazy-unspawned terminals just open the WebSocket which triggers a spawn), flips the tile to `alive` (swaps class + mounts xterm + opens WebSocket), then transitions to the requested view. On failure the original placeholder is restored so the user can retry. HS-6837 (tile bell indicators): on dashboard enter, `subscribeToBellState` (from `bellPoll.tsx` §24) registers a callback that walks `liveTiles` on every long-poll tick; tiles whose terminalId appears in the bell state entry get `.has-bell` (bounce keyframe + 2 px accent outline); tiles that drop off the list have the class removed. `term.onBell` is also wired per-tile so same-project live bells fire the bounce immediately without waiting for the next tick. `clearTileBell(tile)` removes the class and fires `POST /api/terminal/clear-bell` — called from both `centerTile()` and `enterDedicatedView()`. Server's clear-bell wakes the bell-state long-poll so other windows / the project-tab indicator update promptly. HS-6836 (dedicated full-viewport view): double-click on a live tile calls `enterDedicatedView()` which appends a `.terminal-dashboard-dedicated` overlay to `#terminal-dashboard-root` (positioned absolute, z-index 400), renders a slim back-button bar (`lucide arrow-left` + `Project › Terminal` label), and mounts a NEW `XTerm` instance with `FitAddon` in the full body — separate from the grid tile's computed-cols × rows xterm. The dedicated xterm calls `fit.fit()` immediately and forwards `onResize` to a fresh WebSocket that DOES include `cols`/`rows` in the URL so the PTY can grow to match. HS-7063 (post-replay refit): the dedicated view's WS message handler uses `applyDedicatedHistoryFrame(term, fit, frame)` (from `terminalReplay.ts`) instead of `replayHistoryToTerm` directly — the replay's inner `term.resize(historyCols, historyRows)` fires xterm's `onResize` which gets relayed to the server and shrinks the PTY; without the follow-up `fit()` nano/vim/less stay at the drawer's dims and leave the bottom of the pane empty. Back button / Esc / dashboard-toggle-click all route through `exitDedicatedView()` which disposes the dedicated xterm + WebSocket, removes the overlay, and restores the prior state (grid OR whichever tile was centered before double-click). The grid tile's own fixed-cols-rows xterm is untouched — both instances attach to the same PTY so the dedicated view's fit() can't break the grid. Click/dblclick disambiguation uses a 220 ms single-click delay (`SINGLE_CLICK_DELAY_MS`) cancelled by dblclick — `e.detail` alone is unreliable because the centered tile's position shifts mid-gesture and confuses the browser's dblclick hit-testing. HS-6835 (click-to-center): single-click on a live tile centers it via `centerTile()` — adds `.centered` class, applies fixed position / `translate(-50%,-50%)` / z-index 500, resizes the preview to min(70 vw, 70 vh × 4/3) preserving the 4:3 aspect, re-runs `applyTileScale()` at the zoomed dims, and appends a `.terminal-dashboard-center-backdrop` (z-index 499 with a fade-in keyframe). `mountTileXterm()` wires `term.onData` to the tile's WebSocket so the centered tile accepts keystrokes. CSS drops `pointer-events:none` + the helper-textarea hide rule for `.centered` tiles so focus + input work. Dismissal: click backdrop (uses `uncenterTile()`), click same tile (toggle), Esc (document-level capture-phase keydown listener so it fires before xterm swallows the key — consumes Esc when a tile is centered and routes to `uncenterTile()` instead of `exitDashboard()`). `applyTileSizing()` persists `gridPreviewWidth` / `gridPreviewHeight` on the `DashboardTile` struct so `uncenterTile()` restores exactly the same grid dims without re-running the global sizer. HS-6834 (live tiles): `/api/terminal/list` extended to annotate every entry with `state: 'alive' | 'exited' | 'not_spawned'` so the client knows which tiles can safely WebSocket-attach. For `state === 'alive'` tiles `mountTileXterm()` creates a fresh `XTerm` instance at an initial `DASHBOARD_INITIAL_COLS × DASHBOARD_INITIAL_ROWS` (80 × 60 fallback) and calls `reapplyTileScaleFromPreview()` on the next rAF so the xterm's natural dims committed by the renderer get scaled into the preview. `connectTileSocket()` opens `/api/terminal/ws?project=&terminal=` WITHOUT `cols`/`rows` query params so the server skips the first-attach cleanup (HS-6799), then `replayHistoryToTerm()` resizes xterm to the history frame's cols × rows (= the PTY's real geometry) and writes the bytes so the scrollback renders at the geometry it was formatted for — and THEN (HS-7097 follow-up) the tile immediately resizes its xterm back down to tile-native 4:3 via `tileNativeDimsFromXterm(term, xtermRoot)` + `tileNativeGridFromCellMetrics(cellW, cellH)`. Those tile-native dims become the tile's `targetCols / targetRows` for the rest of the tile's lifetime, and the `.xterm-screen` ResizeObserver re-runs the scale pass when xterm commits the post-resize render. The xterm never runs `fit()` and the tile visually scales via `transform: scale(s)` only. `applyTileScale()` reads the authoritative natural size from the `.xterm-screen` child (xterm.js sizes that element to `cols × cellW` / `rows × cellH`, whereas the outer xterm root is a block-level wrapper that fills its parent horizontally and therefore mis-reports width as the tile width — root cause of HS-6931), then routes through `computeTileScale()` to apply a uniform `transform: scale(s)` + explicit inline `width` / `height` + an `absolute` left/top offset. HS-6997: `computeTileScale()` returns `left = horizontal-centered, top = 0` so the scaled xterm is top-aligned inside the preview — vertical dead space falls below the content (macOS-Terminal-style) rather than sandwiching it top-and-bottom, which was jarring for wide / short PTYs (e.g. 151 × 13). The preview background (HS-6866) matches xterm's theme background so the dead space is visually seamless with the canvas. HS-6865: without explicit dims xterm's absolutely-positioned `.xterm-viewport` collapses and the tile lays out wrong — `width` / `height` are set to the xterm's natural pixel size. HS-6931 (supersedes HS-6898): single uniform `scale(s) = min(tileW/naturalW, tileH/naturalH)`. The earlier two-axis `scale(scaleX, scaleY)` filled the 4:3 tile but stretched text enough to look distorted (a bad measurement against the block-level root also produced ~9× axis mismatches like `scale(3.478, 0.375)`). HS-6931's follow-up that resized xterm to a measured-cell 4:3 target (1280 × 960) was superseded by HS-6965 (see below) — stretching is gone, but letterboxing is accepted as the cost of correct wrapping. HS-6965 / HS-7097 follow-up (tile-native 4:3 dims, **PTY follows**): the `connectTileSocket` history handler first replays bytes at the PTY's cols × rows (so scrollback renders in its original geometry — HS-6965's original insight), resizes the tile's xterm back to tile-native 4:3 dims via `computeTileGridDims(measuredCellW, measuredCellH)`, AND sends a `'resize'` JSON message back over the WebSocket so the server-side PTY also moves to tile-native dims. The PTY-resize step is the load-bearing one — without it, a TUI like `nano` keeps drawing for the drawer's wide-short geometry and the tile's xterm shows nano's footer at row 14 of a 60-row buffer with rows 15-60 empty (HS-7097's "still wrong" screenshots). Pushing the PTY too makes nano `SIGWINCH`-redraw to fill the new geometry and the tile fills properly. Symmetric exit-path: `exitDedicatedView()` re-sends a `'resize'` with the tile's stored `targetCols × targetRows` so the PTY snaps back from the dedicated pane's geometry to the tile's after the dedicated view closes — without that, the tile would stay rendering bytes formatted for the dedicated pane until the next view-switch. E2E coverage: `e2e/terminal-dashboard-tile-rendering.spec.ts` configures a real PTY running a Python SIGWINCH-redraw fixture (`e2e/fixtures/terminal-draw.py`) and asserts the BOTTOM marker sits within 10 % of the preview bottom in grid / centered / dedicated views. HS-6964 (centered positioning): `centerTile()` also sets `tile.style.width = previewWidth` — without it, flex `align-items: center` slides the larger preview around the grid-slot-sized tile and the visual centre drifts away from the viewport centre. `applyTileSizing()` skips centered tiles on resize, and the dashboard resize handler calls `recenterTile()` so the centered overlay tracks the viewport centre across window resizes. Preview CSS width/height snap writes are done with `transition: none` + forced reflow in `centerTile`, `finishUncenterTile`, and `recenterTile` so the outer FLIP animation is the only grow/shrink animation running. The dedicated view's `fit.fit()` is wrapped in `requestAnimationFrame` + a per-body `ResizeObserver` so it doesn't measure pre-layout dims — xterm would otherwise stay at its default 80 cols and the pane would leave half the viewport empty. HS-6867 (grow-from-slot animation): `centerTile()` inserts a grey `.terminal-dashboard-tile-slot` placeholder of the tile's current grid size into the tile's slot before promoting it to the fixed-position overlay, so the surrounding tiles don't reflow. The tile is placed at its final centered geometry, then an inverse `translate(dx, dy) scale(sx, sy)` transform is applied without a transition to visually pin it at the placeholder, and in the next frame that transform is removed with a transition enabled — the browser interpolates the transform so the tile "grows" out of the slot. `uncenterTile()` runs the animation in reverse against the placeholder's current bounding box, then on `transitionend` (with a safety timeout) removes the placeholder and returns the tile to grid flow. HS-6868: while `body.terminal-dashboard-active` is set, the `.project-tab.active` highlight is suppressed via CSS — the dashboard is a cross-project view, so no project tab reads as "selected"; status dots and bell indicators are untouched. Both the grid-tile and the dedicated-view xterm instances pass `theme: readXtermTheme()` (HS-6866) so tile canvases pick up `--bg` / `--text` / `--accent` instead of xterm's default black palette — same helper the drawer terminal uses. Grid-view tiles are non-interactive (`pointer-events: none` on `.terminal-dashboard-tile-xterm`, helper textarea hidden). `teardownAllTiles()` disposes xterm + closes WebSockets on exit or project switch — PTYs are left alone. Lazy / exited tiles keep the placeholder stub; HS-6838 will replace that with a spawn-on-enlarge flow. Zoom / dedicated view, bell indicators, and placeholder spawn are owned by HS-6835 through HS-6838. HS-7096 (no animation on slider / window resize): the `.terminal-dashboard-tile-preview` CSS no longer transitions `width` / `height` — slider drags and window resizes must snap, not animate, or the xterm `transform: scale(..)` chases the animating preview box instead of matching it; the only size-change animation is the tile-root FLIP transform during center / uncenter. HS-7097 (rescale via `.xterm-screen` ResizeObserver — NOT a rAF chain): `mountTileXterm` installs a per-tile `ResizeObserver` on the `.xterm-screen` element and calls `reapplyTileScaleFromPreview(tile)` every time xterm's renderer commits new inline `width` / `height` (initial mount, post-`term.resize()`, font-load reflow, anything). Replaces the earlier two-rAF chain in `connectTileSocket`'s history handler, which raced xterm's own render scheduler and could fire while `.xterm-screen` still held pre-resize dims on first-history-after-mount. Observer is tracked on `DashboardTile.screenObserver` and disconnected in `disposeTile`. HS-7098 (dedicated-view padding + inner pane wrapper + flex-centered xterm): `.terminal-dashboard-dedicated-body` keeps `padding: 16px` (visual frame), xterm opens inside an inner `.terminal-dashboard-dedicated-pane` flex child with NO padding (so FitAddon's `getComputedStyle(parent).height / width` with the app-wide `box-sizing: border-box` reset sees the real available space — otherwise fit overcounts rows by `padding * 2 / cellHeight` and clips bottom content). Follow-up: the pane is itself a flex container with `align-items / justify-content: center` and its direct `.xterm` child uses `flex: 0 0 auto` — xterm shrinks to `.xterm-screen`'s content width (viewport / helpers are absolutely positioned, so they don't contribute to intrinsic width), and the sub-cell slop between `cols * cellW` and pane width becomes symmetric left/right padding instead of an asymmetric gap on the right. HS-7099 (REMOVED by HS-7097 follow-up): earlier policy resynced the grid tile's xterm to the dedicated view's fit dims — both live (on `term.onResize`) and as a safety net on `exitDedicatedView`. Removed because mirroring the dedicated pane's non-4:3 geometry onto the grid tile re-introduced the vertical-dead-space regression HS-7097 fixed. `tileResyncOnExitDedicated` is gone from `terminalDashboardSizing.ts`; the tile now stays at tile-native 4:3 for the whole dedicated-view lifecycle. HS-7129 (default slider value 33): `pages.tsx` starts `#terminal-dashboard-size-slider` at `value="33"` and the module-level `sliderValue` default is `33` — previous `50` produced tiles large enough that only 1–2 fit per row on a typical laptop, defeating the dashboard's "see every terminal at a glance" purpose. HS-7228 (dashboard `+` button eager-spawns): `createDashboardTerminal` passes `{ spawn: true }` to `POST /api/terminal/create` (the drawer's `+` button does not — it relies on `selectDrawerTab` attaching a WS to trigger the spawn). `src/routes/terminal.ts`'s `/create` handler, when `spawn: true` is set, calls `ensureSpawned(secret, dataDir, id, config)` so the PTY launches synchronously during the request; without this, the dashboard's new tile would render as a cold `not_spawned` placeholder and the user would have to click it to get a running shell. See docs/25-terminal-dashboard.md.

**Embedded terminal:** `terminal.tsx` + `terminalOsc133.ts` (HS-7267 pure helpers `parseOsc133ExitCode` / `exitCodeGutterClass` plus HS-7268 `computeLastOutputRange(input)` — takes `{ current, commands, cursorLine }` from the per-terminal `shellIntegration` state and returns `{ start, end } | null` giving the absolute xterm buffer line range of the latest command's output. Prefers an alive in-flight record (running command — end = cursorLine + 1), falls back to the latest completed record (end = commandEnd.line when D is alive, else cursorLine + 1), returns null when no records exist / C missing / C disposed / range empty. `terminal.tsx`'s `copyLastOutput(inst)` walks the range via `term.buffer.active.getLine(y).translateToString(true)`, trims trailing blanks, writes to `navigator.clipboard`, flashes the `.terminal-copy-output-btn` green on success or shakes it on no-op. Button visibility is gated on `shellIntegration.enabled` AND the `shell_integration_ui` setting via `applyShellIntegrationToolbarVisibility` — hidden in initial markup, revealed on first OSC 133 A, re-hidden by `resetShellIntegration` on PTY restart or by unchecking Settings → Terminal → "Enable shell integration UI". HS-7269 `findPromptLine({promptLines, fromLine, direction})` pure helper returns the newest line below / oldest line above `fromLine` (used by `jumpToPromptMarker` on `Cmd/Ctrl+Up/Down` via `term.attachCustomKeyEventHandler` → `term.scrollToLine`). `attachGutterHoverPopover(el, term, record)` mounts a per-glyph hover popover with Copy command (`readRecordCommand` B→C) / Copy output (`readRecordOutput` C→D) / Rerun (`term.paste(commandText + '\r')`) actions; single shared popover element reused across hovers, closes via 200 ms timer on mouseleave cancelled on popover re-enter. `shell_integration_ui: boolean` per-project setting (default true, `AppSettings`) gates the whole Phase 2 UI; toggling fires `hotsheet:shell-integration-ui-changed` which triggers `reapplyShellIntegrationDecorations` + `applyShellIntegrationToolbarVisibility` across all instances without a PTY restart. HS-7270 `buildAskClaudePrompt({command, exitCode, cwd, output, maxOutputChars})` pure helper assembles the canonical Claude-Channel diagnose-and-fix template with cwd-clause omission / null-exit-code wording / empty-output placeholder / tail-truncation (default 8 000 chars); `askClaudeAboutRecord(inst, term, record)` in `terminal.tsx` reads the record's B→C + C→D ranges, calls the helper with `inst.runtimeCwd` (OSC 7 from §29), and dispatches via `triggerChannelAndMarkBusy(message)` from channelUI. Ask Claude button in the popover is conditionally rendered at open time via `isChannelAlive()` (accent-coloured + bold so it reads as the headline action; absent entirely when the channel is down). See docs/31-osc133-copy-last-output.md + docs/32-osc133-jump-and-popover.md + docs/33-osc133-ask-claude.md. 41 unit tests in `terminalOsc133.test.ts`), + `terminalTabSelection.ts` (HS-7275 — pure `pickNearestTerminalTabId(orderBeforeClose, closedIds)` helper used by the new `selectFallbackAfterClose` flow in `terminal.tsx`; after a close, walk right from the first closed tab's position in the pre-close tab-strip order for a surviving tab, then left, fall back to Commands Log when no terminal survives, and skip entirely when the drawer was already on a non-closed tab — `commandLog.tsx` now exports `getActiveDrawerTab()` for that check. Covered by 10 unit tests in `terminalTabSelection.test.ts` plus an e2e path in `e2e/terminal.spec.ts`). The in-drawer close entry points all route through this fallback: the × button on the tab, the context-menu Close Tab, and the bulk Close Others / Left / Right flows (which snapshot the order before the first close so the anchor is the original first-closed position, not one that shifts mid-loop). `terminal.tsx` (per-terminal tab state, xterm mount, WebSocket, stop/start power button, `onProjectSwitch` teardown so tabs rebuild per-project, `onTitleChange` → `runtimeTitle` and `onBell` → `hasBell` for HS-6473. `activateTerminal()` runs `doFit()` BEFORE `connect()` so the WebSocket URL (`?project=&terminal=&cols=N&rows=M`) carries the real pane geometry instead of xterm's default 80×24 — the server uses those dims for the eager-spawn first-attach cleanup (HS-6799). History-replay on subsequent attaches is delegated to `terminalReplay.ts`'s `replayHistoryToTerm(term, h)` which resizes xterm to the history's origin cols/rows BEFORE writing the bytes — writing at xterm's own 80×24 default followed by a resize leaves stray glyphs from wrapped escape sequences (HS-6799) — **only the in-pane terminal toolbar** tracks `runtimeTitle`; the drawer tab keeps its static configured/derived name so long per-cwd shell titles don't clutter the narrow tab (HS-6473 follow-up). Bell glyph wiggles on the drawer tab when the bell fires while the tab isn't active. HS-6603 Phase 2 seed: `loadAndRenderTerminalTabs()` reads the server-side `bellPending` field from `/api/terminal/list` entries and seeds `inst.hasBell` accordingly; a `subscribeToBellState` hook re-syncs the active project's in-drawer indicators on every bellPoll tick; `activateTerminal()` fires-and-forgets `POST /api/terminal/clear-bell` so the server-side flag drops. HS-6701: closing an alive dynamic tab (via × button, right-click → Close Tab, or Close Others / Close Tabs to the Left / Close Tabs to the Right) now routes through an in-app `confirmDialog` — single-tab flow reveals the target tab first; bulk flow shows a single "Stop all running terminals?" dialog listing the alive tab names. Tabs whose status is `exited` / `not-connected` still close silently. Tab context menu (HS-6470) also includes a **Rename...** entry (HS-6668) that opens `promptRenameTerminal(inst)` — a transient in-memory rename that updates `config.name` on the client instance but never persists to `settings.json`), `terminalsSettings.tsx` (settings outline list + edit modal for configured default terminals). Per-project drawer state (`drawer_open`, `drawer_active_tab`) persists in `settings.json` and is reapplied by `commandLog.applyPerProjectDrawerState` on project switch and on initial load.

**Plugins:** `pluginSettings.tsx`, `pluginConfigDialog.tsx`, `pluginUI.tsx`.

**Undo stack:** `undo/{stack.ts,actions.ts,types.ts}`.

**Constants:** `constants/{timers.ts,unicode.ts}`.

**Styling:** `styles.scss` (single entry; see conventions — split into partials as it grows).

**Assets:** `assets/` — app icon PNGs (`icon-default.png`, `icon-variant-1..9.png`, plus `glassbox-icon.png`).

**Other client modules not separately narrated above** (one-liner each — see file headers / git blame for full context):
- `editDiffPreview.tsx` — HS-7951 inline unified-diff preview for the Edit-tool permission overlay (replaces the flat-JSON dump for `old_string` / `new_string`).
- `permissionDialogShell.tsx` — HS-8066 shared chrome (chip / title / body / actions / always-affordance / lifecycle callbacks) consumed by `permissionOverlay.tsx` (§47) and `terminalPromptOverlay.tsx` (§52). HS-8156 caps long titles (`>200` chars or any newline) to a single-line summary in the header (full text exposed via `title` attribute) and prepends a scroll-bounded `.dialog-shell-title-overflow` block at the top of the body slot so the user can read the whole thing in the scrollable details area.
- `projectTabsFingerprint.ts` — pure-helper fingerprint of the project-tab list shape used by `projectTabs.tsx` to skip rebuilds when the visible tab set is unchanged.
- `terminalSnapshot.ts` — **deleted in HS-8177 (2026-05-04)**. Pre-deletion: client-side scrollback snapshot helper used by §47 permission popup (HS-7999 → HS-8139 → HS-8158 → HS-8159 iterations). HS-8171 v2 replaced the popup's snapshot path with a real §54 `checkout()` of the live terminal; the snapshot module then had zero production callers. Deleted alongside the pause/drain plumbing it depended on (`pauseEntryWrites` / `takePausedBytes` / `resumeEntryWritesAndDrain` / `sendPtyResize` exports + `paused` / `pausedBytes` fields on `StackEntry` in `terminalCheckout.tsx`). A future surface needing a one-shot mirror would need to reintroduce a similar capture path.

### `src/plugins/`, `src/sync/`, `src/terminals/`, `src/git/`, `src/shared/`, `src/components/`, `src/utils/`

- `plugins/{types.ts,loader.ts,syncEngine.ts}` — plugin API types, discovery/activation, bi-directional sync.
- `sync/markdown.ts` — debounced export of `worklist.md` / `open-tickets.md`.
- `terminals/` — embedded terminal backend (see `docs/22-terminal.md`):
  - `config.ts` — `TerminalConfig` type, `listTerminalConfigs` (reads `settings.terminals`, migrates legacy `terminal_command`/`terminal_cwd`), `findTerminalConfig`.
  - `eagerSpawn.ts` — `eagerSpawnTerminals(secret, dataDir)` spawns every `lazy:false` configured terminal via `ensureSpawned`. Called from `cli.ts` at project registration and from `/file-settings` PATCH when `terminals` changes.
  - `resolveCommand.ts` — resolves the chosen terminal's command template (`{{claudeCommand}}` substitution) + cwd; accepts optional `terminalId` and a `configOverride` for dynamic terminals. **HS-7991** added `resolveTerminalCwd(setting, projectDir)` pure helper: blank → project root, `{{projectDir}}` token expanded inline, absolute paths verbatim, relative paths resolved against the project root via `path.resolve`. `~` is NOT expanded.
  - `ringBuffer.ts` — FIFO byte buffer capped at a max size for scrollback.
  - `registry.ts` — `TerminalRegistry` keyed by `${secret}::${terminalId}`; lazy node-pty spawn, subscriber broadcast, restart / kill / destroy lifecycle, `ensureSpawned` (no-subscriber spawn for eager mode), `listProjectTerminalIds`, `destroyProjectTerminals`. Each session has a `hasBeenAttached` flag (HS-6799): on the first real attach to an eager-spawned PTY that provides client dims, `attach()` resizes the PTY to those dims, clears the scrollback ring buffer, and writes `\x0c` (Ctrl-L) to the PTY so the shell redraws its prompt at the right geometry — the 80×24 startup output is otherwise replayed into a wider pane and leaves stray glyphs at the top. Session also carries a `bellPending` flag (HS-6638 §24.2): the PTY `onData` handler runs an OSC-aware `scanForRealBell(session, chunk)` that tracks OSC/DCS/APC/PM/SOS string state across chunks so `\x1b]0;TITLE\x07` title updates, OSC 7 cwd updates, etc. don't trip the bell (HS-6766). Helpers `getBellPending`, `clearBellPending`, `listBellPendingForProject` expose it to the route layer; `restartTerminal` resets both the flag and the OSC-scan state. **HS-8034**: each session also owns a `promptScanner` (see `promptScanner.ts`) constructed with a closure that wires `handleScannerMatch` for server-side auto-allow gating; `pendingPrompt` slot stashes unmatched prompts for the bell-state long-poll to surface cross-project. `setPtyFactory` for tests. **HS-8189 (2026-05-06)** — split the 1006-line / 36-export god module into four behavioural slices under `src/terminals/registry/` (`attach.ts` / `lifecycle.ts` / `state.ts` / `scannerHandler.ts`) plus a shared `types.ts` (public types + internal `SessionState` shape) and `sessionStore.ts` (the `sessions` Map + `sessionKey` + constants + `resolveScrollbackBytes`). `src/terminals/registry.ts` is now a thin re-export facade preserving the public import surface so every external caller (`routes/terminal.ts`, `routes/projects.ts`, `terminals/websocket.ts`, `terminals/eagerSpawn.ts`, `cli.ts`, `lifecycle.ts`) keeps working unchanged. New file sizes: facade 65, attach 163, lifecycle 414, state 192, scannerHandler 111, types 134, sessionStore 34. The lifecycle module owns `createSession` / `spawnIntoSession` / `teardownPty` / the `activeFactory` + `setPtyFactory` / `defaultFactory` + `ensureSpawnHelperExecutable` + `scrubParentEnv` + `shouldStripEnvKey` + every destroy/kill/restart/list export; `scannerHandler.ts` owns the auto-allow gate (`handleScannerMatch` / `findMatchingRuleForProject` / `buildAutoAllowAuditStrings` / `appendAutoAllowAuditEntry` / `notifyPromptWaiters`). All 82 registry tests + 108 cross-cutting tests (registry / eagerSpawn / websocket) + 40 route tests (terminal / projects / lifecycle) + 123 api tests pass unchanged.
  - `websocket.ts` — `wireTerminalWebSocket(httpServer)` attaches a `ws.Server` (noServer mode) to the Node HTTP server; authenticates upgrade by project secret and parses `?terminal=<id>&cols=N&rows=M`; bridges ws ⇄ registry per terminalId. Client dims from the URL are forwarded into `attach()` so the PTY can be spawned/resized to match before the history frame is sent (HS-6799).
  - `claudeSpinner.ts` — HS-6702 `containsClaudeSpinner(text)` checks for the six Claude busy-spinner glyphs; powers the channel-degraded-busy indicator.
  - `processInspect.ts` — HS-7591/HS-7596 `inspectForegroundProcess(pid)` resolves the foreground child of a PTY's shell (cross-platform `ps -o comm` parsing + `normalizeComm()` basename stripper) for the quit-confirm dialog.
  - `promptScanner.ts` — HS-8029 server-side prompt scanner per `TerminalSession`. Owns a small `@xterm/headless` instance (200 cols × 30 rows, 50-row scrollback). `createPromptScanner({onMatch})` returns `{ingest, notifyUserKeystroke, setSuppressed, resize, dispose}`; ingests every PTY chunk and runs the shared parser registry from `src/shared/terminalPrompt/parsers.ts`.
  - `scrollbackSnapshot.ts` — pure helpers for capture-time scrollback snapshots: `stripAnsi`, `tailLines`, plus historical `buildScrollbackPreview` (deleted in HS-8045 cleanup; only utility helpers survive).
  - `shellHistory.ts` — HS-7965 per-(project, terminal-id) shell-history scoping. Generates per-shell init files that override `HISTFILE` after the user's rc has loaded.
  - Registered HTTP routes live in `src/routes/terminal.ts` (`/api/terminal/list` — now includes `bellPending` per entry, `/status`, `/restart`, `/kill`, `/create`, `/destroy`, `/clear-bell`, `/prompt-respond`, `/prompt-dismiss`, `/prompt-resume`). WebSocket endpoint is `/api/terminal/ws?project=<secret>&terminal=<id>`. Cross-project bell aggregation is exposed via `/api/projects/bell-state` (long-poll) in `src/routes/projects.ts`.
- `git/` — HS-7954-7956 git status tracker (core feature, NOT a plugin):
  - `status.ts` — `getGitStatus(projectDir)` wraps `git status --porcelain` + `git rev-parse --abbrev-ref HEAD` + ahead/behind via `git rev-list`. 500 ms cache.
  - `watcher.ts` — chokidar `fs.watch` on `.git/index` + `.git/HEAD` per project, debounced cache-bust. `disposeAllGitWatchers()` is wired into `lifecycle.ts::gracefulShutdown`.
- `shared/` — code shared between server and client (kept separate from `client/` so server modules can import without dragging in client deps):
  - `terminalPrompt/parsers.ts` — pure parser registry + `MatchResult` shape (HS-8029 moved here from `client/`). `claudeNumberedParser`, `yesNoParser`, `genericParser`. Helpers: `hashQuestion`, `pickTitleLine`, `isDecorativeLine`, `buildNumberedPayload`, `buildYesNoPayload`, `buildGenericPayload`, cancel-payload helpers. **HS-8210 Phase A (§58.3)** — new exported `CHANNEL_LINE_RX = /^[ \t]*Channels:\s+(\S+)\s*$/m` + new optional `channel?: string` field on `NumberedMatch`. `claudeNumberedParser.match` runs the regex against `questionLines.join('\n')` after the strip / trim / question-region walker; captures the verbatim `\S+` token (includes the `server:` prefix). Drives Tier 0 of the §58 channel-keyed allow-rule path (Phase B). **HS-8216 (2026-05-06)** — replaced the hardcoded 2-literal `NUMBERED_FOOTERS: Set<string>` with regex `NUMBERED_FOOTER_RX = /^(?:Enter to confirm|Esc to cancel)(?:\s+·\s+\S.*)?$/` so Claude Code 2026-05's `Esc to cancel · Tab to amend` footer (emitted on prompts with an "amend" affordance, e.g. `Yes, allow reading from Desktop/ during this session`) is recognised. `isClaudeNumberedFooter` now uses the regex. 7 new tests in `parsers.test.ts` cover the new variants (bare `Esc to cancel`, future ` · Tab to expand` / ` · Shift+Tab to skip` suffixes), prose-lookalike rejection (`Esc to cancel the operation` → false), and end-to-end on the user's exact LingoGist Read-permission prompt.
  - `terminalPrompt/allowRules.ts` — `findMatchingAllowRule`, `parseAllowRules`, `buildAllowRule`, `payloadForAutoAllow` (HS-8034 moved here from `client/` so the server-side scanner can call it). **HS-8210 Phase B (§58.4)** — added `match_channel?: string` to `TerminalPromptAllowRule`. `parseAllowRules` accepts empty `question_hash` only when `match_channel` is a non-empty string; non-channel rules still require a non-empty hash. Dedupe key extended to include `match_channel ?? ''` so a channel-keyed rule and a hash-keyed rule for the same `(parser_id, choice_index)` coexist. `findMatchingAllowRule` gained Tier 0 — `(parser_id, match.channel)` lookup running BEFORE Tiers 1–4, numbered-shape only, bounds-checked. Tiers 1–4 now skip rules with `match_channel !== undefined` so channel-keyed rules ONLY fire via Tier 0 (prevents a non-channel prompt's preview from accidentally tripping Tier 3 on a channel rule). New `buildChannelAllowRule(match: NumberedMatch, choiceIndex, choiceLabel)` helper (throws when `match.channel` is undefined; produces a rule with empty `question_hash` + populated `match_channel`).
- `components/layout.tsx` — the server HTML shell.
- `utils/{escapeHtml.ts, errorMessage.ts}` — small shared helpers.

### Out-of-src

- `plugins/{github-issues,demo-plugin}/` — bundled plugins (each has `src/` + `manifest.json`).
- `e2e/` — Playwright specs (lifecycle, plugins, sync-*, channel, columns, keyboard, etc.) + `coverage-fixture.ts`.
- `src-tauri/` — Rust Tauri v2 wrapper: `src/{main.rs,lib.rs}`, `capabilities/{default,remote-localhost}.json`, `icons/`, `resources/` (CLI launchers), `binaries/` (Node sidecar), `server/` (bundled app).
- `docs/` — requirements (see `docs/ai/requirements-summary.md` for a synthesized view).
- `dist/` — build output (`cli.js`, `channel.js`, `client/{app.global.js,styles.css,assets/}`, `plugins/*`).

---

## 4. API routes catalog

All API calls require header `X-Hotsheet-Secret: <settings.secret>` for non-localhost or mutating requests. Mutation requests may include `X-Hotsheet-User-Action: true` to bump `last_read_at` (controls the unread dot).

**Tickets:** `GET /api/tickets` (filters: category, priority, status, up_next, search, sort_by, sort_dir, **`include_backlog`**, **`include_archive`** — HS-7756; composite status: `open`, `non_verified`, `active`); `POST /api/tickets`; `GET|PATCH|DELETE /api/tickets/:id`; `DELETE /api/tickets/:id/hard`; `POST /:id/restore`; `POST /:id/up-next`; `POST /api/tickets/batch`; `POST /api/tickets/duplicate`; `POST /api/tickets/query` (custom views); **`GET /api/tickets/search-counts`** (HS-7756 — backlog + archive match counts for the "Include …" rows); **`GET /api/tickets/prefixes`** (HS-8036 — distinct ticket-number prefixes for the link-detection regex); **`GET /api/tickets/by-number/:number`** (HS-8036 — ticket lookup by `ticket_number` for the stacking ref dialog); **`GET|POST|PATCH|DELETE /api/tickets/:id/feedback-drafts[/:draftId]`** (HS-7599 — local-only feedback-dialog drafts).

**Notes:** `PUT /api/tickets/:id/notes-bulk`, `PATCH|DELETE /api/tickets/:id/notes/:noteId`.

**Attachments:** `POST /api/tickets/:id/attachments`; `DELETE /api/attachments/:id`; `POST /api/attachments/:id/reveal`; `GET /api/attachments/file/*`.

**Settings:** `GET|PUT /api/categories`, `GET /api/category-presets`, `GET /api/tags`, `GET|PATCH /api/settings` (DB, plugin-only), `GET|PATCH /api/file-settings`.

**Dashboard/poll:** `GET /api/poll` (long-poll), `/api/stats`, `/api/dashboard`, `/api/worklist-info`, `/api/browse`, `/api/global-config`.

**Channel / MCP:** `GET /api/channel/status`, `POST /api/channel/trigger`, `GET /api/channel/heartbeat`, `GET /api/channel/claude-check`, `GET /api/channel/permission`, `POST /api/channel/permission/{respond,dismiss,notify}`, `POST /api/channel/done`.

**Command log:** `GET|DELETE /api/command-log`, `GET /api/command-log/count`, `POST /api/terminal-prompt/audit` (HS-7987 §52 — writes a `terminal_prompt_auto_allow` event_type entry).

**Shell:** `POST /api/shell/exec`, `POST /api/shell/kill`, `GET /api/shell/running`.

**Terminal:** `GET /api/terminal/list`, `GET /api/terminal/status`, `POST /api/terminal/restart`, `POST /api/terminal/kill` (body `{ signal?, terminalId? }`), `POST /api/terminal/create`, `POST /api/terminal/destroy`, `POST /api/terminal/clear-bell` (HS-6638 §24.3.2), `GET /api/terminal/foreground-process` (HS-7596), `GET /api/terminal/command-suggestions` (HS-7791), `POST /api/terminal/open-cwd` (HS-7262 — opens the OSC 7 cwd in the OS file manager), `POST /api/terminal/prompt-respond` / `POST /api/terminal/prompt-dismiss` / `POST /api/terminal/prompt-resume` (HS-8034 — server-side prompt scanner cross-project surface). WebSocket upgrade at `/api/terminal/ws?project=<secret>&terminal=<id>`.

**Plugins:** `GET /api/plugins`, `GET /api/plugins/ui`, `POST /api/plugins/:id/{action,validate,config,test}`, `PATCH /api/plugins/:id/config`, `POST /api/plugins/sync/manual`, `GET /api/plugins/:id/conflicts`, `POST /api/plugins/:id/conflicts/:cid/resolve`, `PATCH /api/plugins/global-config`, `GET|POST|DELETE /api/plugins/schedules[...]`, `POST /api/plugins/bundled/:id/install`, `GET /api/plugins/:id/image-proxy`.

**Backups:** `GET /api/backups`, `POST /api/backups/restore/:id` (with preview flow).

**Projects (multi-project):** `GET|POST /api/projects`, `DELETE /api/projects/:secret`, `GET /api/projects/:secret`, `POST /api/projects/reorder`, `POST /api/projects/:secret/reveal`, `GET /api/projects/permissions` (long-poll), `GET /api/projects/bell-state` (HS-6638 §24.3.3 — long-poll, includes `pendingPrompts` per HS-8034), `GET /api/projects/channel-status`, `GET /api/projects/quit-summary` (HS-7596 — cross-project foreground-process aggregation for the quit-confirm dialog).

**Git (HS-7598):** `GET /api/git/status` (optional `?files=true` for the popover bucket lists), `POST /api/git/fetch`, `POST /api/git/reveal` (opens a path under the git root in the OS file manager).

**Database (HS-7897 / HS-7899):** `GET /api/db/recovery-status`, `POST /api/db/dismiss-recovery`, `POST /api/db/repair/find-working-backup`, `GET /api/db/repair/pg-resetwal-availability`, `POST /api/db/repair/run-pg-resetwal`.

**Diagnostics (HS-8054 v3):** `POST /api/diagnostics/freeze` (client posts long-task / heartbeat events; server appends to `<dataDir>/freeze.log`).

**Server control:** `POST /api/shutdown`.

**Static/HTML:** `GET /` (page), `GET /static/app.js`, `GET /static/styles.css`, `GET /static/assets/*`.

---

## 5. Database schema

Schema lives in one place: `src/db/connection.ts` `initSchema()` function — all `CREATE TABLE` + additive `ALTER TABLE IF NOT EXISTS` migrations go there.

| Table | Key columns | Notes |
|---|---|---|
| `tickets` | `id SERIAL PK`, `ticket_number TEXT UNIQUE`, `title`, `details`, `category`, `priority`, `status`, `up_next BOOL`, `created_at/updated_at/completed_at/deleted_at/verified_at TIMESTAMPTZ`, `notes TEXT` (JSON array), `tags TEXT` (JSON array), `last_read_at TIMESTAMPTZ` | `idx_tickets_status`, `idx_tickets_up_next` |
| `ticket_seq` | sequence starting 1 | Source of `HS-N` ticket numbers |
| `attachments` | `id`, `ticket_id (FK→tickets CASCADE)`, `original_filename`, `stored_path`, `created_at` | `idx_attachments_ticket` |
| `settings` | `key TEXT PK`, `value TEXT` | **Plugin-only.** Project/app config moved to `.hotsheet/settings.json`. Plugin keys: `plugin:{id}:{key}`, `plugin_enabled:{id}` |
| `stats_snapshots` | `date TEXT PK`, `data TEXT` (JSON) | Dashboard historical charts |
| `command_log` | `id`, `event_type`, `direction`, `summary`, `detail`, `created_at` | `idx_command_log_created`. Capped at ~1000 entries. Event types in code today (HS-8077 audit): `trigger`, `done`, `permission_request`, `shell_command`, `push` (plugin push-ticket — HS-7115 lineage), `sync` (plugin sync engine), `terminal_prompt_auto_allow` (HS-8034 §52 Phase 2 — server-side auto-allow on a `terminal_prompt_allow_rules` match). |
| `ticket_sync` | `ticket_id (FK)`, `plugin_id`, `remote_id`, `last_synced_at`, `remote_updated_at`, `local_updated_at`, `sync_status`, `conflict_data`. UNIQUE(ticket_id, plugin_id) | Indexes on plugin_id, sync_status, (plugin_id, remote_id) |
| `sync_outbox` | `ticket_id`, `plugin_id`, `action` (`create`/`delete`), `field_changes`, `attempts`, `last_error` | 5+ failed attempts → permanent removal |
| `note_sync` | `ticket_id`, `note_id`, `plugin_id`, `remote_comment_id`, `last_synced_at`, `last_synced_text`. UNIQUE(ticket_id, note_id, plugin_id) | `last_synced_text` = baseline for three-way merge |
| `feedback_drafts` | `id TEXT PK`, `ticket_id (FK→tickets CASCADE)`, `parent_note_id TEXT?` (nulled when parent deleted; HS-7599 §21), `prompt_text TEXT` (snapshot of FEEDBACK NEEDED prompt at save-time so click-to-reopen works after parent deletion), `partitions_json TEXT` (block structure + inline responses + catch-all verbatim per §21.2.3), `created_at/updated_at TIMESTAMPTZ` | `idx_feedback_drafts_ticket`, `idx_feedback_drafts_parent_note`. Local-only — drafts never sync to GitHub / plugin backends. |

All timestamp columns are `TIMESTAMPTZ` (older DBs migrated in place).

---

## 6. Client bundle & globals

- **Single IIFE:** `dist/client/app.global.js` built from `src/client/app.tsx`. IIFE format, ES2020 target, minified.
- **Styles:** `dist/client/styles.css` compiled from `src/client/styles.scss`.
- **Assets:** `dist/client/assets/*` (icons, lucide JSON).
- **Globals:** the client does not currently expose a stable `window.*` API. State is module-local in `src/client/state.tsx`; `window.__TAURI__` is used only in Tauri builds. **HS-8054** adds three diagnostic globals — `window.__hotsheetGetLongTasks()`, `window.__hotsheetClearLongTasks()`, `window.__hotsheetGetInteractions()` — for grabbing the in-memory longtask + interaction buffers from DevTools when the user notices a hang.
- **JSX in client:** always use `toElement(<…/>)` from `src/client/dom.ts` to get real DOM nodes. Never call `document.createElement` directly. For `innerHTML` strings, use `<…/>.toString()`.

---

## 7. Plugin system (`src/plugins/`)

- **Feature flag:** `PLUGINS_ENABLED` in `src/feature-flags.ts` (default true; browser-safe via global fallback).
- **Install location:** `~/.hotsheet/plugins/<id>/` (global, user-wide). Bundled plugins under `plugins/*` are copied there by `installBundledPlugins()` on startup if missing or outdated; dismissed plugins tracked in `~/.hotsheet/dismissed-plugins.json`.
- **Manifest:** `manifest.json` (or `package.json#hotsheet`). Fields: `id`, `name`, `version`, `description?`, `author?`, `entry?`, `icon?` (inline SVG), `preferences?`, `configLayout?`.
- **Preferences:** `{ key, label, type: string|boolean|number|select|dropdown|combo, default?, description?, required?, secret?, scope?: "global"|"project", options? }`. `secret:true` routes through the keychain (`src/keychain.ts`) with dual-write file fallback.
- **Entry exports:** `activate(ctx)`, `onAction(actionId, ctx)`, `validateField(key, value)`.
- **PluginContext:** `config`, `log(level,msg)`, `getSetting/setSetting`, `registerUI(elements)`, `updateConfigLabel(id, text, color?)`.
- **TicketingBackend:** `id`, `name`, `capabilities` (create/update/delete/incrementalPull/comments/syncableFields), `fieldMappings` (category/priority/status toRemote+toLocal), CRUD (`createRemote/updateRemote/deleteRemote`), sync (`pullChanges(since)`, `getRemoteTicket?`), status (`checkConnection`, `getRemoteUrl?`, `shouldAutoSync?`), comments (`getComments/createComment/updateComment/deleteComment`), attachments (`uploadAttachment?`).
- **UI extensions:** 8 locations — `toolbar`, `status_bar`, `sidebar_actions_top`, `sidebar_actions_bottom`, `detail_top`, `detail_bottom`, `batch_menu`, `context_menu`. Element types: `button`, `link` (rendered); `toggle`, `switch`, `segmented_control` (declared, not yet rendered).
- **Sync engine (`syncEngine.ts`):** push via direct-compare (`ticket.updated_at` vs `sync.local_updated_at`); pull via `pullChanges(since)`; comments via three-way merge using `note_sync.last_synced_text`; attachments uploaded, then posted as markdown link.
- **Bundled plugins:** `plugins/github-issues/` (real backend), `plugins/demo-plugin/` (exercises every surface — see `docs/19-demo-plugin.md`).
- **Plugin image proxy:** `GET /api/plugins/:id/image-proxy` — proxies private-repo images with stored PAT.

---

## 8. Channel / MCP (`src/channel.ts`)

- **Version:** `CHANNEL_VERSION = 4`. `EXPECTED_CHANNEL_VERSION` in `src/channel-config.ts` must match. Bump **both** when changing the channel HTTP surface or MCP behavior.
- **Launch paths:** production = `node dist/channel.js --data-dir <path>`; dev = `npx tsx src/channel.ts`. Both are registered in `.mcp.json` by `src/channel-config.ts`.
- **Transport:** stdio (MCP) to Claude Code + a local HTTP port (written to `.hotsheet/channel-port`).
- **Channel HTTP:** `/health`, `/permission` (120s expiry), `POST /permission/respond`, `POST /permission/dismiss`, `POST /permission/inject` (HS-8205 debug injector — used by `scripts/simulate-claude-prompts.mjs` permission-* types; reads `<dataDir>/channel-port` to find the running server).
- **Hot Sheet ↔ Channel:** Hot Sheet `/api/channel/trigger` POSTs to the running channel; Hot Sheet polls `/api/channel/permission` (long-poll) for pending Claude tool-use approvals.
- **Status / auto mode:** sidebar play button (see `src/client/channelUI.tsx`). Heartbeat hooks installed into `~/.claude/settings.json` by `src/claude-hooks.ts` (PostToolUse/UserPromptSubmit/Stop).
- **Worklist file:** `src/sync/markdown.ts` builds `.hotsheet/worklist.md` with curl examples the AI session can run; `src/skills.ts` maintains the `.claude/skills/hotsheet` skill.

---

## 9. Tauri desktop (`src-tauri/`)

- **Layout:** `src/{main.rs,lib.rs}` (minimal stub + app setup), `tauri.conf.json`, `Cargo.toml`, `Entitlements.plist`, `capabilities/{default,remote-localhost}.json`, `icons/`, `resources/` (per-platform CLI launchers), `binaries/` (bundled Node), `server/` (bundled JS + assets).
- **Sidecar model:** Node.js is packaged as a sidecar because PGLite (WASM) needs filesystem access — single-binary compilation breaks it. Tauri either (a) connects to a pre-started server via `HOTSHEET_SERVER_URL` env, or (b) spawns the sidecar and waits for `running at` on stdout.
- **Commands exposed to JS:** `quicklook`, `set_app_icon`, `install_cli`, `check_cli_installed`, `check_for_update`, `install_update`, `get_pending_update`, `request_attention[_once]`, `open_url`, `pick_folder`, `open_project`, `confirm_quit` (HS-7596 — second `WindowEvent::CloseRequested` after the JS quit-confirm flow accepted), `show_native_notification` (HS-7272 — `tauri-plugin-notification` wrapper invoked from `bellPoll.maybeFireNotificationToast` when `document.hidden || !document.hasFocus()` so OSC 9 messages also surface in the OS Notification Center; Tauri permission is primed once from `app.tsx` boot via `requestNativeNotificationPermission()` in `tauriIntegration.tsx`).
- **Icon variants:** 9 `.icns` files in `src-tauri/icons/variants/`; runtime change via `set_app_icon` using `NSApplication.setApplicationIconImage()` on macOS; `Window::set_icon()` on Windows/Linux.
- **Updates:** GitHub Releases, signed, verified against `plugins.updater.pubkey` in `tauri.conf.json`.

---

## 10. Build system

From `tsup.config.ts` + `package.json` scripts:

- `npm run build` → tsup builds three ESM outputs: server (`src/cli.ts` → `dist/cli.js`, externals: pglite, hono, @hono/node-server), channel (`src/channel.ts` → `dist/channel.js`, fully bundled), client (`src/client/app.tsx` → `dist/client/app.global.js` IIFE minified) + SCSS → `dist/client/styles.css` + asset copy + **append `node_modules/@xterm/xterm/css/xterm.css`** to `styles.css` (HS-6799: without this, the Tauri sidecar bundle ships a styles.css missing xterm's helper-textarea / viewport positioning rules).
- `npm run build:client` — client bundle + SCSS + assets only (used by `dev`). Also appends xterm.css to styles.css.
- `npm run build:plugins` — loops `plugins/*` and emits into `dist/plugins/*` with `manifest.json` copied.
- `npm run dev` — `build:client` + `build:plugins` + `tsx src/cli.ts`.
- `npm run tauri:dev` / `npm run tauri:build` — Tauri dev window / release package.

Env flags: `PLUGINS_ENABLED` (build/runtime toggle), `NO_WEB_SERVER` (E2E), `NODE_V8_COVERAGE` (coverage collection).

---

## 11. Testing

- **Unit tests** (`src/**/*.test.ts`): vitest, `pool: 'forks'`, coverage via `@vitest/coverage-v8`. Use `setupTestDb`/`cleanupTestDb` from `src/test-helpers.ts`.
- **E2E tests** (`e2e/*.spec.ts`): Playwright Chromium. Each spec spawns a real Hot Sheet server with a temp data dir. Coverage collected via `NODE_V8_COVERAGE` (server) + `page.coverage.startJSCoverage()` (browser), source-mapped back to `.tsx` sources. See `e2e/coverage-fixture.ts`.
- **Plugin tests** (`plugins/*/src/*.test.ts`): excluded from default `npm test`; run via `test:all-including-plugins` or directly.
- **CI scripts:** `test:fast` / `test:e2e:fast` exclude live GitHub integration tests. Full `test:e2e` requires local GitHub creds.
- **Manual test plan:** `docs/manual-test-plan.md` — anything not reliably automatable (drag-and-drop, platform specifics, Tauri, visual styling, Claude Channel UI).

---

## 12. Settings & config locations

**Per-project (`.hotsheet/`):**
- `settings.json` — reserved: `appName`, `appIcon`, `backupDir`, `ticketPrefix`, `secret`, `secretPathHash`, `port`. User: `categories`, `custom_views`, `custom_commands`, `auto_context`, `auto_order`, `terminals` (defaults to `[]` per HS-6337 — no implicit default terminal), `terminal_scrollback_bytes`, `drawer_open`, `drawer_active_tab`, `permission_allow_rules` (HS-7952 / §47.4 — array of `{ tool, pattern }` rules read by `src/permissionAllowRules.ts::loadPermissionAllowRules` for the channel-permission auto-allow gate), `terminal_prompt_allow_rules` (HS-7972 / §52 — terminal-prompt-overlay always-allow rules read by `src/shared/terminalPrompt/allowRules.ts`), layout/position/widths, etc. The legacy `terminal_enabled` key is ignored on read (removed in HS-6337 when terminals moved to a dedicated Tauri-only Settings tab). JSON-typed keys (e.g. `terminals`, `custom_views`, `custom_commands`, `auto_context`, `categories`, `permission_allow_rules`, `terminal_prompt_allow_rules`) are stored as native JSON arrays/objects, not stringified — `/api/file-settings` PATCH uses `UpdateFileSettingsSchema = z.record(z.string(), z.unknown())` to accept native values.
- `db/` — PGLite database files.
- `attachments/` — uploaded files.
- `backups/{5min,hourly,daily}/` — tar.gz DB snapshots; each tarball is paired with a `.json.gz` sibling holding the full row dump (HS-7893 escape hatch).
- `worklist.md`, `open-tickets.md` — AI-facing markdown exports (auto-generated; do not edit).
- `hotsheet.lock` — single-instance lock (pid/startedAt).
- `channel-port` — channel server's HTTP port (when running).

**Global (`~/.hotsheet/`):**
- `config.json` — `channelEnabled`, `shareTotalSeconds`, `shareLastPrompted`, `shareAccepted`.
- `instance.json` — running instance PID + port (for single-instance mode + multi-project join).
- `projects.json` — registered projects (secret → dataDir).
- `plugins/` — installed plugins.
- `dismissed-plugins.json` — plugins the user has dismissed.

**Claude/AI:** `~/.claude/settings.json` (heartbeat hooks managed by `src/claude-hooks.ts`), `.mcp.json` (channel registration managed by `src/channel-config.ts`), `.claude/skills/hotsheet/SKILL.md` + `.claude/skills/hs-{category}/SKILL.md` (generated by `src/skills.ts`; versioned via `<!-- hotsheet-skill-version: N -->`).

---

## 13. Where do I look for X — reverse index

| Want to… | Edit |
|---|---|
| Add a new HTTP endpoint | A file under `src/routes/`, register it in `src/routes/api.ts`; add a Zod schema in `src/routes/validation.ts` if it takes a body |
| Add/alter a DB column | `initSchema()` in `src/db/connection.ts` (use `ALTER TABLE … ADD COLUMN IF NOT EXISTS` for migrations); update `src/types.ts` `Ticket`; update the relevant query helper in `src/db/*.ts` |
| Add a ticket category preset | `DEFAULT_CATEGORIES` / `CATEGORY_PRESETS` in `src/types.ts` |
| Add a client UI module | New `src/client/foo.tsx`, import and wire from `src/client/app.tsx` |
| Add a keyboard shortcut | `src/client/shortcuts.tsx` |
| Add a filter or sort option | `TicketFilters` in `src/types.ts`, query param in `src/routes/tickets.ts`, UI in `src/client/sidebar.tsx` |
| Add a dashboard metric | `src/db/stats.ts` + `src/routes/dashboard.ts` + `src/client/dashboard.tsx` |
| Add a skill or change its text | `src/skills.ts` — bump the `hotsheet-skill-version` header so skills regenerate |
| Change the worklist export | `src/sync/markdown.ts` (debounced) |
| Add/change an MCP endpoint | `src/channel.ts` **and** bump `CHANNEL_VERSION` + `EXPECTED_CHANNEL_VERSION` in `src/channel-config.ts` |
| Add a Tauri native command | `src-tauri/src/lib.rs` (new `#[tauri::command]`), invoke from `src/client/tauriIntegration.tsx` |
| Add a plugin UI location or element type | Extend `PluginUILocation` / element types in `src/plugins/types.ts`; render in `src/client/pluginUI.tsx` |
| Add a plugin sync capability | Extend `TicketingBackend` in `src/plugins/types.ts`; handle in `src/plugins/syncEngine.ts`; add DB columns via `initSchema()` if needed |
| Add a channel event type | `src/channel.ts` (MCP), `src/routes/channel.ts` (HTTP), `src/db/commandLog.ts` (log), `src/client/commandLog.tsx` (UI) |
| Add a setting to `settings.json` | `src/file-settings.ts` (reserved-key filter if needed), read where you need it; for UI, `src/client/settingsDialog.tsx` tabs |
| Add a global config field | `src/global-config.ts` + `src/routes/dashboard.ts` (`/api/global-config`) |
| Add an auto-cleanup rule | `src/cleanup.ts` |
| Add a backup tier / policy | `src/backup.ts` |
| Add a Claude hook | `src/claude-hooks.ts` |
| Change the keychain fallback | `src/keychain.ts`; plugin integration in `src/plugins/loader.ts` |
| Add an E2E test | `e2e/<topic>.spec.ts` — start real server with a temp data dir, prefer real interactions over mocks |
| Add a git-status surface (chip / popover / route) | Server: `src/git/status.ts` (status query) + `src/routes/git.ts` (`/api/git/*`). Client: `src/client/gitStatusChip.tsx` (sidebar chip) + `src/client/gitStatusPopover.tsx` (expanded popover). See §48. |
| Add a terminal-prompt parser | `src/shared/terminalPrompt/parsers.ts` (pure parser registry — `claude-numbered`, `yesno`, `generic`). Server-side scanner driver: `src/terminals/promptScanner.ts`. Client overlay: `src/client/terminalPromptOverlay.tsx`. Allow-rule store: `src/client/terminalPrompt/allowRulesStore.ts`. See §52. |
| Add a ticket cross-reference behaviour | Linkifier: `src/client/ticketRefs.ts`. Stacking dialog + global click handler: `src/client/ticketRefDialog.tsx`. Server prefix lookup: `GET /api/tickets/prefixes` + `GET /api/tickets/by-number/:number` in `src/routes/tickets.ts`. See §55. |
| Wire a new consumer of the terminal LIFO stack | Call `checkout({...})` from `src/client/terminalCheckout.tsx`. Per-`(secret, terminalId)` xterm + WebSocket lifecycle is owned by the module; consumers describe `mountInto`, `cols`/`rows`, optional `onBumpedDown` / `onRestoredToTop` / `onControlMessage` callbacks. See §54. |
| Add a reader-mode trigger surface | `src/client/readerOverlay.tsx::openReaderOverlay({title, markdown})`. Existing triggers: per-note book button (`noteRenderer.tsx`) and the Details `<label>` row (`detail.tsx`). See §49. |
| Add a quit-confirm exempt-process default | `quit_confirm_exempt_processes` JSON array in `.hotsheet/settings.json` — UI in Settings → Terminal. Foreground-process inspection in `src/terminals/processInspect.ts`. Quit-confirm wiring in `src/client/quitConfirm.tsx` + Tauri command `confirm_quit` in `src-tauri/src/lib.rs`. See §37. |
| Add a Tauri menu item | `src-tauri/src/lib.rs` (`MenuBuilder` block in `setup()` and the matching `#[tauri::command]` for the action). Sidebar / menu wiring on the JS side via `src/client/tauriIntegration.tsx::getTauriInvoke()`. |
| Add a visibility-grouping behaviour | `visibility_groupings` array + `active_visibility_grouping_id` + `hidden_terminals` mirror in `.hotsheet/settings.json`. Client: `src/client/visibilityGroupings.ts` (state + selector logic), `src/client/visibilityGroupingSelect.ts` (dropdown), `src/client/hideTerminalDialog.tsx` (multi-tab Show/Hide dialog). See §39. |

---

## 14. Conventions that aren't obvious from the code

- **One primary export per file.** Private helpers live alongside.
- **`.js` extensions on imports** (TS ESM requirement).
- **No ORM** — raw PGLite `query()` with parameterized placeholders.
- **Server-rendered shell, client-enhanced UI.** Initial HTML comes from `pages.tsx`; the IIFE hydrates interactivity.
- **All styles in one SCSS entry** (`styles.scss`). Split into `_partial.scss` files by concern when it grows.
- **Never `document.createElement` in client code** — use `toElement(<…/>)`.
- **Never commit automatically** (see CLAUDE.md — non-negotiable).
- **`CHANNEL_VERSION` mismatch is a user-visible warning.** The server compares the running channel's version against `EXPECTED_CHANNEL_VERSION` and tells the user to reconnect via `/mcp`. Always bump both.
- **Ticket numbers never reuse** even after deletion (sequence-backed).
- **Debounces:** worklist 500 ms, open-tickets 5 s, detail panel auto-save 300 ms, command log search 300 ms.
- **Module-level state is the singleton pattern (HS-8086).** Each `src/client/*.tsx` module is a singleton tied to the SPA's never-remounting lifecycle, so a module-level `let X: ... | null = null;` is functionally equivalent to a private field on a singleton class — no class wrapper needed. The audit ran across 139 such declarations in `src/client/` and found exactly one bug (HS-8102: `customViews.tsx::loadTicketsFn` typed without `| null`); every other case is correct. Rules of thumb when adding new module-level state:
  1. **Type as `... | null = null`** (not `... | undefined`, not bare `: T;` with no init) so the type system reflects the pre-init state and TypeScript forces null-checks at every call site. The `customViews.tsx::loadTicketsFn` (HS-8102) and `settingsLoader.tsx::_restoreTicketList` precedents are the reference shape.
  2. **DOM-handle mutables** (e.g. `gitStatusChip.tsx`'s 5, `drawerTerminalGrid.tsx`'s 16, `terminalDashboard.tsx`'s 22) are bound in an `init`/`bind` function during app boot and stay live for the SPA lifetime. `null` until init, `HTMLElement` after.
  3. **Singleton timers** (`pollTimer`, `channelDebounceTimeout`, etc.) — pair with `start*()` / `stop*()` helpers that null-guard before re-init and clear on stop. Examples: `commandSidebar.tsx::shellPollTimer`, `commandLog.tsx::pollTimer`.
  4. **Per-project Maps** (`Map<projectSecret, T>`) — the per-project pattern. Pruned via the project-unregister flow. Examples: `channelUI.tsx::autoModeByProject`, `bellPoll.tsx::recentlyAnsweredPrompts`.
  5. **Long-lived id Sets** (e.g. `commandLog.tsx::expandedEntryIds`, `selectedLogIds`) — bounded by total ever-created entity ids; Set membership is O(1) so growth is non-issue. Don't over-engineer pruning.

  Code-quality scans should not re-flag these as hygiene concerns. The pattern is intentional — refactor-to-class adds indirection without removing state.

---

## 15. Ticket categories (canonical)

`issue` / `bug` / `feature` / `requirement_change` / `task` / `investigation`. Priorities: `highest` / `high` / `default` / `low` / `lowest`. Statuses: `not_started` / `started` / `completed` / `verified` / `backlog` / `archive` / `deleted`. Composite filter values: `open` (= not_started+started), `non_verified` (= open+completed), `active` (excludes deleted/backlog/archive).

---

## 16. Related reading

- **Requirements synthesis:** `docs/ai/requirements-summary.md` (sibling file; read together).
- **Project-level conventions:** `/CLAUDE.md` (repo root).
- **Requirements docs:** `docs/1-overview.md` … `docs/21-feedback.md` + `docs/plugin-development-guide.md`, `docs/tauri-architecture.md`, `docs/tauri-setup.md`, `docs/manual-test-plan.md`, `docs/testing/*`.

---

## 17. Maintenance rules

**Update this document in the same change whenever any of these triggers fire:**

1. **New file or subdirectory under `src/`** — add it to §3.
2. **New route file or endpoint** — update §3 and §4.
3. **Schema change** (new table, column, or index in `src/db/connection.ts`) — update §5.
4. **New event type** in `command_log` or channel — update §5 / §8.
5. **New client module** under `src/client/` — add to §3.
6. **New bundle output** in `tsup.config.ts` — update §10.
7. **New plugin UI location, preference type, or `TicketingBackend` method** — update §7.
8. **Channel protocol change** (new endpoint / tool / capability) — update §8 and bump both `CHANNEL_VERSION` and `EXPECTED_CHANNEL_VERSION`.
9. **New Tauri `#[tauri::command]`** — update §9.
10. **New `.hotsheet/` or `~/.hotsheet/` file** — update §12.
11. **New setting key** exposed to plugins or users — update §12.

Prefer a *small, targeted* edit over a rewrite. The point of this document is that it stays approachable — keep it a map, not a replica of the code.
